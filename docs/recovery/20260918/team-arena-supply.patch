--- a/src/app/bootstrap/simulation/q3/runtime.ts
+++ b/src/app/bootstrap/simulation/q3/runtime.ts
@@ -179,7 +179,7 @@
         touch: (self, other) => { const actor = host.actors.resolveOwned(self);
           if (actor !== null) host.callbacks.touch({ self: actor, other, plane: null, surface: null }); return undefined; } },
       moveClient: (entity, command, options) => this.host.moveClient(entity, command, options),
-      effects: { combat: this.combat }, frame: () => this.level,
+      effects: { combat: this.combat, regenerateAmmo: (actor, rules, msec, clock) => host.regenerateAmmo?.(actor, rules, msec, clock) ?? false }, frame: () => this.level,
       settings: () => ({ synchronousClients: this.integer("g_synchronousClients") !== 0, pmoveFixed: this.integer("pmove_fixed") !== 0,
         debugMove: this.integer("g_debugMove"),
         pmoveMsec: this.integer("pmove_msec"), gravity: this.number("g_gravity"), speed: this.number("g_speed"), dmflags: this.integer("dmflags"),
--- a/src/app/bootstrap/simulation/q3/types.ts
+++ b/src/app/bootstrap/simulation/q3/types.ts
@@ -47,6 +47,7 @@
 
 /** W73 supplies the existing owners; this provider contains source game records and phase functions. */
 export interface Q3SourceHost extends ClientMovementHost {
+  readonly regenerateAmmo?: import("../../../../content/q3/team-arena/client-effects.ts").ClientEffectsContext["regenerateAmmo"];
   readonly serverState: import("./server-state.ts").Q3ServerState;
   readonly moverActors: Pick<MoverActorAccess, "observe" | "write" | "link" | "release">;
   primaryAttackAllowed?(actor: ActorId): boolean;
--- a/src/app/bootstrap/simulation/runtime.ts
+++ b/src/app/bootstrap/simulation/runtime.ts
@@ -131,7 +131,8 @@
 import { isDeepStrictEqual } from "node:util";
 import type { ContentId, ExecutableRecipe, MonsterDefinitionReference, ProviderReference, ResolvedResourceReference } from "../../../contracts/content.ts";
 import type { PickupSupplyProfile, PickupSupplyOffer } from "../../../contracts/pickups.ts";
-import type { AttackProvenance, DamageRequest, ItemId, TransitionIntent } from "../../../contracts/gameplay.ts";
+import type { AttackProvenance, DamageRequest, InventoryEntry, ItemId, TransitionIntent } from "../../../contracts/gameplay.ts";
+import type { AmmoRegenerationClock, AmmoRegenerationRule } from "../../../content/q3/team-arena/client-effects.ts";
 import type { ActorId, ClientId, OwnedActor, ProviderId } from "../../../contracts/identity.ts";
 import { sameActor } from "../../../contracts/identity.ts";
 import type { Vec3 } from "../../../contracts/math.ts";
@@ -268,7 +269,7 @@
   private selectedArsenal: Q1SelectedArsenal | Q2SelectedArsenal | Q3SelectedArsenal | null = null;
   private selectedWeaponSource: SelectedWeaponSource | null = null;
   private readonly primaryCommandBlocks = new Set<ActorId>();
-  private selectedSupply: SharedPickupAdmission | null = null;
+  private selectedSupply: { readonly profile: PickupSupplyProfile; readonly admission: SharedPickupAdmission } | null = null;
   private readonly q3Arsenals = new Map<OwnedActor, Q3ArsenalRuntimeState>();
   private readonly q3Commands = new Map<OwnedActor, ActorCommand>();
   private readonly deathAnimations = new Q3DeathAnimationSequence();
@@ -1013,7 +1014,7 @@
       return { kind: "selected", offer: { kind: "ammo", offer: { item: weapon.ammo,
         amount: pickup.count !== 0 ? pickup.count : pickup.item.quantity } }, respawnSeconds: RESPAWN_AMMO };
     }
-    const profile = arsenal.family === "q1" ? Q3_Q1_SUPPLY_PROFILE : Q3_Q2_SUPPLY_PROFILE;
+    const profile = this.selectedSupply.profile;
     const destination = weapon.ammo === null ? null : profile.ammo.find(entry => entry.source === weapon.ammo)?.destinations[0];
     if (weapon.ammo !== null && destination == null) return { kind: "rejected" };
     const quantity = q3WeaponPickupQuantity({ count: pickup.count, quantity: pickup.item.quantity, dropped: pickup.dropped,
@@ -1025,16 +1026,46 @@
   private previewSelectedQ3Pickup(pickup: SourcePickupDescriptor): SourcePickupPreview {
     const supplied = this.selectedQ3Supply(pickup);
     if (supplied.kind !== "selected") return supplied;
-    const admission = this.selectedSupply;
-    if (admission === null) throw new Error("Selected source supply lost its admission");
+    const admission = this.selectedSupply?.admission;
+    if (admission === undefined) throw new Error("Selected source supply lost its admission");
     return { kind: "selected", offer: supplied.offer, preview: admission.preview(pickup.playerActor, supplied.offer) };
+  }
+
+  private regenerateSelectedAmmo(actor: ActorId, rules: readonly AmmoRegenerationRule[], msec: number, clock: AmmoRegenerationClock): boolean {
+    const selected = this.selectedArsenal, supply = this.selectedSupply;
+    if (selected === null || selected.family === "q3" || !selected.has(actor) || supply === null) return false;
+    const player = this.requirePlayer(actor), inventory = this.inventory.entries(actor), assigned = new Set<ItemId>();
+    for (const rule of rules) {
+      const ammo = q3WeaponItem(rule.weapon)?.ammo;
+      const mapping = supply.profile.ammo.find(row => row.source === ammo);
+      const destinations: InventoryEntry[] = [];
+      for (const item of mapping?.destinations ?? []) {
+        if (assigned.has(item)) continue;
+        const entry = inventory.find(entry => entry.item === item);
+        if (entry === undefined) continue;
+        assigned.add(item); destinations.push(entry);
+      }
+      if (destinations.length === 0) continue;
+      let elapsed = (clock.get(rule.weapon) + msec) | 0;
+      if (destinations.every(entry => entry.count >= Math.min(rule.max, entry.capacity))) elapsed = 0;
+      if (elapsed >= rule.time) {
+        while (elapsed >= rule.time) elapsed -= rule.time;
+        for (const entry of destinations) {
+          const cap = Math.min(rule.max, entry.capacity);
+          if (entry.count < cap) this.inventory.configure(player.actor, { ...entry, count: Math.min(cap, (entry.count + rule.increment) | 0) });
+        }
+      }
+      clock.set(rule.weapon, elapsed);
+    }
+    player.arsenal = selected.read(actor);
+    return true;
   }
 
   private admitSelectedQ3Pickup(pickup: SourcePickupDescriptor): SourcePickupAdmission {
     const supplied = this.selectedQ3Supply(pickup);
     if (supplied.kind !== "selected") return supplied;
-    const admission = this.selectedSupply, actor = this.actors.resolveOwned(pickup.playerActor);
-    if (admission === null || actor === null) return { kind: "rejected" };
+    const admission = this.selectedSupply?.admission, actor = this.actors.resolveOwned(pickup.playerActor);
+    if (admission === undefined || actor === null) return { kind: "rejected" };
     const offer = supplied.offer;
     const accepted = offer.kind === "ammo" ? admission.ammo(actor, offer.offer)
       : offer.kind === "weapon" && admission.weapon(actor, offer.offer, this.options.mode === "deathmatch" ? "better" : "always");
@@ -1054,7 +1085,6 @@
   private createSelectedQ2Arsenal(): Q2SelectedArsenal {
     const product = this.weaponProvider.content.split(":")[2];
     if (product !== "baseq2" && product !== "xatrix" && product !== "rogue" && product !== "mg2") throw new Error("Selected Q2 arsenal has an unsupported source program");
-    if (this.source.kind === "q3" && this.source.game.options.product === "missionpack") throw new Error("Selected Q2 supply on Team Arena is not implemented");
     const timing = providerTiming(this.recipe, this.weaponProvider.provider), profile = timing.clock;
     if (profile.kind !== "q2-classic" && profile.kind !== "q2-rerelease") throw new Error("Selected Q2 arsenal requires its source clock");
     const edition = profile.kind === "q2-classic" ? "classic" : "rerelease", intervalMilliseconds = profile.kind === "q2-classic" ? 100 : profile.frameMilliseconds;
@@ -1103,17 +1133,17 @@
     const selected = new Q2SelectedArsenal({ game, weapons, inventoryDefinitions: missionWeapons.inventoryDefinitions, pickupOrder: missionWeapons.pickupOrder, replacedItems: replacedSupplyItems(supply),
       ...(this.source.kind === "q2" ? {} : { loadout: this.source.kind === "q1" ? q1Q2SupplyLoadout() : q3Q2SupplyLoadout() }),
       observe: actor => { const player = this.requirePlayer(actor); return { owner: { actor: player.actor, viewHeight: player.viewHeight }, input: this.q2WeaponInput(player) }; } });
-    this.selectedSupply = new SharedPickupAdmission({ inventory: this.inventory, profile: supply,
+    const admission = new SharedPickupAdmission({ inventory: this.inventory, profile: supply,
       ammoGranted: (actor, grants, autoSwitch) => { selected.pickupAmmo(actor, grants, autoSwitch); this.requirePlayer(actor.id).arsenal = selected.read(actor.id); return undefined; },
       weaponGranted: (actor, granted, selection) => { selected.pickupWeapons(actor, granted, selection); this.requirePlayer(actor.id).arsenal = selected.read(actor.id); return undefined; } });
-    if (this.source.kind === "q1") this.source.game.pickupAdmission = this.selectedSupply;
-    else if (this.source.kind === "q2") this.source.items.setPickupAdmission(this.selectedSupply);
+    this.selectedSupply = { profile: supply, admission };
+    if (this.source.kind === "q1") this.source.game.pickupAdmission = admission;
+    else if (this.source.kind === "q2") this.source.items.setPickupAdmission(admission);
     return selected;
   }
 
   private createSelectedQ1Arsenal(): Q1SelectedArsenal {
     const product = this.weaponProvider.content.split(":")[2];
-    if (this.source.kind === "q3" && this.source.game.options.product === "missionpack") throw new Error("Selected Q1 supply on Team Arena is not implemented");
     if (product !== "id1" && product !== "hipnotic" && product !== "rogue" && product !== "dopa" && product !== "mg1" && product !== "mg3") throw new Error("Selected Q1 arsenal has an unsupported source program");
     const timing = providerTiming(this.recipe, this.weaponProvider.provider);
     const random = new SourceRandom(this.options.seed, "classic");
@@ -1175,11 +1205,12 @@
       },
       replacedItems: [...(this.source.kind === "q1" ? [] : replacedSupplyItems(profile)), ...(this.source.kind === "q2" ? ["q2:weapon_blaster"] satisfies readonly ItemId[] : [])],
       observe: actor => { const player = this.requirePlayer(actor); return { viewAngles: player.viewAngles, waterLevel: player.waterLevel }; } });
-    this.selectedSupply = new SharedPickupAdmission({ inventory: this.inventory, profile,
+    const admission = new SharedPickupAdmission({ inventory: this.inventory, profile,
       ammoGranted: (actor, grants, autoSwitch) => { selected.pickupAmmo(actor, grants, autoSwitch); this.requirePlayer(actor.id).arsenal = selected.read(actor.id); return undefined; },
       weaponGranted: (actor, weapons, selection) => { selected.pickupWeapons(actor, weapons, selection); this.requirePlayer(actor.id).arsenal = selected.read(actor.id); return undefined; } });
-    if (this.source.kind === "q2") this.source.items.setPickupAdmission(this.selectedSupply);
-    if (this.source.kind === "q1") this.source.game.pickupAdmission = this.selectedSupply;
+    this.selectedSupply = { profile, admission };
+    if (this.source.kind === "q2") this.source.items.setPickupAdmission(admission);
+    if (this.source.kind === "q1") this.source.game.pickupAdmission = admission;
     return selected;
   }
 
@@ -1697,6 +1728,7 @@
         primaryAttackAllowed: actor => this.selectedArsenal === null && (this.weaponSlots.get(actor)?.primarySelected() ?? true),
         admitPickup: item => this.admitSelectedQ3Pickup(item),
         previewPickup: item => this.previewSelectedQ3Pickup(item),
+        regenerateAmmo: (actor, rules, msec, clock) => this.regenerateSelectedAmmo(actor, rules, msec, clock),
         foreign: actor => { if (this.actors.isLive(actor)) throw new Error("Foreign Q3 actor projection is not attached"); return null; },
         isPlayer: actor => this.player(actor) !== null,
         moverActors: {
@@ -4052,9 +4084,9 @@
     const entry = entries.find(entry => entry.item === named?.id || normalize(entry.item) === normalize(requested)
       || normalize(entry.item.split("/").at(-1) ?? "") === normalize(requested));
     if (entry === undefined) {
-      const profile = this.source.kind === "q1" ? arsenal.family === "q3" ? Q1_Q3_SUPPLY_PROFILE : arsenal.family === "q2" ? Q1_Q2_SUPPLY_PROFILE : Q1_HIPNOTIC_SUPPLY_PROFILE
+      const profile = this.selectedSupply?.profile ?? (this.source.kind === "q1" ? arsenal.family === "q3" ? Q1_Q3_SUPPLY_PROFILE : arsenal.family === "q2" ? Q1_Q2_SUPPLY_PROFILE : Q1_HIPNOTIC_SUPPLY_PROFILE
         : this.source.kind === "q2" ? arsenal.family === "q3" ? Q2_Q3_SUPPLY_PROFILE : arsenal.family === "q1" && arsenal.game.registeredWeapons.has("hipnotic:laser") ? Q2_HIPNOTIC_SUPPLY_PROFILE : Q2_Q1_SUPPLY_PROFILE
-          : this.source.kind === "q3" ? arsenal.family === "q2" ? Q3_Q2_SUPPLY_PROFILE : arsenal.family === "q1" && arsenal.game.registeredWeapons.has("hipnotic:laser") ? Q3_HIPNOTIC_SUPPLY_PROFILE : Q3_Q1_SUPPLY_PROFILE : null;
+          : this.source.kind === "q3" ? arsenal.family === "q2" ? Q3_Q2_SUPPLY_PROFILE : arsenal.family === "q1" && arsenal.game.registeredWeapons.has("hipnotic:laser") ? Q3_HIPNOTIC_SUPPLY_PROFILE : Q3_Q1_SUPPLY_PROFILE : null);
       const weapon = profile?.weapons.find(item => item.source === requested), ammo = profile?.ammo.find(item => item.source === requested);
       const sourceItem = this.source.kind === "q2" ? this.source.items.lookup(requested) : null;
       const destinations = weapon?.destinations ?? ammo?.destinations;
--- a/src/content/composition/expansion-source-supply.ts
+++ b/src/content/composition/expansion-source-supply.ts
@@ -2,11 +2,15 @@
 import type { PickupSupplyProfile } from "../../contracts/pickups.ts";
 
 const ammoAliases: readonly (readonly [ItemId, ItemId])[] = [
+  ["q3:ammo/nailgun", "q3:ammo/machinegun"], ["q3:ammo/chaingun", "q3:ammo/machinegun"],
+  ["q3:ammo/proxlauncher", "q3:ammo/grenadelauncher"],
   ["rogue:ammo/lava-nails", "q1:ammo/nails"], ["rogue:ammo/multi-rockets", "q1:ammo/rockets"], ["rogue:ammo/plasma", "q1:ammo/cells"],
   ["q2:ammo_magslug", "q2:ammo_slugs"], ["q2:ammo_flechettes", "q2:ammo_bullets"], ["q2:ammo_disruptor", "q2:ammo_cells"],
   ["q2:ammo_trap", "q2:ammo_grenades"], ["q2:ammo_tesla", "q2:ammo_grenades"], ["q2:ammo_prox", "q2:ammo_grenades"],
 ];
 const weaponAliases: readonly (readonly [ItemId, ItemId])[] = [
+  ["q3:weapon/nailgun", "q3:weapon/machinegun"], ["q3:weapon/chaingun", "q3:weapon/machinegun"],
+  ["q3:weapon/proxlauncher", "q3:weapon/grenadelauncher"],
   ["q1:weapon/hipnotic:laser", "q1:weapon/lightning"], ["q1:weapon/hipnotic:mjolnir", "q1:weapon/axe"], ["q1:weapon/hipnotic:proximity", "q1:weapon/grenadelauncher"],
   ["q1:weapon/mg3:laser", "q1:weapon/lightning"], ["q1:weapon/mg3:mjolnir", "q1:weapon/axe"],
   ["q1:weapon/rogue:lava-nailgun", "q1:weapon/nailgun"], ["q1:weapon/rogue:lava-supernailgun", "q1:weapon/supernailgun"],
--- a/src/content/q3/team-arena/client-effects.ts
+++ b/src/content/q3/team-arena/client-effects.ts
@@ -12,8 +12,13 @@
 import type { CombatContext } from "../base/game/combat.ts";
 import { GameFlags } from "../base/game/state.ts";
 import type { GameClient, GameEntity } from "../base/game/state.ts";
+import type { ActorId } from "../../../contracts/identity.ts";
+
+export interface AmmoRegenerationRule { readonly weapon: Weapon; readonly max: number; readonly increment: number; readonly time: number; }
+export interface AmmoRegenerationClock { get(weapon: Weapon): number; set(weapon: Weapon, elapsed: number): void; }
 
 export interface ClientEffectsContext {
+  regenerateAmmo?(actor: ActorId, rules: readonly AmmoRegenerationRule[], msec: number, clock: AmmoRegenerationClock): boolean;
   readonly combat: CombatContext;
   readonly intermissionTime: number;
   readonly smoothClients: boolean;
@@ -24,7 +29,7 @@
   spectatorEndFrame(entity: GameEntity): void;
 }
 
-type EventContext = Pick<ClientEffectsContext, "combat">;
+type EventContext = Pick<ClientEffectsContext, "combat" | "regenerateAmmo">;
 const CONTENTS_LAVA = 8;
 const CONTENTS_SLIME = 16;
 const EF_TICKING = 2;
@@ -104,7 +109,7 @@
     : entity.waterlevel !== 0 && (entity.watertype & (CONTENTS_LAVA | CONTENTS_SLIME)) !== 0 ? context.frySound : 0;
 }
 
-const ammoRegeneration: readonly { readonly weapon: Weapon; readonly max: number; readonly increment: number; readonly time: number }[] = [
+const ammoRegeneration: readonly AmmoRegenerationRule[] = [
   { weapon: Weapon.WP_MACHINEGUN, max: 50, increment: 4, time: 1000 },
   { weapon: Weapon.WP_SHOTGUN, max: 10, increment: 1, time: 1500 },
   { weapon: Weapon.WP_GRENADE_LAUNCHER, max: 10, increment: 1, time: 2000 },
@@ -148,6 +153,7 @@
     if (ps.stats.get(schema.armor) > maximum) ps.stats.set(schema.armor, ps.stats.get(schema.armor) - 1);
   }
   if (ps.product === "missionpack" && persistentTag(client) === Powerup.PW_AMMOREGEN) {
+    if (context.regenerateAmmo?.(entity.actor.id, ammoRegeneration, msec, client.ammoTimes) === true) return;
     for (const rule of ammoRegeneration) {
       let elapsed = (client.ammoTimes.get(rule.weapon) + msec) | 0;
       if (ps.ammo.get(rule.weapon) >= rule.max) elapsed = 0;
--- a/src/content/q3/team-arena/client-think.ts
+++ b/src/content/q3/team-arena/client-think.ts
@@ -50,7 +50,7 @@
   readonly world: ServerWorld;
   readonly spatial: ActorSpatialQueries;
   readonly touches: ClientTouchAccess;
-  readonly effects: Pick<ClientEffectsContext, "combat">;
+  readonly effects: Pick<ClientEffectsContext, "combat" | "regenerateAmmo">;
   frame(): ClientThinkFrame;
   settings(): ClientThinkSettings;
   setPmoveMsec(milliseconds: number): void;
--- a/tests/composition/q1-supply.test.ts
+++ b/tests/composition/q1-supply.test.ts
@@ -200,3 +200,26 @@
   expect(previewPickupGrants(inventory.entries(actor.id), { kind: "ammo", acceptance: "nonzero", ammo: [{ item: "q2:ammo_grenades", amount: 5 }],
     weapons: { kind: "shared-ammo", items: ["q2:ammo_grenades"] } })).toEqual({ accepted: false, ammo: [{ item: "q2:ammo_grenades", before: 50, given: 0 }], weapons: [] });
 });
+
+test("Team Arena source aliases feed selected Quake and Rogue ownership groups with source quantities", async () => {
+  const { expansionSupply } = await import("../../src/content/composition/expansion-supply.ts");
+  const { expansionSourceSupply } = await import("../../src/content/composition/expansion-source-supply.ts");
+  const actors = new SessionActorRegistry(createIdentityOwner("ta-q1-rogue-supply")), actor = actors.allocate("q3:game", "q3:player"), inventory = new SharedInventoryTable(actors);
+  const profile = expansionSourceSupply(expansionSupply(Q3_Q1_SUPPLY_PROFILE, ["q1-rogue"]));
+  const weapons = new Set(profile.weapons.flatMap(row => row.destinations)), ammo = new Set(profile.ammo.flatMap(row => row.destinations));
+  inventory.create(actor, [...weapons].map(item => ({ item, count: 0, capacity: 1 })).concat([...ammo].map(item => ({ item, count: 0, capacity: 100 }))));
+  const pickup = new SharedPickupAdmission({ inventory, profile, ammoGranted: () => undefined, weaponGranted: () => undefined });
+  try {
+    expect(pickup.weapon(actor, { item: "q3:weapon/chaingun", ammo: [{ item: "q3:ammo/chaingun", amount: 80 }] }, "always")).toBe(true);
+    expect(inventory.count(actor.id, "q1:weapon/nailgun")).toBe(1);
+    expect(inventory.count(actor.id, "q1:weapon/rogue:lava-nailgun")).toBe(1);
+    expect(inventory.count(actor.id, "q1:ammo/nails")).toBe(80);
+    expect(inventory.count(actor.id, "rogue:ammo/lava-nails")).toBe(80);
+    expect(pickup.ammo(actor, { item: "q3:ammo/nailgun", amount: 30 })).toBe(true);
+    expect(inventory.count(actor.id, "q1:ammo/nails")).toBe(100);
+    expect(pickup.ammo(actor, { item: "q3:ammo/chaingun", amount: 1 })).toBe(false);
+    expect(pickup.weapon(actor, { item: "q3:weapon/proxlauncher", ammo: [{ item: "q3:ammo/proxlauncher", amount: 5 }] }, "always")).toBe(true);
+    expect(inventory.count(actor.id, "q1:weapon/rogue:multi-grenade")).toBe(1);
+    expect(inventory.count(actor.id, "rogue:ammo/multi-rockets")).toBe(5);
+  } finally { actors.close(); }
+});
--- a/tests/composition/q3-q2-supply.test.ts
+++ b/tests/composition/q3-q2-supply.test.ts
@@ -34,3 +34,106 @@
   expect(inventory.count(actor.id, "q3:holdable/teleporter")).toBe(1);
   actors.close();
 });
+
+test("Team Arena source aliases grant selected Rogue weapons and shared ammo without replacing holdables", async () => {
+  const { expansionSupply } = await import("../../src/content/composition/expansion-supply.ts");
+  const { expansionSourceSupply } = await import("../../src/content/composition/expansion-source-supply.ts");
+  const actors = new SessionActorRegistry(createIdentityOwner("ta-rogue-supply")), actor = actors.allocate("q3:game", "q3:player"), inventory = new SharedInventoryTable(actors);
+  inventory.create(actor, [
+    { item: "q2:weapon_machinegun", count: 0, capacity: 1 }, { item: "q2:weapon_chaingun", count: 0, capacity: 1 }, { item: "q2:weapon_etf_rifle", count: 0, capacity: 1 },
+    { item: "q2:weapon_grenadelauncher", count: 0, capacity: 1 }, { item: "q2:weapon_proxlauncher", count: 0, capacity: 1 },
+    { item: "q2:ammo_bullets", count: 0, capacity: 200 }, { item: "q2:ammo_flechettes", count: 0, capacity: 200 },
+    { item: "q2:ammo_grenades", count: 0, capacity: 50 }, { item: "q2:ammo_prox", count: 0, capacity: 50 }, { item: "q2:ammo_tesla", count: 0, capacity: 50 },
+    { item: "q3:holdable/teleporter", count: 1, capacity: 1 },
+  ]);
+  const profile = expansionSourceSupply(expansionSupply(Q3_Q2_SUPPLY_PROFILE, ["q2-rogue"]));
+  const pickup = new SharedPickupAdmission({ inventory, profile, ammoGranted: () => undefined, weaponGranted: () => undefined });
+  try {
+    expect(pickup.weapon(actor, { item: "q3:weapon/nailgun", ammo: [{ item: "q3:ammo/nailgun", amount: 20 }] }, "always")).toBe(true);
+    expect(inventory.count(actor.id, "q2:weapon_etf_rifle")).toBe(1);
+    expect(inventory.count(actor.id, "q2:ammo_flechettes")).toBe(20);
+    expect(pickup.ammo(actor, { item: "q3:ammo/chaingun", amount: 40 })).toBe(true);
+    expect(inventory.count(actor.id, "q2:ammo_bullets")).toBe(60);
+    expect(inventory.count(actor.id, "q2:ammo_flechettes")).toBe(60);
+    expect(pickup.weapon(actor, { item: "q3:weapon/proxlauncher", ammo: [{ item: "q3:ammo/proxlauncher", amount: 5 }] }, "always")).toBe(true);
+    expect(inventory.count(actor.id, "q2:weapon_proxlauncher")).toBe(1);
+    expect(inventory.count(actor.id, "q2:ammo_prox")).toBe(5);
+    expect(inventory.count(actor.id, "q2:ammo_tesla")).toBe(5);
+    expect(inventory.count(actor.id, "q3:holdable/teleporter")).toBe(1);
+  } finally { actors.close(); }
+});
+
+for (const arsenal of ["q1-classic-hipnotic", "q2-classic-rogue", "native"]) test(`Team Arena runtime supply and regeneration retain source ownership with ${arsenal}`, async () => {
+  const { applicationPreset, loadApplicationContent } = await import("../../src/app/bootstrap/content.ts");
+  const { parseApplicationCommand } = await import("../../src/app/bootstrap/options.ts");
+  const { createSimulation } = await import("../../src/app/bootstrap/simulation/index.ts");
+  const { discoverInstalledContent, presetChoice, resolveLaunch } = await import("../../src/content/catalog/index.ts");
+  const { spawnItem, finishSpawningItem } = await import("../../src/content/q3/base/game/item-lifecycle.ts");
+  const { SpawnVariables } = await import("../../src/content/q3/base/game/spawn.ts");
+  const { itemList } = await import("../../src/content/q3/base/shared/items.ts");
+  const { Powerup, Weapon, statSchema } = await import("../../src/content/q3/base/shared/definitions.ts");
+  const { clientTimerActions } = await import("../../src/content/q3/team-arena/client-effects.ts");
+  const launch = parseApplicationCommand(["--content-root", "/home/buzzkill/Projects/qfiles", "--game", "q3-missionpack", "--map", "mpteam1", "--movement", "q3", "--character", "q3", "--mode", "deathmatch", "--dedicated"]);
+  if (launch.kind !== "run") throw new Error("Missing Team Arena launch");
+  const catalog = await discoverInstalledContent({ corpusRoot: launch.options.corpusRoot, discoverMods: false }), preset = applicationPreset(catalog, launch.options);
+  const recipe = await resolveLaunch({ catalog, preset, choice: { ...presetChoice(preset.id), ...(arsenal === "native" ? {} : {
+    weapons: { kind: "selected", value: [{ provider: arsenal.startsWith("q1") ? "q1:official" : "q2:official", content: catalog.require(arsenal).id }] },
+  }) } });
+  const content = await loadApplicationContent(launch.options, recipe), identity = createIdentityOwner(`ta-supply-${arsenal}`), client = identity.client(0, 0);
+  const options = { identity, recipe: content.recipe, world: content.world, mounts: content.mounts, skill: 0, mode: "deathmatch", seed: 1, maxClients: 1 } satisfies Parameters<typeof createSimulation>[0];
+  const simulation = createSimulation(options);
+  try {
+    const actor = simulation.admitPlayer(client).actor, player = simulation.movementPlayer(actor), source = simulation.q3Source();
+    if (source === null || player === null) throw new Error("Missing Team Arena source/player");
+    const receiver = source.records.nativeByActor(actor), schema = statSchema("missionpack");
+    if (receiver?.client == null) throw new Error("Missing source receiver");
+    const native = receiver.client, items = itemList("missionpack");
+    const regeneration = items.findIndex(item => item.tag === Powerup.PW_AMMOREGEN && item.className === "item_ammoregen");
+    if (regeneration < 0) throw new Error("Missing source ammo regeneration item");
+    native.ps.stats.set(schema.persistentPowerup, regeneration);
+    for (let weapon = 0; weapon < 14; weapon++) { native.ammoTimes.set(weapon, 0); native.ps.ammo.set(weapon, 0); }
+    const timer = (msec: number) => clientTimerActions({ combat: source.combat,
+      regenerateAmmo: (id, rules, elapsed, clock) => source.host.regenerateAmmo?.(id, rules, elapsed, clock) ?? false }, receiver, msec);
+    if (arsenal === "native") {
+      timer(1000);
+      expect(native.ps.ammo.get(Weapon.WP_MACHINEGUN)).toBe(4);
+      expect(native.ps.ammo.get(Weapon.WP_CHAINGUN)).toBe(5);
+      return;
+    }
+    const q1 = arsenal.startsWith("q1"), cells = q1 ? "q1:ammo/cells" : "q2:ammo_cells", bullets = q1 ? "q1:ammo/nails" : "q2:ammo_bullets";
+    for (const entry of simulation.inventory.entries(actor)) if (entry.item.startsWith("q1:ammo/") || entry.item.startsWith("q2:ammo_")) simulation.inventory.configure(player.actor, { ...entry, count: 0 });
+    for (const classname of ["weapon_nailgun", "weapon_chaingun", "weapon_prox_launcher"]) {
+      const item = items.find(item => item.className === classname); if (item === undefined) throw new Error(`Missing ${classname}`);
+      const pickup = source.pool.spawn(); pickup.classname = classname; pickup.spawnflags = 1;
+      spawnItem(pickup, item, new SpawnVariables([]), () => false, source.itemLifecycle); finishSpawningItem(pickup, source.itemLifecycle);
+      pickup.touch?.(pickup, receiver, { self: pickup.actor, other: actor, plane: null, surface: null });
+      expect(pickup.r.contents).toBe(0);
+    }
+    expect(simulation.inventory.count(actor, q1 ? "q1:weapon/nailgun" : "q2:weapon_etf_rifle")).toBe(1);
+    expect(simulation.inventory.count(actor, q1 ? "q1:weapon/hipnotic:proximity" : "q2:weapon_proxlauncher")).toBe(1);
+    expect(source.host.giveSelectedItem?.(actor, ["q3:ammo/nailgun", "17"])).toBe(true);
+    expect(simulation.inventory.count(actor, bullets)).toBe(17);
+    for (const entry of simulation.inventory.entries(actor)) if (entry.item.startsWith("q1:ammo/") || entry.item.startsWith("q2:ammo_")) simulation.inventory.configure(player.actor, { ...entry, count: 0 });
+    timer(1500);
+    expect(simulation.inventory.count(actor, cells)).toBe(5);
+    expect(simulation.inventory.count(actor, bullets)).toBe(4);
+    expect(native.ps.ammo.get(Weapon.WP_MACHINEGUN)).toBe(0);
+    expect(native.ammoTimes.get(Weapon.WP_MACHINEGUN)).toBe(500);
+    if (!q1) { expect(simulation.inventory.count(actor, "q2:ammo_flechettes")).toBe(4); expect(simulation.inventory.count(actor, "q2:ammo_disruptor")).toBe(5); }
+    const holdable = native.ps.stats.get(schema.holdableItem), active = player.arsenal.activeWeapon;
+    simulation.drainPresentationEvents();
+    const restored = createSimulation({ ...options, restore: simulation.checkpoint(), restoredClients: [client] });
+    try {
+      const restoredActor = restored.players()[0];
+      if (restoredActor === undefined) throw new Error("Missing restored actor");
+      const restoredSource = restored.q3Source(), restoredPlayer = restoredSource?.records.nativeByActor(restoredActor);
+      if (restoredSource === null || restoredPlayer?.client == null) throw new Error("Missing restored source player");
+      expect(restoredPlayer.client.ammoTimes.get(Weapon.WP_MACHINEGUN)).toBe(500);
+      clientTimerActions({ combat: restoredSource.combat, regenerateAmmo: (id, rules, elapsed, clock) => restoredSource.host.regenerateAmmo?.(id, rules, elapsed, clock) ?? false }, restoredPlayer, 500);
+      expect(restored.inventory.count(restoredActor, bullets)).toBe(8);
+      expect(restored.inventory.count(restoredActor, cells)).toBe(5);
+      expect(restoredPlayer.client.ps.stats.get(schema.holdableItem)).toBe(holdable);
+      expect(restored.movementPlayer(restoredActor)?.arsenal.activeWeapon).toBe(active);
+    } finally { restored.close(); }
+  } finally { simulation.close(); await content.close(); }
+});
