# Execution status

## Accepted world-text controls, source movement and guest preparation

Source cutoff: `e5e51456d04a911528da8b52af9a50b794738318`. Accepted `200493b6` joins live `gl_debug_distfrac` to common world-text culling; root passed CPU/GL two-seat checks, 6 tests/82 assertions. Accepted `ef4d34d1` joins the actual native DLL's two world-text imports to the common store, root 1/16. The other eight debug-shape imports remain open.

Accepted `e0409dc0` routes the exercised native QW commands through the shared ordered command phase, root 5/206. Spectators, pause, complete administration and arbitrary mods remain open.

Accepted `98074c6` corrects native authored Q1 soldier/dog gib and head construction using existing helpers in source order. Root passed 14/301 and the actual dedicated rerelease e1m1 witness: heads clear ground, retain source bounds and signed spin, fly and remain alive at 23.5 seconds; gib pieces accelerate, bounce, settle and expire. Rerelease corpse-extension collision policy remains open.

Accepted `3176aee` joins selected Q2 source movement profiles and native server configuration publication. Root actual authored bio/base travel checks passed 1/68. Rerelease remote-client admission and matching native prediction remain missing; local source/profile and decoded configuration checks do not qualify that remote path.

Accepted `e5e51456` prepares the native Win64 Q2 guest source lifecycle, root 7/109. Complete bindings and normal application guest admission remain open. This does not turn the accepted world-text imports into full native-mod compatibility.

The finite ledger review changes only the reasons for `q1.physics.rerelease-gibs-corpses` and `q2.rerelease.q64-movement`; both remain open. All 477 verdicts/methods and the other 475 records carry unchanged from `7964fc31bfa186e538994eea3ed71cb6b8d035f6`, whose verdicts carried from `39ea4c0cab9910c6b78d5f8d18d3bc36494dd519`. Counts remain 288 Done/189 Not done, with no new full audit. The installed binary remains `3a073e87ec7a4126f396e15833b8afb9df569403`; no rebuild is claimed. The delivered [project-status snapshot](project-status-current.md) retains its earlier `98074c6e` cutoff.

## Accepted native QW hosting and shared SOCKS clients

Source cutoff: `7964fc31bfa186e538994eea3ed71cb6b8d035f6`. Transport `7b2d38b` and QC messages `d4b9f8c` precede shared native QC `60bbc638` (root 9 tests/718 assertions, including actual source execution and NQ retention) and dedicated QW host `88f84c10` (root 3/132 across source/host/transport). An independent donor protocol-28 client exercised firing (shells 25 to 23), authoritative movement (x 528 to 434) and travel from `e1m1` to `dm2` with servercount 2; evidence is in `/tmp/qw-host-donor-peer.log`. The accepted host uses base protocol 28 and the pinned CRC-54730 QW program, without spectators. Local graphical QW hosting, arbitrary mods, other commands/admin, master/rcon and full parity remain open. Later native-QW command work is outside this cutoff.

Accepted Q3 print `3d6589c5` restores native message-body reading (root 1/9). Shared SOCKS `7964fc31` connects the existing transport before native RemoteApplication sends, using archived/latched settings from the existing client configuration owner. Root passed the exact five cases/55 assertions: authenticated Q2 CPU signon, travel on the same association, reopen/settings, plus NQ/QW/Q3 native handshakes and decoded replies. Cancellation and proxy-loss checks use controlled loopback endpoints; configured proxy loss cannot silently send directly. Evidence is in `/tmp/root-socks4-final.log`. These tests do not establish GL, physical-network or all-family full-play qualification. Browser/listen-server proxy configuration remains separate.

Four existing reasons are narrowed; no verdict changes. Counts remain 288 Done/189 Not done across 477 unique IDs, with the other 473 records carried forward. The installed binary remains `3a073e87ec7a4126f396e15833b8afb9df569403`; no executable rebuild is claimed.

## Accepted audio devices and world-text culling

At source cutoff `39ea4c0cab9910c6b78d5f8d18d3bc36494dd519`, accepted audio `0065d73b` passed root 31 tests/263 assertions: actual dummy SDL queued PCM/clock/music retention, CPU/GL menu/restart, and native NQ remote named-device travel from `e1m1` to `e1m2` in the same window. Device names/default selection and device/effects/music settings persist per product through ConfigStore. Output remains 44100 Hz, stereo, 16-bit. Physical audio quality/performance and injected remote failure races are not qualified.

Accepted world text `39ea4c0c` passed root 6 tests/60 assertions through CPU/GL units and actual two-seat Q2 rerelease `base1`; root inspected images from both changes. Shared culling uses the native strict condition `cellSize < dot(origin-camera.origin, camera.axis[0]) * distanceCullFactor`, with producer default `0.004`, signed forward distance and rejection before glyph lookup. Native `size * 8` remains. Live `gl_debug_distfrac` control and guest debug imports were unjoined at this historical cutoff; the later control and two world-text import receipts appear above. Both frozen source units passed scoped strict/policy checks.

The two reviewed ledger requirements remain open: configurable output formats/complete source audio workflow and the full display/video restart workflow are not established by these receipts. Counts remain 288 Done/189 Not done across 477 IDs; the other 475 records were not re-audited. The installed binary remains `3a073e87ec7a4126f396e15833b8afb9df569403`, without a rebuild.

## Accepted NQ profiles and device persistence

Accepted `5a0e1848` supplies classic-id1 host protocol selection and shared-client negotiation of NQ 15/666/999, limits, alpha/scale, movement/travel and version flags. Root passed 9 tests/209 assertions and strict/policy checks; independent donor servers exercised signon and authoritative movement over all three protocols through the hidden shared client with no local world. Root inspected three images and donor-codec entity updates of 9/15/21 bytes matched inside actual UDP payloads. Rerelease, expansion/mod admission, private extensions, full bidirectional qualification, static visual-field production, nondefault lerpfinish and a menu protocol selector remain open.

Accepted `36f395ee` (tree `2358be0ff3ec42658264ccc02017c372f4657371`) supplies shared device/tuning persistence: root 8 tests/99 assertions and strict/policy checks covered two-seat restart, serial/explicit-none choices, keyboard fallback, custom bind remaps 42→77→88, gyro profile fallback, mouse/history and haptic enable/strength. That 8/99 total includes remote CPU/GL checks (2/60). Physical hotplug remains unqualified; identical GUID/ordinal devices without serials cannot be disambiguated after reordering. Only device persistence becomes Done/shared-replacement; counts are 288 Done/189 Not done across 477 IDs. No audio acceptance or installed-binary rebuild is claimed.

## Accepted native QW remote admission

Accepted `66e6136` joins native protocol 28 `--connect-qw` with default qw/id1 mounts, challenge/connect lists, sound/model download admission, checksum2 spawn/begin, shared Q1 presentation and prediction history. Root final checks passed 4 tests/46 assertions; earlier receiver/prediction/application checks passed 8/123. An independent TypeScript donor loopback server exercised retail start signon, CPU rendering and authoritative movement; root inspected its screenshot. Strict/scoped policy passed on snapshot `qw16-WMnsLt` (source `8b4ee3e6c1f4dc3f251a5e0f7daa9125bf606ae8419d92cb5e9f721e6e31684d`, accepted tree `c8c1a6ce7e93089bca0ebcf38ecb26d2534a9534`).

Scope is base Q1 aliases and one remote seat. Spectator/extension profiles are rejected; other-player two-pass/fractional prediction, full effects, demos, hosting and split-screen remain open. The subsequent accepted skin work and its remaining policy limits are recorded below. At that earlier cutoff counts were 287 Done/190 Not done; current accepted counts are given above. The installed binary remains source `3a073e87` with no rebuild.

### Accepted skin and identity follow-up

Accepted `0c0499c3` joins shared indexed MDL skins/colors and proper PCX source cropping while retaining native geometry and MD5 fallback (root actual CPU/GL Q1 rerelease and Q2-map checks: 6 tests/68 assertions). Accepted `066ab042` joins QW application skins and saved pre-connect name/skin/colors/rate (root 6/106). An independent protocol-28 donor server verified an exact 913-byte skin, live colors 2/9 without a kick and authoritative movement; root inspected screenshots. Accepted `a8f9e2f` forwards local Q1 source colors through two-seat update/save/load (root 1/5); this is source-setter evidence, not an editable local color-console route or visible local-color proof.

At combined cutoff `a8f9e2f` (tree `644449d48b72b9be7592f1047cf92b5c4faa6342`), editable local setup/color console and cross-source appearance ownership remain open. QW downloads remain limited to base qw/id1; arbitrary mod gamedirs and application server-host download denial policy remain unjoined. Prediction, hosting, spectator, split-screen and demo limits above remain unchanged. The subsequently accepted NetQuake update is recorded above. At that earlier cutoff counts were 287 Done/190 Not done; current accepted counts are given above. The installed binary remains `3a073e87`.

For the item-by-item implementation Done/Not done list, see [completion status](completion-status.md). It uses the fixed accepted source cutoff recorded there, separately from formal verification acceptance and installed-build status.

Finite capture review at source cutoff `a5c4da3` moves `q2.capture.screenshots` to Done/shared-replacement and narrows `q3.recording.screenshots-levelshots` to remaining recording-clock/FPS-timescale/restart work. The ledger now records 281 Done and 196 Not done (Q2: 109/193); all other verdicts carry from `27c17a9`, without a new inventory-wide audit. The priority appendix removes the completed Q2 row. Multi-seat and resized-window screenshots remain an explicit unexercised verification case, not an identified capture implementation gap.

Accepted `fc0c15e` joins shared screenshot/levelshot commands to active application capture; `3f6be42` adds shared world-text storage and CPU/GL scene submission. Final frozen source `9c9bea07c94ee1dfae8ad1c591e31f87107e397deb01e28984a18ef7cc43c719` passed strict TypeScript and policy checks. Root independently executed 17 world-text tests/144 assertions, including the separately enabled GL unit, and retained unchanged capture checks (5/77) and core/config checks (22/266): 44 distinct tests/487 assertions. The final three-file correction keys zero-lifetime text to the authoritative source frame. Root inspected actual two-seat CPU/GL images with legible text at the intended world locations. This used controlled source-entity spawning on retail `base1`, not proof of naturally authored campaign text; background lighting differs between renderers, so full-frame parity is not claimed. Guest debug imports and donor distance-culling remain unfinished; depth behavior was tested. Local prompt checkpoint `d07700b` passed an actual application proof with 25 assertions and CPU image review; private native remote prompt transport remains unsupported. These receipts do not change the 477 requirement verdicts. The installed executable remains `3a073e8`.

Accepted `7b7110e` extends shared cvar commands while preserving Q1 custom-variable promotion/defaults; `8637652` adds verified parallel HTTP ranges. Root passed the combined cvar/input/range/HTTP suite (60 tests/557 assertions), existing common network checks (8/49), and actual ordinary/range-enabled remote-shell probes. The range probe downloaded a 1,991,612-byte PAK using one HEAD and four GETs at peak concurrency four, with no whole GET, matching installed/mounted SHA and native WAL fallback; the session retained no local authoritative world. GIF checkpoint `e05343b` passed root installed-image checks (17/139, no skips) and existing cinematic/world checks (25/417), including CPU/GL world movies. Original/captured frames and a matching CPU/GL pair were inspected; blanket renderer parity is not claimed. Exact frozen strict/policy checks passed for both source batches. The extraction baseline/manifests and 477 requirement verdicts are unchanged; local prompt work was still in progress at that checkpoint; its later acceptance is recorded above. The installed executable remains `3a073e8`.

Accepted source `936d475`/`ab0f90a`/`1fa5d62`/`9356b07` joins user-content overlays, contained QVM writes, shared audio commands and download ownership checks. Root passed storage 19/134, audio 2/78, actual VM travel 1/23 and shared sink 1/5; isolated strict/policy passed. Q3 application download/settings checkpoint `2b025db` passed the full source application file (4 tests/76 assertions), retaining the existing rate default of 25,000. HTTP checkpoint `30f824f` adds Q2 package discovery/reload, PKZ mounts and HTTP transfer with native fallback. Root passed 17 tests/168 assertions, reran the typed HTTP service (5/25, overlapping that suite), and ran the actual remote shell through a downloaded PAK map, HTTP WAL 404/native UDP fallback and rendered output with no local authoritative world. Final strict/policy passed on source `c5b8d2271999ed6bab1f1720c5f2bca51313aaeafb655d9a81d449cc8d04596e`. These bounded checks do not qualify every server, transfer failure or mod. The installed executable remains `3a073e8`; completion verdicts are unchanged.

Checkpoint `f7e331b`, with picture registration `93aed0e` and native ammo sentinels `9c89418`, joins stock Q3 client VMs to the shared application services and input path, including awaited guest shutdown before orderly travel/close invalidates the old generation. Root independently passed the exact CPU application files (5 tests/105 assertions) and the actual GL remote-client fixture (1 test/23 assertions). Strict/policy passed on the tested source; the final snapshot `e58928ef3c1fe10935c33d24a7334268bc47eaff38633edae8088a60ad50fb20` differs only in two reviewed software-renderer branding strings. This is bounded stock pure-client execution evidence, not arbitrary-mod, retail-peer or complete client-trap compatibility. Application download followthrough remains separate work. The installed executable remains `3a073e8`. Accepted gyro core/UI `221547a`/`27c17a9` passed root 22 tests/237 assertions plus the separately enabled SDL menu check (1 test/5 assertions); physical-controller qualification remains unclaimed. The finite gyro review changes one requirement verdict to Done: the completion inventory now records 280 done and 197 not done, with other verdicts carried from its stated prior cutoff.

Checkpoints `e46e924`, `ad0375d`, `1af767c` and `080c829` add native Q2 discovery, exactly-once remote Q1 sound delivery, native Q3 remote clients/content checks, and startup server browsing. Root retained the unaffected passing cases from the combined browser/client suite and independently reran the corrected Q1 file (4 tests/124 assertions). Final strict and policy checks passed; native retail-peer compatibility remains unqualified.

Checkpoints `1b50a46` and `11e6098` add asynchronous QVM client calls and common engine trap services. Root independently passed 36 tests/333 assertions; corrected strict/policy checks passed. Checkpoints `7c44237`, `47b358d` and `89d5c20` add shared script preprocessing, cinematic traps and staged Q3 downloads. Root passed 30 tests/436 assertions across the extension suite, including retail cinematic, CPU/GL movie, bot/parser and download checks, then rechecked the corrected cinematic API (4 tests/20 assertions); these overlapping counts are not added together. Final strict/policy checks passed on `c45f35493cfdccf9270b0ba490cae60dda3523ec5a573bafca5136a688498b91`. At that checkpoint, stock pure-server VM application/input integration remained uncommitted; the primitive-only evidence did not establish complete VM initialization or pure-server support. See the later accepted application checkpoint above. The installed executable remains `3a073e8`, and requirement completion counts are unchanged.

Preceding checkpoint `508b2be` adds a native NetQuake protocol-15 remote client, shared full-character opacity and host settings; retail-peer compatibility remains unqualified. Accepted source checkpoints `df44a12` and `af49a69` add authored material lighting across CPU/GL rendering and native protocol-34 Q2 client asset downloads with cancellation-safe loading. Root independently passed material checks (22 tests/278 assertions) and download, storage-integrity and existing remote-frontend checks (12 tests/123 assertions). Final isolated strict and policy checks passed on source snapshot `347dce8ec02c43d25655e4ca8f3611a39bde7dfbd5eafc95937c9ba48f3e98c2`; final type-only corrections retain that runtime evidence. This does not establish rerelease-wire or retail-peer compatibility. The installed executable remains `3a073e8`; these source checkpoints are not installed. Requirement completion counts are unchanged.

Implementation is authorized through the full engine until the user explicitly stops it. This ledger records the lead's dispatches and handoffs. Historical planning-only source assessments remain historical evidence. The current canonical graph contains 74 packages and 303 acceptance dependency edges after the user-directed code-first sequencing change.

The implementation phase started from `6439acd6c5f0d38ca93867ae0f6100ecf4cdbee4`. Checkpoint `1fbc44b` adds shared attached-body transport and save format 2. Checkpoint `7521ab4` makes shared body ground authoritative and shares Q3 gauntlet/lightning execution. Root checks cover foreign support, cleared support, a retail moving platform carrying a player/item/foreign grenade, suspended-item preservation, native/selected attacks, reflection and saves. Checkpoint `83b4dfe` shares Q3 shotgun execution and source impact effects; actual native/selected fire, Team Arena reflection, binary saves and shooter-release cleanup pass. Checkpoint `db11bbd` shares native and selected Q3 machinegun/chaingun execution and preserves actor identity through Q3 trigger/impact dispatch. Checkpoints `b078c5c`, `11eab2e` and `2a434c4` add shared Q3 mover, damage/use, radius/spawn and target-laser observations; `368e179` fixes Q1 CTF program admission. Root checks cover real foreign barrels, native Team Arena reflection, LMCTF rules, selected bullet saves and contact ordering. Checkpoint `3d59ed6` adds shared grapple weapon slots; `a036455` fixes semantic anchor lifetime and `39ba6f8` fixes Q3 brush collision roles. Earlier `edb5b35` adds selected offhand grapple execution. Earlier checkpoints `e3437e2`, `f8999c4`, `c6c25b6` and `0ac29b3` supply shared offhand grenades, native primary handoffs and explicit map-content identity. The executable installed in `~/Projects/qfiles` now contains verified source through `3a073e8`; see the [installed build record](#installed-build). W03 remains accepted only for its recorded early tooling scope. Other package states record work underway, not full acceptance. The diagram records acceptance dependencies and cannot supply a completion percentage.

Checkpoint `69f33f9` shares Q3 rail execution; `0cbad5d` shares radius damage and preserves projectile provenance. Native/selected attacks, reflection, temporary rail unlinking, binary saves and source effects have bounded checks. Checkpoints `c74d205` and `cb89c28` attach selected Q3 world weapons and preserve Q2 SKY surfaces carrying NODRAW. Root inspected CPU captures from the production scene composition. Checkpoints `b640bb4` and `5a1d7a6` publish the initial Q1 view weapon and apply native MDL skin-background preprocessing; same-camera captures verify the missing gun and blue edge corrections. Checkpoint `e8cc40c` shares native and selected Q3 projectile continuation, including bounded damage, bounce/fuse, owner-release saves and reflection checks. Checkpoints `4ee713a` and `674a0b0` preserve special missile contacts and lifetimes across actual actor release. Complete visual fidelity remains open. Checkpoint `0652053` admits selected classic Q2 infantry on Q1 maps and classic Q1 army on Q2 maps through the same actor, body and source behavior services. Root checks cover natural attacks in both directions, binary saves with explicit lifetime remapping, and base1's actual pickup-triggered activation, combat-point route and death target. Source and authored item drops remain distinct. This does not establish the full bestiary, rerelease interchangeability, Q1 path-corner traversal or all mission scripts; finer unsupported source cadences reject explicitly. Checkpoint `bc17271` registers classic dog and berserk through existing source services. Root checks cover untouched authored berserk placement/attack/save on Q1, selected dog bite/leap/save on a fitting Q1 recipe, and native startup drop/flags/callback ordering. Actual all-dog replacement on Q2 base1 now refuses obstructed source bounds explicitly; no creature is moved or shrunk. This is not universal dog placement or rerelease berserk acceptance.

Checkpoint `ed91729` runs selected Q1 weapons and native supply pickups on Q2/Q3 maps. Independent checks cover first shots, pickups, quad damage, native Q3 movement, Q2 live-grenade save/travel and grapple-slot resume. Checkpoint `550c7b0` shares map-owned silencer charges, shot/impact noise and selected-character attack presentation; six actual-map probes cover 31-shot exhaustion, saves, coop projectile impacts and Sarge animation. Checkpoints `b70af97` and `bf94ded` preserve map lighting colors, Q1 model surfaces, view-weapon depth and local lightning placement; the lead inspected frozen CPU captures on both maps. Checkpoint `d6c6131` adds display gamma after CPU/GL scene rendering. Frozen Q2 shotgun and Q3 lightning captures verify the exact shared lookup table against raw framebuffer pixels, unchanged alpha and a byte-identical neutral redraw; the actual GL driver was NVIDIA, not Mesa. Default-framebuffer versus FBO rasterization differences remain a fidelity limit. Native audio and complete visual fidelity remain unproved.

Checkpoint `8bf2485` separates Q2 weapon owners from native player entities and preserves actual-actor impact noise and lifetime checks. Checkpoint `f17a688` runs the reusable Q1 creature controller on shared map actors, with source startup, mission routes, contact goals, projectiles and backpack supplies. Independent frozen checks cover classic enforcer attacks on Q2, flying-projectile and death/drop saves, ammo-only backpack behavior, existing army/dog regressions and explicit obstructed-placement refusal. The combined source candidate passed 73 tests with 1,558 assertions and ten actual gameplay probes. Root inspected the enforcer CPU captures and native attack-frame geometry. This is bounded classic enforcer evidence, not full-roster, rerelease or campaign acceptance; native army/dog controller ownership is unchanged.

Checkpoint `064d1bf` admits selected base-Q2 weapons on Q1/Q3 maps through shared actors and actual classic/rerelease source cadence. Independent checks passed 13 actual gameplay probes, 55 source tests with 1,718 assertions, two adapter tests with 339 assertions and the corrected BFG visibility test. Evidence covers firing, pickups, saves/travel, source timing, quad changes during projectile flight and offhand quad behavior. Root inspected the production CPU Q1/Sarge/Q2-shotgun capture at gamma 1 and 1.3: native view-model frame 9, kick and flags 21 were present, with no unhandled effects. Checkpoint `3ccaeb0` preserves proximity-mine attacker credit for live native and foreign owners, with released-owner fallback; its source callback check passed 36 assertions. Expansion weapon admission, complete foreign target semantics and native visual/audio fidelity remain open.

Checkpoint `af35a0f` restores native Q1 barrel box collision, stationary movement and source drop behavior. Checkpoint `7cbab8d` reads actual target-owned solid and Q2 flags for rail/BFG, and dispatches dodge to the target's actual Q2 monster context. Independent actual-source checks cover the untouched e1m1 barrel at slot 322 taking scheduled BFG laser damage from 20 to 15 health, and admitted infantry switching from stand to its native duck move. The existing Q2 weapon suite passed 23 tests with 328 assertions. These checks do not establish complete foreign-target semantics; the exact `misc_explobox` BFG policy remains, with `misc_explobox2` adaptation deferred.

Checkpoints `8f91da4` and `6d7c253` add shared pickup-grant previews and remove unsupported Q1 crouching from bot navigation. Independent checks passed seven supply tests with 147 assertions and three posture tests with 94 assertions. Checkpoint `eb0d55f` runs the same Q3-derived bot core on Q1 standard deathmatch with selected base-Q2 weapon knowledge, preserving existing Q2/Q3 behavior. Frozen checks passed the Q1/Q2 application tests (2 tests/42 assertions), native Q3 application tests (2/15) and catalog tests (3/22). Actual probes cover respawn resetting rail decision 10 to starter shotgun 2, teleport retaining weapon preference, actor lifetime reuse, and unsupported bot arsenals leaving the game loaded. Q1 dm4→dm5 travel retains the exact admitted composition, module artifacts and asset precedence while resolving the new map. At that checkpoint, native Q1 and foreign Q3 bot arsenal support, health/armor/powerup goals and remaining source-ID tactical comparisons were incomplete. These source commits are included in the installed `b071a5c` development executable.

Checkpoint `d771286` adds source-owned weapon/ammo pickup goals to the shared bot controller, following observer `2aba7e5` and grant-preview helper `0625ca0`. Independent actual probes on the final frozen source cover Q1 dm4 with selected base-Q2 weapons and native Q2 base1: bots start in verified supported approach lanes, then ordinary movement/touch collects the untouched authored pickup, matches previewed inventory gains and observes source respawn. Q1 collected a rocket launcher plus five rockets and five grenades; Q2 collected a shotgun plus ten shells. Q2 checks also preserve a full-ammo refused pickup, value ammo for an empty owned weapon and retire a removed pickup identity. The library fixture covers generation reuse; physical same-slot reuse was not established by the map probe. Reachable trigger-volume targets and generated-edge progress fix the observed route failures without changing native AAS steering. Final strict/policy checks passed, with focused arena/source checks (6 tests/268 assertions). At that checkpoint, health, armor and powerup goals, remaining native-Q1 weapons/foreign-Q3 bot arsenals and broader modes remained open. This source checkpoint is included in the installed `b071a5c` executable; formal acceptance counts are unchanged.

Checkpoint `55e0b80`, following native preview `00b08bf` and weapon-intent routing `2c632a4`, admits native base-id1 Q1 deathmatch weapons through the same bot controller. The bounded Q1 rerelease dm4 fixture uses a Q2 character and source teleport into a verified supported 96-unit approach lane: ordinary bot movement collects the untouched authored rocket launcher with exact previewed ammo, observes respawn, switches from axe to a ranged weapon and deals actual damage, then retains its client through dm5 travel and removes the bot cleanly. Root independently passed the full shared-world fixture (3 tests/59 assertions); final isolated strict/policy checks passed. Existing native Q3 behavior remains supported. Grenade-launcher tactics, other Q1 programs, team modes and all-map coverage remain unqualified. These commits are included in the installed `b071a5c` executable; formal acceptance counts are unchanged.

Checkpoint `24fe385` reads actual Q1/Q2 weapon owners independently of Q1/Q2 map ownership, using engine join `daee0a8` to preserve repeated selected-Q1 fire. The bounded new case uses Q2 rerelease base1, Q2 movement/character and selected classic Q1 weapons. From a supported source-teleport approach lane, ordinary bot movement collects the untouched Q2 shotgun pickup into the previewed Q1 inventory, observes source respawn, reads map-owned quad, then switches from axe to shotgun and consumes ammunition while dealing damage. Root independently passed all four shared-world cases (4 tests/77 assertions); final isolated strict/policy checks passed. At this checkpoint, Q3-map foreign arsenals, remaining weapon tactics, team modes and all-map coverage were unqualified. The installed executable now includes this checkpoint through `b071a5c`; formal acceptance counts are unchanged.

Checkpoint `f8447c6` admits selected base-Q2 weapon bots on baseq3 standard deathmatch maps through the same controller and actual arsenal observations. Detached movement prediction no longer executes native Q3 weapon callbacks for a foreign arsenal. The bounded actual Q3 fixture uses a supported approach lane and deliberately empties starter bullets to verify alternate-weapon choice, then checks mapped shotgun pickup, damage provenance and native medkit use to 125 health. Root independently passed the frozen shared-world and arena tests (6 tests/311 assertions); full isolated strict/policy checks passed. At that checkpoint, broader modes and other foreign arsenals remained unqualified. The installed executable now includes this checkpoint through `b071a5c`; formal acceptance counts are unchanged.

Checkpoint `ebc68d0` preserves map-owned combat feedback and bot powerup observations with foreign weapons. Actual Q3-map selected-Q2 shotgun damage updates native hit count and attacker feedback; Q1/Q2 quad and environmental protection are read from the map owner after arsenal inventory refresh. Expiry checks query source-clock boundaries; they do not prove timer execution. Root independently passed the frozen affected shared-world cases and native Q3 arena (4 tests/301 assertions, two unchanged cases intentionally filtered), plus source/authority checks (14 tests/146 assertions). Full isolated strict/policy checks passed. At that checkpoint, health/armor/powerup pickup goals, broader modes and complete source feedback remained open; the installed executable and formal acceptance counts are unchanged.

## Recent monster and bot checkpoints

Checkpoint `bec7c29` expands the classic Q1 ordinary roster through the existing source creature services. Root's actual authored-map checks passed four tests with 278 assertions. Checkpoint `b85c499` adds ordinary classic Q2 species, source-equivalent authored placement, selected-source cadence and projectile save continuation. Checkpoint `7146aff` updates shared bot combat observations and aim. Independent frozen checks passed nine monster tests with 645 assertions, two bot tests with 37 assertions, one native arena test with 217 assertions and 54 affected native tests with 799 assertions. Same-source Q1 wizard and Q2 mutant placement preserves native geometry behavior; different selected bounds still reject. Mutant jump continuation uses an explicit source attack invocation, so this evidence does not establish natural AI jump initiation.

Checkpoint `e20b6d6` adds ordinary Q2 rerelease roster admission and shared monster muzzle audio, including extended rerelease flash IDs. The common effects consumer now handles berserk slam and plain explosion; classic monsters emit the actor-bound typed muzzle event. Actual resource closure checks and independent roster, native source, dummy-audio and CPU effects tests passed 36 tests with 615 assertions. This source checkpoint is included in the installed `b071a5c` executable.

Checkpoint `5c0bb90` adds rerelease heavy monsters and reads host gravity in source callbacks. Independent retail and native-source checks passed 29 tests with 426 assertions.

Checkpoint `053f70e` renders source-modeled beams through the shared model path and admits the ordinary Q1 rerelease roster. Independent catalog, authored-map, model and parasite checks passed 16 tests with 962 assertions; after three narrow typing corrections, focused beam and rerelease-ogre checks passed two tests with 121 assertions. Root compared the seven unchanged files and inspected the three corrections, actual CPU/GL beam captures and native source algorithm. Rerelease Q1 e1m2 evidence covers natural ogre grenade flight, fresh restore, attributed damage and authored kill credit.

Each checkpoint passed strict TypeScript and policy checks on its frozen source. The heavy-monster/gravity and modeled-beam/Q1 rerelease checkpoints are included in the installed `b071a5c` executable.

Checkpoint `7b1fd47` admits Q1 fish and classic/rerelease Q2 flippers through existing shared actors and preserves source-equivalent rerelease flying placement. Independent final checks passed the full Q1 application file (5 tests/391 assertions) and Q2 aquatic plus rerelease flying placement (3/64); the unchanged catalog checks passed 5/152. Actual water movement, chase/bite and fresh save continuation are bounded to those authored maps. Strict TypeScript passed before policy; both passed on the final snapshot. This checkpoint is included in the installed `b071a5c` executable.

Checkpoint `8f5e9a3` joins selected Brain power armor with actual foreign Q1/Q3 weapon damage. Root independently passed eight catalog/actual-brain tests with 260 assertions, including unchanged authored placement, front/rear damage, native rerelease armor arithmetic, source cells and fresh saves. A synthetic one-damage probe is separate from the real weapon checks. Strict TypeScript passed before policy; both passed on the frozen source. This source is included in the installed `b071a5c` executable.

Checkpoint `0d4d3c4` preserves Shambler rocket resistance across native and foreign source weapons through the actual victim service. Root independently passed the actual fixture and existing authority suite (14 tests/198 assertions): native Q1 halving remains, actual Q2/Q3 rockets deal 53/50 in the fixture, rail damage stays unchanged, and attached/restored victims retain the source rule. Final isolated strict TypeScript and policy passed. The installed executable now includes this source through `b071a5c`.

These bounded results do not establish a complete bestiary, boss/finale or mission-script parity, all-map composition, full audio/rendering fidelity or formal package acceptance.

Checkpoint `7ecc497` makes native Q3 damage use the same victim-owned armor resolver as shared attacks. Root reviewed the unchanged production extraction, passed the prior snapshot checks (9 tests/202 assertions) and reran the corrected fixture (2/50). The final frozen source passed strict TypeScript followed by policy. This checkpoint is included in the installed `b071a5c` executable.

Checkpoint `ff08df0` preserves native Q1 placement when the original floor drop fails, using source setup facts rather than moving the authored creature or relaxing different-source geometry checks. Root independently passed the exact placement fixture (2 tests/39 assertions).

Checkpoint `6331b05` rejects application launch choices whose QC, QVM or native execution path is not joined, before creating a world. This makes the existing execution boundary explicit; it does not implement a guest application path. Checkpoint `d4ed30f` preserves the modeled beam endpoint in Q2 network presentation. Root independently passed catalog and network checks (6 tests/65 assertions), including native UDP. The explicitly synthetic lower-level rerelease endpoint probe does not establish supported rerelease application wire admission or full source rendering parity. The combined eight-file snapshot passed strict TypeScript followed by policy. These checkpoints are included in the installed `b071a5c` executable.

Checkpoint `bd4308b728ee125fe60f28cb9bf40b2b6f06aa3a` preserves Q1 authored deathmatch inhibition by extending the selected-monster source mask from `0x700` to `0xf00`. Root independently passed the exact e1m5 fixture (1 test/14 assertions), covering native/selected filtering and rejection of an unknown behavior flag. The frozen source passed strict TypeScript followed by policy. The installed `b071a5c` executable includes this source correction.

Checkpoint `be01725` publishes already-committed QC source damage observations through the common authority without replaying source writes. Root independently passed 24 QC/authority tests with 255 assertions on the final typed fixture. The witness uses a known-id1 fixture mapper; it does not join application guest lifecycle or establish arbitrary mod behavior.

Checkpoint `43924e30c290fa1d657ff9a7b6c62617f52352af` joins base Q2 classic/rerelease arsenals in both edition directions, including canonical catalog/UI selection, source resources, active weapon presentation and shared pickup overlap handling. Root independently passed nine runtime tests with 239 assertions and seven catalog/pickup tests with 928 assertions. Actual base1 checks cover independent source cadence, authored shotgun touch, active kick/loop state, map-owned silencer/noise, live projectile and fresh-save continuation. The grenade item case explicitly uses a synthetic native spawn when that map edition lacks an authored grenade item; it does not prove natural pickup navigation. This is bounded base-Q2 edition mixing, not expansion arsenals, arbitrary multi-provider primary weapons or every map/movement permutation. Both checkpoints passed strict TypeScript followed by policy on frozen source. The new compiled candidate passed the standard build gates and silent help/menu checks before atomic installation.

Checkpoint `40726d0` joins base-id1 Q1 classic/rerelease arsenals in both edition directions. Root independently passed the full runtime/catalog/startup checks (18 tests/1,349 assertions), then the corrected deadline-presence fixture (2/86). The frozen source passed strict TypeScript followed by policy. Expansion and QuakeWorld arsenals remain outside this evidence.

Checkpoint `46ca5f0` joins the QC walkmove adapter to existing shared monster movement/collision and trigger dispatch, including source ground-entity projection. Root independently passed QC and shared-physics checks (20 tests/231 assertions); isolated strict and policy passed. At that checkpoint, an unchanged e1m1 source probe progressed through walkmove to the still-unimplemented checkclient builtin. This is a bounded library/host join, not an application guest lifecycle or campaign claim.

Checkpoint `d1a96b0` preserves map-defined native monsters beside explicit per-class replacements, including native bosses and separate source ownership, counters and fresh-save continuation. Root independently passed the full Q1 application/catalog/save checks and new actual Q2 mixed-roster case (22 tests/649 assertions). The integrated QC-plus-roster snapshot passed the focused mixed-roster, QC spatial and shared-physics checks (9/121). Strict and policy passed before a final two-call awaited-close cleanup; root reviewed that exact cleanup and tested the final integrated tree. At that checkpoint, the startup roster editor was separate work; and Q3-map creature admission remains open. These three source checkpoints are included in the installed `b071a5c` executable.

Checkpoints `a39c254` and `4db8011` separate Q1 weapon recoil from movement and expose per-class monster roster choices in the startup menu. Root independently passed recoil, menu/source and integrated roster checks (23 tests/451 assertions) and inspected CPU/GL roster screens. Strict and policy passed on the frozen source after a catch-parameter type annotation. The recoil visual evidence is camera-only; it did not establish complete viewweapon rendering.

Checkpoints `e973079` and `c12153d` fix common selected-weapon presentation and share Q1 client visibility between native and QC hosts, including saved cache state and source field projection. Root independently passed runtime and render/HUD checks (33 tests/1,144 assertions, with one pre-existing SDL skip) and inspected eight CPU/GL e1m1/q3dm1 captures. The final frozen source passed strict and policy after removal of an unused helper and duplicate import; root reviewed those exact cleanups. At that checkpoint, the controlled Q3 prepared-scene capture predated shotgun animation, and false HUD inventory and overlap remained separate limitations. Visibility sharing does not complete guest frame physics or application guest lifecycle. These four commits are included in the installed `b071a5c` executable.

Checkpoints `38042e2` and `703cc0a` add QC movetogoal execution, correct selected-source prediction angles and project canonical weapon/ammo status and icon resource metadata for native, selected and remote arsenals. Root independently passed 41 tests with 1,753 assertions across source, prediction, catalog/assets and native UDP checks. Five actual retail cases confirmed source provider/content and ammo transitions: Q1 super-shotgun transitions from one shell to zero; Q2 BFG is blocked at 49 cells, ready at 50 and consumes 50. The QC startup probe completed 23 startup callbacks and 12 actual movetogoal calls; it does not establish complete guest frame physics. At that checkpoint, the display consumer remained separate work; canonical HUD data alone did not prove the displayed HUD correct. The frozen source passed strict TypeScript followed by policy. Both commits are included in the installed `b071a5c` executable.

Checkpoints `8b88b8d` and `68c33b9` join QC id1 damage boundaries/shared pusher execution and the common weapon HUD/radial consumer, with source-grounded viewweapon lighting and demand registration. Root independently passed the pusher/movement/QC suite (32 tests/503 assertions). Earlier slice checks passed QC/authority 27/390, render/UI/Q3 application 17/562 with one existing SDL skip, and standalone Q3 presentation 4/35; those overlapping results are not summed. An actual Q3-map/Q1-arsenal radial replay matched one-shell, zero-shell and unmetered-axe states through ordinary commands, with no false ammo sound in 64 checks. Head/gun overlap remains a known presentation limit. The final typing-only fixture cleanup passed strict and policy.

Checkpoints `365634a` and `36c450b` use native Q1 source-local pusher timers, shared ground publication and geometry-edition Q2 lighting, including resolved external-brush provenance. Root independently passed four distinct actual/runtime/model/application files (44 tests/1,495 assertions, no skips) and reviewed four CPU/GL edition captures. Strict and policy passed on the exact 35-file snapshot. This is bounded source behavior and presentation evidence, not full campaigns, guest lifecycle or feature-family acceptance.

Checkpoints `75ee9bb` and `2fabe9c` move Q1 voices into the common Q1/Q3 audio core and admit pinned id1 QC through dedicated `Application.open`, without a TypeScript gameplay fallback. Root independently passed 80 distinct tests with 1,953 assertions across seven files, with no skips: audio 17/164, QC/runtime 33/1,300 and water/movement/foundation 30/489. Strict and policy passed on snapshot source `8eb91805cbcb772ca16e0667b1cf77f1586318a414a36be409967d039d2d173d`. The actual application fixture ran three frames at 100/100/50 ms with 30 PUSH entries and source frametime publication. A separate owner probe observed 258 moving-actor frames over four seconds without raw bounds mismatch; that is bounded review evidence, not campaign acceptance. At that checkpoint, Q2 audio cutover remained in flight; QC client execution, saves and unknown programs were unsupported. These commits are included in the installed `b071a5c` executable.

Checkpoint `484b651` moves Q2 voices into the same common audio core as Q1 and Q3 and removes the separate mixer implementation/export. Root independently passed 22 audio tests with 201 assertions using dummy audio/offscreen video; strict and policy passed on the exact frozen five-file snapshot. One core does not establish complete feature union, audible fidelity or a performance advantage; environmental reverb, universal Doppler/application joins and broader source behavior remain open.

Checkpoints `3f31e53` and `93c3248` join pinned QC attacks/shared aiming and prime startup audio from measured preparation cost. Root independently passed 52 tests/862 assertions across four files (QC/native foundation 29/656, audio 23/206). The final snapshot retained those tested bytes except reviewed type-only Map/target-array annotations; strict and policy passed. Root normal-Application CPU/GL probes ran 32 frames each with dummy audio/offscreen video and no empty queues. Initial/final queued audio was CPU 651/450 ms and GL 639/152 ms. The buffering trades latency for startup coverage: a separate owner run still observed a later 6.7 ms CPU shortage. These bounded silent probes do not establish that all choppiness is fixed or qualify audible latency/fidelity. At that checkpoint, source was pushed through `93c3248` and QC client lifecycle remained uncommitted. The attack/startup commits are now included in the installed `b071a5c` executable.

Checkpoints `b895d2e` and `5dcdd93` preserve dedicated QC client source ordering/live body shape and join environment sampling to common application audio. Root independently passed 26 distinct tests/725 assertions: clients 6/391, movement 8/138, audio/collision 10/144 and native/selected bot cases 2/52. Strict and policy passed on frozen source `5324ef9e1bf5eb8a3292922ad0544ec2e365801c001269a7c182cbfe7163bd9a`. Corrected source-think ordering, changed hulls and removal without PostThink are bounded lifecycle evidence; graphical/network QC clients, QC saves, unknown programs and general world hazards remain unsupported or unqualified. Player checkpoint subrecord version 1 rejects older unversioned records. At that checkpoint, reverb loaded genuine Q2 rerelease environment data from selected audio content; the foreign Q1 proof selected Q2 rerelease audio, and independent environment-versus-sound selection remained in flight. Reverb sampling includes actual floor/air/sky contacts and transformed inline geometry; sky evidence is point-based, unknown floors remain explicit, and audible/EFX parity is unproved. At that checkpoint these source commits were outside installed `b071a5c`; they are included in the current `3a073e8` build.

Checkpoints `406689c` and `b6cc48e` select environment data independently from game sounds and preserve provenance at all nine pinned id1 environmental damage sites. Root independently passed 18 distinct tests/739 assertions: environment audio/menu/recipe checks 15/231 across three files and final hazards 3/508. The exact frozen source passed strict and policy; root inspected three CPU and two GL menu captures. Selected environment data retains native Q1/Q3 sound assets, missing data fails explicitly, and menu plus recipe encode/decode are covered. A full application save/reload for this selection was not separately run. Hazard proofs cover nonfatal damage, source bytes and velocity; lethal obituary behavior and nested hazards remain unverified. At that checkpoint both commits were outside installed `b071a5c`; they are included in the current `3a073e8` build.

Checkpoints `3a4b23f`, `17c87c6` and `67f73c7` fix singular Doppler hangs, extend the generic QC broadcast host, and join standard Q1 point effects and sounds to the common consumers. Root independently passed 30 distinct tests/691 assertions: Doppler 23/190, QC 3/371 and effects 4/130; strict and policy passed on the exact seven-file snapshot. Doppler uses intentional large-span averaging while preserving ordinary captured PCM; universal Doppler remains open. The QC host covers eight source writers and 14 temporary-entity types for `MSG_BROADCAST` only, without admitting unknown mods. Actual CPU/GL captures and sound checks used mixed e1m1 with Q1 movement/weapons and Sarge, not an all-native configuration. Root inspected active and correctly cleared effects; the earlier mislabeled expiry capture was a probe error. At that checkpoint these commits were outside installed `b071a5c`; they are included in the current `3a073e8` build.

Checkpoints `9196e76` and `fb6f421` join QC localcmd to application command phases and expose source/off Doppler selection. Root passed 41 distinct tests/675 assertions: corrected Kfgrx5 QC fixtures 4/355 and audio/menu/save 37/320 carried from BRSS80 because all 17 Doppler files were byte-identical. Strict and policy passed on corrected Kfgrx5. The installed Hipnotic function runs unchanged. Application evidence uses fixture-created native info_command actors and QC frame-hook injection to check startup, next-frame, wait and unknown-command behavior; it does not show maps naturally triggering those commands, and map transitions remain unverified. Doppler source/off selection is covered by recipe, audio and actual CPU/GL menu evidence, not universal moving-loop Doppler. These two source commits are newer than installed `3a073e8`.

Checkpoints `05ce434` and `3658b90` release native Q1 torches, globes and func_illusionary actors into the persistent shared scene. Alias flames, inline BSP and external BSP routes were exercised, with save continuation and 24 ordered e1m2 torch records in native UDP signon. Sprites use the existing common renderer; separate sprite execution was not proved here. Generic QC makestatic preserves model-string/precache lookup, actor removal and empty-model index 0 semantics. Root independently passed 17 distinct tests/653 assertions; strict and policy passed on exact static10 NfPNYu. The original brush omission was corrected before acceptance. Root inspected actual native e1m2 and fixed-camera e2m1 CPU/GL captures, including an explicit static-consumer-withheld comparison; these are unified-runtime captures, not native reference imagery. QC graphical/network/general-program guards remain, and extra addon override static spawns are not yet migrated. Installed `3a073e8` does not include these commits.

Accepted checkpoints `a8ace02`, `3e57748`, `60cfc8e` and `a5fca35` join per-seat haptics and strength controls, Q1 addon static overrides, and NAV2 provenance/live navigation observations. Root independently passed 37 tests/585 assertions across five files for the final strength/static/navigation snapshot; strict and policy passed. Four haptics requirements and NAV2 loading are now Done through common extensions. Dynamic navigation remains Not done because unsupported mover travel is still rejected. Strength retains authored envelope timing and uses existing frontend preferences; disk persistence and physical-controller qualification are not claimed. Installed `3a073e8` is unchanged and excludes these checkpoints.

## Installed build

The Linux x64 executable at `/home/buzzkill/Projects/qfiles/quake-typescript` is built from `3a073e87ec7a4126f396e15833b8afb9df569403`, replacing `b071a5c`. Without arguments it opens the main menu before creating a world, player or game audio. Bun 1.3.14 produced the 119,519,360-byte executable from an isolated tracked snapshot; strict typing, policy and input-stability gates passed. Installation was atomic with mode `0755`. Localcmd and source/off Doppler selection are not included.

- Source tree: `cc81be471ec416658908febd465abeb370f696c0`.
- Source SHA-256: `5f34c8ceb10e00971938eab006e3a5297620d48a7aaecade4ea0834049168673`.
- Executable SHA-256: `c6f3a0df8c03aee584cd45dbd45ea5df1064808b90f2ea38b1955a2a4281ddcd`.
- Provenance: adjacent `quake-typescript.build.json`; previous executable, sidecar and instructions use backup prefix `quake-typescript.previous-20260912T211600531Z`.
- Root independently verified executable SHA, size, mode, sidecar and installed destination/instructions. Help exited 0; the no-argument menu stayed alive eight seconds with offscreen/dummy drivers, then SIGTERM exited 0 with empty stderr. Actual CPU and GL e1m1/Q1-movement/Sarge runs each completed eight frames with exit 0. Both warned that classic id1 music track 6 was unavailable; a loose track exists only in rerelease/id1, so these runs do not prove music playback. All owned processes closed.

Development saves from older builds are incompatible: Q1 foundation version 5 rejects version 4, and Q1 player checkpoint subrecord version 1 rejects old unversioned records. QC saves remain unsupported.

Start with `~/Projects/qfiles/quake-typescript`. The mouse-driven main menu offers Single Player, Multiplayer, Load Game, Options and Quit. Configuration resolves independent supported selections into the exact recipe on Play; failures return to the retained draft. Load Game lists actual saves with metadata and unavailable-save diagnostics. Display Apply changes the actual renderer; Sound/Controls reuse in-game bindings. Independent frozen probes passed actual Play/save/load, preferences, display Apply and runtime-failure return; CPU/GL menu and pause captures were inspected. These results do not establish complete campaigns, arbitrary pickup replacement, Q3 full saves, server/lobby/bot administration, audio fidelity or full rendering parity. Installed Quake III or rerelease font assets are currently required for proportional menus. The current build verification used offscreen video and dummy audio.

## Current source and runtime state

Offhand Q2 grenades are verified on Q1/Q2/Q3 worlds. Shared grapple slots now use actual source handoffs, outgoing presentation and saved state; root checks cover representative native/mixed selection, rapid switching, gauntlet suppression, saves and travel. Rerelease CTF follows an actual q3dm11 door through 94 units and its stop. LMCTF retains its initial hit and 0.7-second player-contact pulses, while shared attachment saves remap anchor identities. Threewave/LMCTF moving Q3 brush checks pass under Q1 movement; native Q3 movement beside a real LMCTF hook also passes. Active work addresses the remaining monster roster, source cadence, mission obligations and visual fidelity. The close army muzzle capture remains insufficient evidence of firing-frame fidelity. Map BSPs and sidecars stay within logical map content. Current saves use recipe schema 3, the QTSAVE2 container and world:simulation provider schema 11; unsupported versions reject explicitly. Complete sky/render fidelity, general foreign source views and Q3 full saves remain outside this evidence. These checkpoints passed isolated typecheck and policy; they do not accept a complete package.

Verified source checkpoint `423101a` fixes Q2 intensity upload, MD2 face culling, Sarge child-model skin mapping, adaptive audio queuing/frame cost and selected Q3 jump audio under Q1 movement. For selected-Q3-arsenal checkpoint `c19745a`, the lead reports actual shared-actor checks on Q1/Q2 maps with all three movement providers, exact damage, rockets/grenades, saves and travel. These are bounded results, not complete interoperability or installed-binary evidence. Root independently checked real Q1 e1m1 pickups and e1m2 travel, plus Q2 classic/rerelease base1 pickups and base2 travel: pending weapon switches survive saves, Q1 mission keys clear, respawn restores starter capacities, and Q2 power-armor cells remain intact. Old interim Q1 selected saves without a supply profile now reject explicitly. This documentation refresh did not rerun gameplay.

The user's architecture rule applies to every subsystem: coalesce the strongest implementation and add every useful feature from every game to that common system. One engine, world and executable must expose those capabilities across content. Source-specific policies preserve required behavior. The latest user direction requires full independent swappability and a usable startup menu; `ca5003b` supplies the bounded menu workflow above while engine interoperability remains incomplete.

| Work | Confirmed bounded progress | Remaining dependency or evidence |
| --- | --- | --- |
| W73/W74 first playable | Mixed-provider CPU/GL gameplay, firing, local seats and travel have live evidence. | Complete specimen mission, fresh-process and co-op criteria remain subject to formal acceptance. |
| W41–W51 content and composition | Selected-Q3 arsenal checks cover shared actors, movement, damage, projectiles, saves and travel. Named Q1 id1 and classic/rerelease Q2 base-program standard-match supply profiles now admit native pickups into that arsenal. | Ordinary classic/rerelease Q1 and Q2 creatures attach through shared actors and source services, with bounded authored-map attack/save evidence above; remaining species and mission obligations are still open. Arbitrary pickup replacement, unsupported expansion arsenals and supply profiles, remaining composition-specific semantics and full interoperability remain incomplete. |
| W23 saves | Q1/Q2 capture and restoration are implemented; reported application checks cover CTF/LMCTF save restoration and captures. | Full source-private continuation, all original formats and every provider remain open. Q3 and guest save support are not inferred from Q1/Q2 results. |
| W47 CTF/LMCTF | Pause/voting landed in `ec2686b`; match travel/settings landed in `cdc2cc5`. | Complete match, administration, HUD and voting workflows still require combined acceptance. |
| W57/W58 bots and navigation | Checkpoints `060403c` and `7146aff` complete bounded standard-deathmatch checks for the nine basic map-family/arsenal-family combinations through the same controller. Actual authored supply pickup, weapon choice, firing and damage checks use supported approach lanes. `75d7bf1` shares native Q3 item-inventory projection for health, armor and powerup decisions; later supply and arsenal joins preserve those observations. | Broader maps and modes, expansion arsenals, remaining weapon tactics, and complete cross-family pickup/goal semantics remain unqualified. The nine basic combinations are bounded cases, not an all-content compatibility matrix. |
| W18/W19/W59 presentation | Checkpoint `423101a` fixes Q2 intensity, MD2 culling, Sarge child skins, audio queuing/frame cost and selected-character jump audio. | The installed executable is updated through `3a073e8`; see the [build record](#installed-build). Shared startup/pause menus have inspected CPU/GL captures; complete presentation coverage remains open. |
| W30–W40 guests | Interpreted native loaders/execution are committed. A native rerelease retail run passed 36 assertions across 502 edicts through Init, Spawn, ClientConnect, Begin, Think and active RunFrame. | Full-map JSON saving exceeded the 20-million-instruction budget. Full callback, semantic override, gameplay and save coverage remain incomplete. |
| W52–W55 multiplayer | NetQuake 15 and Q3 protocol 68 server application joins landed in `ec2686b`; Q3 pure/archive downloads landed in `cdc2cc5`. | Native wire support covers family-compatible configurations. Mixed transport, connected-peer behavior and full source-wire interoperability remain unfinished. |
| W66–W72 integration and release | W66 application integration is underway, with bounded runtime evidence above. | Complete W66, native gameplay W40 and performance W68 before full W67 configuration accounting, Linux artifact qualification, independent review and release closure. |

The user confirms LMCTF has no soundtrack of its own. The music check verified `lmctf09` requests track 1 and decoded inherited Q2 track 02 without changing playback or files; its warning does not establish missing LMCTF content. The immediate work is remaining creature behavior, foreign-target damage semantics and mission coverage, then combined engine interoperability evidence. W60 has bounded composition/save menu implementation; W61 server, lobby and bot administration workflows remain incomplete. The graph still requires official gameplay → W50/W51 → W66 and W66 plus native execution → W40, with menus required before full W66 acceptance. W67 also requires independent evidence W63–W65, content completeness W07 and performance W68. Broad hardening follows imports, live human play and optimization under the user's code-first sequence. No full package is accepted by this refresh.

## Current coordination

The lead assigns exclusive paths and uses Astra at low or medium effort for current recovery tasks. `q1_monster_roster` is the sole current Git writer, staging exact frozen snapshots after the lead reviews their scope and evidence. Historical owner names and xhigh dispatches below do not assign current ownership or effort. The lead must assign any new work explicitly.



## Historical handoffs at the core checkpoint

The following table records the earlier core-checkpoint handoff. Agent names are relative to `/root/`. Those dispatches used `gpt-6-astra` with `xhigh` effort and `review_w06_platform` as Git writer. They are retained as history and do not override current lead assignments.

| Work | Current owner | Exclusive paths or bounded handoff |
| --- | --- | --- |
| W23 and W41 state | `w41_actors` | `src/persistence/**`, `src/world/actors/**`, `src/world/gameplay/**`, and corresponding existing checks; real identity and save joins |
| W42 Q1 base | `w42_q1_base` | `src/content/q1/base/**`, `tests/gameplay/q1-base/**` |
| W43 Q1 mission packs | `w59_native_ui` | `src/content/q1/missionpacks/**`, `tests/gameplay/q1-missionpacks/**`; earlier native UI slice handed off |
| W44 Q1 additions | `w49_q3_modes` | `src/content/q1/addons/**` and corresponding checks; running source import |
| W45 Q2 base | `w45_q2_base` | `src/content/q2/base/**` except `player/**` and the completed `entities/**` slice, with matching base checks |
| W45 Q2 player | `w45_q2_player` | `src/content/q2/base/player/**` and matching player checks |
| W46 Q2 mission packs | `w45_q2_entities` | `src/content/q2/missionpacks/**`, `tests/gameplay/q2-missionpacks/**`; earlier 38-entity source slice handed off |
| W46 Q2 mission-pack monsters | `w45_q2_base` | Monster subtrees within `src/content/q2/missionpacks/**` and matching checks, reserved from the W46 parent |
| W48 Q2 rerelease | `w45_q2_player` | `src/content/q2/rerelease/**` except `monsters/**`, and corresponding rerelease checks except monster checks; running source import |
| W48 Q2 rerelease monsters | `plan_validator` | `src/content/q2/rerelease/monsters/**`, `tests/gameplay/q2-rerelease/monsters/**`; source-specific registration over the shared Q2 monster controller |
| W74 Q1 restoration | `w74_q1_foundation` | `src/content/q1/foundation/**` and corresponding existing checks |
| W74 Q2 restoration | `w74_q2_foundation` | `src/content/q2/foundation/**` except the separately owned monster and weapon paths |
| W74 Q2 monsters | `w14_q3_models` | `src/content/q2/foundation/monsters/**` and matching checks; finishing this source slice |
| W54 Q2 network | `w54_q2_network` | `src/network/q2/**`, `tests/network/q2/**` |
| W73 network integration | `w54_q2_network` | `src/app/bootstrap/network/**` |
| W31 PE | `w01_q1_features` | `src/guest/pe/**`, `tests/guest/pe/**` |
| W32 ELF | `w01_q2_features` | `src/guest/elf/**`, `tests/guest/elf/**` |
| W33 i386 | `w02_q1_reference` | `src/guest/x86/**`, `tests/guest/x86/**` |
| W34 x64 | `w02_q2_reference` | `src/guest/x64/**`, `tests/guest/x64/**` |
| W35 floating-point | `w02_q3_reference` | `src/guest/floating-point/**`, `tests/guest/floating-point/**` |
| W36 ABI | `w01_products` | `src/guest/abi/**`, `tests/guest/abi/**`; this dispatch uses `xhigh`, superseding its earlier inventory effort |
| W73 Q3 host | `w49_q3_gameplay` | `src/app/bootstrap/simulation/q3/**`, under the lead's narrow bootstrap handoff |
| W73 effects | `w49_q3_presentation` | `src/app/bootstrap/effects.ts` |
| W73 audio | `w18_cpu` | `src/app/bootstrap/audio.ts` |
| W17 and W15 corrections | `w17_scene_world` | Scene shadow caster and `src/materials/legacy-fog.ts`; other material writes stay with their owner |
| Build and commit writer | `review_w06_platform` | `tools/build.ts`, `src/types/png.d.ts`, and the lead-assigned policy correction. Sole Git writer, staging only reviewed exact paths |
| Execution ledger | Lead-controlled handoff | The bounded refresh by `plan_validator` is complete; that worker now owns W48 monsters. The lead assigns further writes to `docs/work-packages.json`, `docs/project-plan.md`, `docs/execution-status.md`, and `docs/dependency-graph.mmd` |

Q1 network source was handed off by `w53_q1_network`, and Q3 network source by `w55_q3_network`; both workers have left the active pool. W62 artwork was handed off by `w01_products`, now assigned ABI work. `w59_native_ui` completed the art-manifest cleanup and its documentation before moving to W43. Those completed source slices remain running at package level until the lead evaluates integration and acceptance. The lead assigns a current writer before corrections to an unowned completed slice. Historical worker names do not reactivate them.

## Bounded live evidence after the core checkpoint

The lead independently ran this actual source command with exit 0, loading Q2 `base1` with Q1 movement and Q3 Sarge:

```bash
env SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy bun run src/main.ts --renderer cpu --hidden --width 160 --height 120 --frames 2
```

That success preceded the Q2 floater registration error and correction recorded above. The command remains the reproducible startup check; this ledger update did not rerun it during concurrent integration.

The retained `/tmp/w73-interaction.ts` script opens the real CPU application, applies movement and fire input, steps the simulation, checks displacement, collects presentation-event kinds, and captures pixels. Its two preset runs retained `/tmp/w73-q2-classic-baseq2.png` and `/tmp/w73-q1-rerelease-id1.png`. The retained `/tmp/w73-menu.ts` script opens the real native menu, checks settings focus, and captures `/tmp/w73-menu.png` and `/tmp/w73-controls.png`. The files were present and the script contents were inspected for this ledger update. Execution success and visual review are lead-reported observations, not fresh runs by the ledger owner.

After the floater-table correction, the lead reports a successful actual application run of `/tmp/w73-travel.ts`. It reloads `base1` while retaining the native window, seat object, and client identity. The emitted result retained 125 health and nine picked-up bullets, replaced actor generation 10 with 21, and rejected the previous handle. The retained script was inspected and explicitly checks identity invalidation, seat/client/window retention, and the bullet count. This is one bounded source travel case, not save-load completion or acceptance of every campaign transition.

The lead also reports CPU and GL startup for both specimens, a two-seat CPU run, and dedicated startup. Exact retained command output for those additional runs is not recorded here. Temporary captures and scripts can disappear, and none supplies the final original-source, campaign, multiplayer, or compiled-release evidence required at W67–W71.

## Earlier dispatch history

This table preserves earlier ownership records and completed handoffs. It is not a fresh dispatch or permission to edit a path that the lead has reassigned. The current handoffs above and later explicit lead assignments take precedence. The earlier `execution_state` assignment does not grant the current ledger owner permission to change validator code or fixtures.

All workers used `gpt-6-astra`. Effort was `high` for `w01_products`, `w06_audio`, `w06_fonts`, `execution_state`, `w04_manifest_tests` and `additional_fixtures`; other dispatches used the package default `xhigh` unless the lead announced an override.

| Package | Owner | Exclusive paths |
| --- | --- | --- |
| W01 | `w01_census` | `verification/source-manifest.json`, `verification/feature-ledger.json`, `tools/inventory/source-census.ts`, `tools/inventory/aggregate-features.ts`, `tests/inventory/source-census.test.ts`, `tests/inventory/feature-ledger.test.ts` |
| W01 | `w01_q1_features` | `verification/features/q1.json` |
| W01 | `w01_q2_features` | `verification/features/q2.json` |
| W01 | `w01_q3_features` | `verification/features/q3.json` |
| W01 | `w01_products` | `verification/product-manifest.json`, `tools/inventory/products.ts` |
| W01 | `w01_lineage` | `docs/reference-contracts.md` |
| W01 | `additional_fixtures` | `verification/additional-fixtures.json`, `tools/inventory/additional-fixtures.ts`; searches user-provided Downloads and Steam locations for missing fixtures |
| W02 | `w02_references` | `tools/reference/environment.ts`, `tools/reference/schema.ts`, `tools/reference/README.md`, shared runner files directly under `tools/reference/`, `verification/reference-environment.json`, `verification/reference-targets.json` |
| W02 | `w02_q1_reference` | `tools/reference/q1/**`, `verification/reference-cases/q1/**` |
| W02 | `w02_q2_reference` | `tools/reference/q2/**`, `verification/reference-cases/q2/**` |
| W02 | `w02_q3_reference` | `tools/reference/q3/**`, `verification/reference-cases/q3/**` |
| W02 | `w02_native_q2`, dispatch assigned | `tools/reference/q2-native/**`, `verification/reference-cases/q2-native/**` |
| W03 | `w03_policy` | `package.json`, `bun.lock`, `tsconfig.json`, `eslint.config.ts`, `tools/check-policy.ts`, `tools/build.ts`, `.github/workflows/**`, `tests/policy/**`, `.gitignore` |
| W05 | `w05_contracts` | `src/contracts/**` except the peer files below, `docs/contracts.md` |
| W05 | `w05_content_contracts`, bounded content interface complete | `src/contracts/content.ts` |
| W05 | `w05_scene_contracts` | `src/contracts/scene.ts`, `src/contracts/render.ts`; frozen except explicit lead correction handoffs |
| W05 contract correction | `w25_q2_movement` | `src/contracts/movement.ts` for Q2 rerelease fields |
| W05 | `w05_execution_contracts` | `src/contracts/protocol.ts`, `src/contracts/execution.ts`, `src/contracts/ui.ts` |
| W03 review | `review_w03_policy` | Read-only; no write paths |
| W04 | `w04_verification` | `tools/verify/**`, `verification/schema/**`, `verification/suites.json`, `tests/verification/**` except the two files reserved below |
| W04 | `w04_manifest_tests` | `tests/verification/product.test.ts`, `tests/verification/schema.test.ts` |
| W06 | `w06_platform` | `src/platform/sdl.ts`, `src/platform/native-libraries.ts`, `src/platform/sdl-render-context.ts`, `src/platform/gl.ts`, `src/platform/gl-programs.ts`, `src/platform/runtime.ts`, `src/platform/index.ts`, `tests/platform/window-native.test.ts`, `tests/platform/window-headless.test.ts`, `tests/platform/window-runner.test.ts`, `tests/platform/native-libraries.test.ts` |
| W06 | `w06_audio` | `src/platform/audio.ts`, `src/platform/vorbis.ts`, `tests/platform/audio.test.ts`, `tests/platform/vorbis.test.ts` |
| W06 | `w06_fonts` | `src/platform/freetype.ts`, `src/platform/freetype-layout.ts`, `tests/platform/freetype.test.ts` |
| W06 | `w06_controllers` | `src/platform/controller.ts`, `tests/platform/controller.test.ts` |
| W08 | `w08_math` | `src/core/math.ts`, `src/core/numeric.ts`, `src/core/qvm-math.ts`, `src/core/renderer-math.ts`, `src/core/common-parse.ts`, `src/core/common-error.ts`, `src/core/binary/**`, `tests/core/math.test.ts`, `tests/core/binary.test.ts`, `tests/core/parse.test.ts` |
| W08 | `w08_commands` | `src/core/commands/**`, `src/core/cvars/**`, `tests/core/commands.test.ts`, `tests/core/cvars.test.ts` |
| W08 | `w08_sessions` | `src/world/session/**`, `src/world/scheduler.ts`, `src/core/diagnostics.ts`, `tests/core/session.test.ts`, `tests/core/scheduler.test.ts` |
| W09 | `w09_archives` | `src/content/archive/**`, `tests/content/catalog/archive.test.ts` |
| W09 | `w09_catalog` | `src/content/catalog/**`, `src/content/mounts/**`, `tests/content/catalog/catalog.test.ts`, `tests/content/catalog/mounts.test.ts` |
| W10 | `w10_q1_maps` | `src/formats/q1-map/**`, `tests/formats/q1-map/**` |
| W11 | `w11_q2_maps` | `src/formats/q2-map/**`, `tests/formats/q2-map/**` |
| W12 | `w12_q3_maps` | `src/formats/q3-map/**`, `tests/formats/q3-map/**` |
| W13 | `w13_q12_models` | `src/formats/q12-model/**`, `tests/formats/q12-model/**` |
| W14 | `w14_q3_models` | `src/formats/q3-model/**`, `tests/formats/q3-model/**`, except the narrow MD5 correction below; original model slice completed, worker now assigned Q2 foundation monsters |
| W14 correction | `w17_scene_models` | `src/formats/q3-model/md5.ts` for `q1Md5AnimationTiming` and its corresponding check in `tests/formats/q3-model/md5.test.ts` |
| W15 | `w15_images` | `src/formats/images/**`, `tests/materials/images.test.ts` |
| W15 | `w15_materials` | `src/materials/**`, `tests/materials/materials.test.ts` |
| W18 | `w18_cpu` | `src/render/cpu/**`, `tests/render/cpu/**` |
| W19 | `w19_gl` | `src/render/gl/**`, `tests/render/gl/**` |
| W16 | `w16_q1_collision` | `src/world/geometry/q1-solid/**`, `src/world/collision/q1/**`, `tests/world/collision/q1/**` |
| W16 | `w16_collision` | `src/world/geometry/**`, `src/world/collision/**`, `src/world/spatial/**`, `tests/world/collision/**`, excluding the Q1 paths above |
| W17 | `w17_scene_world` | `src/render/scene/**` except `models/**` and `particles/**`, `src/render/commands/**`, `tests/render/commands/**` except `models/**` |
| W17 | `w17_scene_models` | `src/render/scene/models/**`, `src/render/scene/particles/**`, `tests/render/commands/models/**` |
| W20 | `w20_audio` | `src/audio/**`, `tests/audio/**` |
| W21 | `w21_media` | `src/media/**`, `tests/media/**` |
| W21 | `w21_text` | `src/text/**` except `truetype.ts`, `tests/text/**` except `truetype.test.ts` |
| W21 | `w13_q12_models`, completed font slice | `src/text/truetype.ts`, `tests/text/truetype.test.ts` |
| W22 | `w22_input` | `src/input/**`, `src/console/**`, `src/settings/**`, `src/capture/**`, `tests/input/**`, `tests/settings/**` |
| W27 | `w27_qc` | `src/compat/qc/**`, `tests/compat/qc/**` |
| W29 | `w29_qvm` | `src/compat/qvm/**`, `tests/compat/qvm/**` |
| W30 | `w30_guest` | `src/guest/core/**`, `tests/guest/core/**` |
| W41 | `w41_actors` | `src/world/actors/**`, `src/world/gameplay/**`, `tests/world/gameplay/**` |
| W08 numeric correction | `w17_scene_models` | `src/core/game-numeric.ts` |
| W24 | `w24_q1_movement` | `src/movement/q1/**`, `tests/movement/q1/**` |
| W25 | `w25_q2_movement` | `src/movement/q2/**`, `tests/movement/q2/**`; movement contract exception recorded above |
| W26 | `w26_q3_movement` | `src/movement/q3/**`, `tests/movement/q3/**` |
| W52 | `w52_network` | `src/network/common/**`, `src/network/services/**`, `tests/network/common/**` |
| W74 | Q1 foundation lane | `src/content/q1/foundation/**`, `tests/gameplay/foundation/q1/**` |
| W74 | Q2 foundation parent lane | `src/content/q2/foundation/**` except `monsters/**` and `weapons/**`; `tests/gameplay/foundation/q2/**` except matching monster and weapon checks |
| W74 | Q3 foundation lane | `src/content/q3/foundation/**`, `tests/gameplay/foundation/q3/**` |
| W74 | `w14_q3_models`, resumed monster assignment | `src/content/q2/foundation/monsters/**` and matching Q2 foundation monster checks |
| W74 | `w74_q2_weapons` | `src/content/q2/foundation/weapons/**` and matching Q2 foundation weapon checks |
| W73 | `w73_bootstrap` | `src/app/bootstrap/**`, `src/main.ts`, `tests/first-playable/**`, `verification/first-playable/**` |
| Lead records | `execution_state` | `docs/work-packages.json`, `docs/dependency-graph.mmd`, `docs/validate-plan.ts`, `docs/test-plan-validator.ts`, `docs/project-plan.md`, `docs/execution-status.md` |

W03 owns every build and package registration. Other workers submit registration requests. Its early build proof compiles real implemented TypeScript tools through the production Bun build pipeline. The production runtime entry arrives in W73. W03 requires no placeholder engine entry and does not wait on W73. Production engine compilation and gameplay remain W73, W66 and W69 obligations.

## Handoffs and acceptance

Each bounded handoff names its source revision, exact changed paths, commands and results, retained evidence, unresolved findings, reviewer and commit disposition. The lead appoints the sole Git writer before staging exact reviewed paths. A progress commit records its bounded result; it does not accept a package automatically.

Only the lead changes a package to accepted after evaluating its acceptance requirements and independent review against the combined tree. An accepted task includes an `acceptanceRecord` with `acceptedBy: "lead"`, full `sourceRevision` and `commit` revisions, and nonempty `evidence` and `review` references. The validator checks the record's shape and requires accepted prerequisites for review and accepted states. Running work may use concrete published inputs while prerequisites remain unfinished; a failed or blocked prerequisite rejects dependent running work. The lead judges actual input readiness and assigns exclusive paths. It does not authenticate the author, inspect those commits, or prove the referenced runtime results. Reopening a prerequisite requires the lead to invalidate dependent active or accepted states and stale evidence before the graph passes again.

| Handoff | Source revision | Evidence and review | Commit disposition |
| --- | --- | --- | --- |
| Initial dispatch of W01/W02/W03/W04/W06 | `6439acd6c5f0d38ca93867ae0f6100ecf4cdbee4` | Work running; package acceptance evidence and independent reviews pending | No implementation commit recorded here |
| Execution records and validator v1.1.0 | Same starting revision plus the scoped working-tree changes | `bun docs/validate-plan.ts`: PASS, 74 tasks/309 edges; `bun docs/test-plan-validator.ts`: PASS, 20 negative cases and synthetic accepted-prerequisite progression; `bun run typecheck`: PASS. `review_dependency_graph` independently passed all 20 negative checks; the lead accepted this bounded execution-state code review. No W01/W02/W03/W04/W06 acceptance follows from it. Full policy check reported three findings in concurrently edited `tools/verify/` files and no findings in these docs scripts; those findings were sent to W03. | Lead review accepted for execution-state code only; included in foundation checkpoint `e291921df44b5778a9dd5189c1f383e5752a8b02`. No Git mutations by this worker |

`w02_references` also resumed inventory of retail binaries in the Steam source supplied by the user. Its existing write boundary remains unchanged.

The synthetic acceptance fixture uses invented revisions and evidence text only inside a temporary fixture directory. It proves validator behavior and accepts no real package. This ledger is a passive file maintained through exclusive ownership; no daemon or shared task service coordinates workers.

## Code-first steering

The user directed code imports first, then live human play and optimization, followed by extended hardening and regression infrastructure. W05 is now running from the existing reviewed `docs/reference-contracts.md` and `verification/source-manifest.json`; W01 exhaustive inventory continues in parallel. The lead removed W01 completion as a W05 prerequisite. No package was marked accepted by this change.

W42–W49 no longer wait for W73. W42, W45 and W49 directly require W74 permanent behavior foundations, with expansions inheriting those foundations through their base packages. W73 remains a required real integration milestone, and W66 retains its bootstrap ownership handoff. W02 initial source cases and actual Q2 references are sufficient code inputs. W03 retains practical build/type guards; W04 retains a basic runner. Broad retail/performance capture, extended hardening, regression infrastructure and final real runtime qualification remain mandatory at W67–W71 and release.

This authorized graph change removes W01 → W05 and eight W73 → W42–W49 edges, then adds W74 → W42/W45/W49: 74 packages and 303 edges. The earlier 309-edge validator and review results above describe the previous snapshot only.

W50 also consumes the existing source census and comparison rather than requiring the unfinished exhaustive feature ledger as an import input. Final feature-ledger reconciliation remains required. `bun docs/validate-plan.ts` passes the revised 74-package/303-edge graph with W01–W06 running and none accepted. The existing 20 negative validator cases and synthetic progression case pass after updating their dependent-package fixture from W05 to W07. No new hardening tests were added.

## Published-input dispatch

The lead now starts source ports against concrete published interfaces while prerequisite packages finish. Acceptance dependency edges still govern review and accepted states. No foundation package is marked accepted merely to dispatch downstream code. W05 math, identity, numeric and time contracts are on disk, with content, scene and execution interfaces being implemented under published names. W02/W03/W06 code and reference inputs are frozen for the next imports. The lead is dispatching W08 common services and W09–W15 raw format ports under exclusive ownership.

Validator v1.2.0 rejects running work only when a direct prerequisite is failed or blocked; it leaves concrete-input readiness to the lead. Review and acceptance still require accepted prerequisites. Existing fixture coverage is updated for this scheduling rule, without adding a readiness framework or new hardening suite.

## Foundation checkpoint and active ports

The lead committed and verified foundation checkpoint `e291921df44b5778a9dd5189c1f383e5752a8b02`. W03 is accepted only for EARLY_TOOLING under its current criteria, using that revision for both source and commit identity. The lead reports passing live typecheck, policy, 32 combined tests and the actual compiled validator; the W03 owner also verified compiled tool relocation. `review_w03_policy` closed its tooling review, and the lead independently executed the artifact. This accepts no production engine runtime or release behavior.

W01, W02, W04, W05 and W06 remain running. W08–W15, W18 and W19 are now running under the exclusive assignments above. W05 content interface work is complete as a bounded handoff; the package remains running while its parent and scene/execution peers finish. W06 platform work resumed for the minimal shader FFI extension in `gl.ts` and `gl-programs.ts`. No other package acceptance is recorded.

## Collision, scene and execution imports

W16, W17, W20, W21, W22, W27, W29, W30 and W41 are running under the assignments above. W03 remains accepted only for its recorded early tooling scope. Several format and component owners have completed bounded implementations; their packages remain running until integration and the lead's explicit acceptance. Acceptance order and final feature/runtime requirements are unchanged.

This checkpoint follows the user's code-first direction with limited checks: update the passive ledger and run the basic live validator. It adds no readiness framework, verification framework or fixture suite.

`w08_math` owns the shared `src/core/common-parse.ts`, `src/core/common-error.ts` and `tests/core/parse.test.ts`. The materials owner migrates its callers and removes its own private copies after those shared modules land. The resumed `w13_q12_models` worker owns only the TrueType files within the W21 text split; W21 remains running.

## Core, content and formats checkpoint

Checkpoint `393833a821230bef2b2420f98b22e56a8f1b840e` records 121 files of core, content and format implementation. The lead reports independent snapshot type and policy checks, 90 passing checks and one explicit MD4 skip, plus 73 root-run tests and snapshot TypeScript checking. The skip is retained as a limit of that checkpoint. These results record completed code slices; they do not accept the full packages or establish live engine gameplay.

W24, W25, W26, W52, W74 and W73 entered running state at this checkpoint. Actual source behavior imports target Q1 rerelease `e1m1`, with 428 authored entities across 41 classes, and Q2 `base1`, with 634 entities across 60 classes. The earlier Q1 count of 460 was incorrect; `src/content/q1/foundation/README.md` records the corrected count. Q2 foundation monsters belonged to the resumed `w14_q3_models` worker, and Q2 weapons to `w74_q2_weapons`. The completed font worker `w13_q12_models` was not assigned weapons: the attempted resume failed at the tool thread limit.

W06 gained GL framebuffer work with an actual smoke run. W03 had a bounded allowlist correction, while its accepted early tooling scope remained distinct from final runtime qualification. W73 had started joining the real components at that checkpoint. The current source and runtime section above supersedes that startup status. Neither the checkpoint nor the later bounded live results accept W73 or W74.

This ledger checkpoint uses only the basic live validator. No additional framework, fixture suite or package acceptance was added.
