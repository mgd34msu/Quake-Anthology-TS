# Execution status

## Installed executable and recent fixes

Installed executable `7e279d2a` includes shared collision filtering and exact binary32 operand decoding. The complete 495-entity native workload measured 71.96 ms per call and 79.90 ms per application step. The original level save now takes 24.70 seconds while executing 52,409,213 guest instructions, versus the earlier 38-second result. Both remain too slow; T10 is open. [Delivery and scope](../.artifacts/resume-20260925/delivery-7e279d2a/RUNTIME-RECEIPT.md).

Source `2a8a50e4` rejects excluded collision candidates before reading and copying their body state. Eligible actors retain fresh state and independent snapshots across all game families. Its complete native workload measured 73.63 ms per call and 81.03 ms per application step, versus 74.19/81.79 ms in the preceding comparison. [Checks and measurements](../.artifacts/resume-20260925/collision-body-admission/ROOT-RECEIPT.md).

Exact binary32 decoding reads the stored sign, exponent and significand directly, avoiding bytewise BigInt assembly. Arithmetic, rounding, exception controls and wider formats retain their original implementations. Root passed 55 existing CPU/floating checks (1035 assertions), strict compilation and scoped policy. [Source and comparison](../.artifacts/resume-20260925/native-binary32-source/ROOT-RECEIPT.md).

Native ABI calls now pack standard integer widths directly instead of shifting each byte through BigInt. Odd aggregate widths and custom register access retain their original behavior. The fresh complete-base1 comparison measured 76.60 to 74.68 ms per native call and 84.40 to 82.13 ms per application step, with all 495 entities retained. Root passed 51 existing checks (1035 assertions), strict compilation and scoped policy. This modest improvement does not close T10. [Receipt](../.artifacts/resume-20260925/native-abi-packing/ROOT-RECEIPT.md).

Checked native scalar memory access now reuses its admitted offset and uses exact numeric bounds for mappings wholly within the safe integer range. High addresses retain the BigInt path. The complete base1 sample measured 73.59 ms per native call and 81.02 ms per application step, down from 79.21/87.41 ms. Root existing memory/CPU/ABI checks passed (55 tests, 1385 assertions), with strict/policy clean. Native performance remains open. [Source and scope](../.artifacts/resume-20260925/native-scalar-lookup/frozen/HANDOFF.md).

Native indirect-call validation now looks up bound callback targets directly instead of materializing the entire callback table. Original target membership, unbind/rebind and executable permission checks remain unchanged. Existing ABI/Windows checks pass (14 tests, 157 assertions), with strict/policy clean. The same full base1 sample measured 79.21 ms per native call and 87.41 ms per application step, versus 82.23/90.28 ms before. This change is included in `07e24a54`; native performance remains open.

Newer source keeps native integer memory operands as exact unsigned words instead of converting them through BigInt. Addresses and complete-range permission checks remain unchanged; writes still commit before observers run. The same full base1 sample measured 82.23 ms per native call and 90.28 ms per application step, compared with 84.59/92.56 ms previously. Root existing memory/CPU checks passed (46 tests, 1297 assertions), with strict compilation and scoped policy clean. This small measured gain does not close T10 performance. [Source and checks](../.artifacts/resume-20260925/native-integer-memory/frozen/HANDOFF.md).

The native interpreter now executes prepared integer operations with exact pairs of 32-bit words, retaining checked memory access and the original path for custom processor implementations. The full base1 sample measured 84.59 ms per native call and 92.56 ms per application step, down from 93.98/102.17 ms. Register aliases, flags, nested calls and fault recovery passed combined existing checks. Native performance remains open. [Implementation and scope](../.artifacts/resume-20260925/native-integer-kernels/frozen/HANDOFF.md).

Prepared native SIMD moves and bitwise operations now share exact byte operations between initial and cached execution. Floating-point arithmetic and MXCSR handling remain unchanged. The full base1 sample measured a 93.98 ms median native call, versus 97.34 ms before this change; it still exceeds the original 25 ms interval. [Source and bounded checks](../.artifacts/resume-20260925/native-raw-simd/frozen/HANDOFF.md).

Native entity publication now reads fresh field subsets and reuses each decoded state. Public reads retain read-only mapping support and exact source values. The same full base1 workload reduced the complete application-step median from 117.77 to 105.36 ms; the original native call itself stayed about 97 ms. Root strict/policy and the existing source-services check pass. [Implementation and scope](../.artifacts/resume-20260925/native-field-reads/frozen/HANDOFF.md).

Further native work removes intermediate copies for checked, contiguous memory operations and retains exact negative hook lookups across unchanged callback registrations. The same full base1 workload measured 97.61 ms per native frame (last-ten median), down from the preceding 108.26 ms. Overlapping writes, permissions, observers and source instruction execution remain intact. Native performance remains open. [Measurement and source identity](../.artifacts/resume-20260925/native-bulk-memory/RECEIPT.md).

Newer source caches decoded native instruction operations and unhooked straight-line blocks. An isolated original retail base1 comparison with all 495 entities reduced the last ten native frame calls from a 120.31 ms median to 108.26 ms, about 10%. Live instruction bytes, callback changes, faults and nested execution remain checked. Root strict/policy and 48 existing checks with 400 assertions pass. Native performance remains open: this workload still exceeds its 25 ms interval. [Change and bounded measurements](../.artifacts/resume-20260925/native-semantic-blocks/frozen/HANDOFF.md).

Installed executable `7e279d2a` also includes the earlier native memory, callback lookup, ABI, instruction, SIMD, player-dimension and objective work. All 2883 archived Git blobs match the committed tree. Build/help and the compiled original retail native GL startup, movement/attack input and normal quit check passed; captured frames were inspected. Previous `07e24a54` is preserved locally. [Delivery receipt](../.artifacts/resume-20260925/delivery-7e279d2a/RUNTIME-RECEIPT.md).

**22/23 targets are accepted in source. Native execution performance is the remaining identified T10 implementation work.** The measured original rerelease save is about 25 seconds, down from 62 seconds before the shared execution changes; that remains slow. The player-dimensions integration passed combined strict/policy and 51 focused checks with 496 assertions, including original retail/Threewave calls. Unified peers require `qts:snapshot-v10`. This does not establish complete campaigns, physical audio quality, arbitrary-artifact compatibility or general rendered FPS.

## September 25 implementation checkpoints

These entries retain their source, measurement and installed-build status at the time. The current installed build is identified above.

Installed executable `807e7c0b` includes T12 closure, live primary QVM item catalogs, source-owned player eye/stance/movement modes, shared teams/scores/objectives, and native decode/memory improvements. All 2876 archived Git blobs match the committed tree. Build/help and the existing compiled original Threewave GL movement/fire/armor/save-load-save/quit check passed; played/restored frames and original/canonical saved state were inspected. Previous `d3814f1c` is preserved locally. **Current source: 22/23 targets accepted; T10 remains open.** Newer source supports indirect objective fields and reuses a decode cursor within each native CPU invocation. Authored body dimensions are integrated in newer source; native execution performance remains active. [Delivery receipt](../.artifacts/resume-20260925/delivery-807e7c0b/RUNTIME-RECEIPT.md).

The latest objective changes follow original QC entity fields, QVM pointer walks and native records through relocation and restoration. Shared match consumers retain the original owner and callbacks. Native execution now reuses a decode cursor within each invocation, including distinct cursors for nested calls. The original rerelease save measured 38.0 seconds, down from 62.1 before the execution improvements. This is still slow; the measured original native frame took 351 ms. These source changes are newer than installed `807e7c0b` and do not close T10 performance.

Authored player dimensions now reach selected collision, stepping and ground probes. Blocked enlargement keeps the accepted hull; disable restores source dimensions through clearance. Original API3/API2023 and declared QVM movement retain their source trace policies. Unified peers require `qts:snapshot-v10`. Root combined strict, 34-file policy and 51 focused checks with 496 assertions pass, including actual original native and Threewave movement calls. This is newer source than installed `807e7c0b`. Native execution performance is the remaining identified T10 implementation work.

The combined source passed production/changed-fixture strict compilation and scoped policy checks. Affected original QC, QVM and native calls passed 48 tests/645 assertions; the subsequent native hook lifecycle passed three existing checks/81 assertions. Newer source extends original QC damage regions to exact amount transformations applied at hit time, including selected Q1/Q2/Q3 weapons and offhand grenades. Root combined strict, 15-file policy and the existing QC/Q2 fixtures pass (43 tests/556 assertions). This amount-transform change is newer than installed `edcfada8`. This remains separate from broader mod composition and whole-target acceptance.

Current source also admits declared global QC damage inputs and original spawn, spawn-selection and objective calls with their actual arguments/globals. Initial player admission follows the declared spawn. Region metadata is validated once and scoped values restore after success or failure. Combined strict, eight-file policy and the two existing QC fixtures pass (20 tests/318 assertions). Installed `edcfada8` predates these changes. Newer source completes offhand source damage/cadence and fixes retained-map native pickups holding a retired world wrapper. The real rerelease roundtrip passes 27 assertions; original Threewave cadence passes 56 and original Xatrix offhand Application checks pass 8. Combined strict/policy and 26 checks with 368 assertions pass. T12 is accepted in source. Original rerelease save/load remains slow (about 83 seconds per level write and 51 seconds reading it); this is explicit remaining T10 execution work. Native component graphs also reach mixed local/remote views with source palettes and owner retirement. The short README now links to focused setup, content-layout, playing and console guides.

Private QVM component combat is integrated in newer source. Shared calls now use declared argument roles, flags, mass and team values; transformed calls retain original private words and source pointers. Existing stock declarations retain their saved identity. Combined strict, eight-file policy and 40 existing checks with 470 assertions pass. Two T10 implementation parts remain: authored player body dimensions and native execution performance.

Shared native execution now avoids redundant per-instruction callback mapping checks, reuses validated fetch ranges and batches arithmetic flags. Narrow integer ALU operations remain exact through 32 bits; 64-bit operations retain BigInt. Existing CPU/memory/ABI checks and 1,451,592 comparisons against the original arithmetic agree. The measured original rerelease level write improved from 62.1 to 54.8 seconds while still executing approximately 52.4 million guest instructions. This remains slow and does not close the performance work or establish rendered FPS.

Newer source caches decoded x64 operands only while the original executable bytes still match. Each instruction retains live register and memory operands; alias writes, changed permissions and remapping invalidate cached decoding. The original rerelease level write measured 48.7 seconds, down from 62.1 seconds before the shared execution changes. Existing CPU/memory/ABI checks and direct live-code mutation checks pass. This decode change is newer than installed `d3814f1c`; native loading performance remains open.

Runtime-generated primary QVM catalogs now read original live item records and private inventory storage. Original initialization and restoration retain ownership; shared selection, pickups, navigation and HUD consume the same catalog. Targeted write tracking invalidates changed records without repeating initialization. Root strict/policy, nine existing checks (77 assertions), and the actual original Threewave Application workflow (29 assertions) pass. This source is newer than installed `d3814f1c`.

Declared QC/QVM/native client outputs now control player eye offset, movement mode and supported source stance through the selected movement owner. Camera, first-person attachments, listener and unified prediction consume the same publication; disable and restore retain source ownership. Unified peers now require `qts:snapshot-v9`. Root strict/policy and 41 existing checks with 360 assertions pass. Arbitrary authored body dimensions remain in progress, so this does not close the T10 player-output part. This source is newer than installed `d3814f1c`.

Native memory now retains a bounded set of recently used mappings and their exact endpoints. Protection changes and unmapping retire cached entries; aliases, observers and cross-range faults remain intact. The actual original rerelease level write measured 46.0 seconds versus 49.7 on the same baseline. The frozen source passes strict/policy and 58 existing checks (817 assertions, three separately gated skips); root independently passed memory/x64 checks (39 tests/695 assertions). Native execution performance remains open.

Shared match services now borrow original QC/QVM/native scores, team membership and named objective state. Mutations preserve original commands, callbacks, refusal, ownership and save/disable lifetime. Campaign gates and bot goals consume declared channels. Root combined strict/policy, the original Copper workflow (31 assertions), and existing combat checks pass. Private artifact layouts still require declarations; this closes the component match-service gap.

Cached x64 instruction decoding now allocates byte/operand storage only on cold decoding and indexes retained operands by their bounded byte position. Each invocation keeps its own cursor and live-code guard. The actual original rerelease level write measured 42.7 seconds versus 46.0 seconds before this change. Strict/policy and existing CPU/nested-call checks pass; root independently reran the x64 fixture. A further checked execution-loop change avoids unused instruction-pointer arithmetic and repeated transient-result allocation; the actual original level write now measures 39.3 seconds. Public pointers remain immutable and all loads/stores retain memory admission. Root CPU/memory/ABI checks pass (49 tests/753 assertions). These loop changes are newer than installed `807e7c0b`. Loading performance remains open.

Previous implementation and delivery checkpoints follow.

Installed executable `745543fb` includes declared QuakeC weapon/client/combat interfaces, complete modern QVM primary server/client declarations, complete Classic/rerelease native primary/world declarations, native inline armor operations, and selected Q3 respawn correction. All 2855 archived source blobs match. Build/help and the existing compiled original Threewave GL movement/fire/armor/save-load-save/quit check passed; three frames and source/canonical saved state were inspected. Separate original-source checks cover the new declarations. The previous executable is preserved locally. **21/23 targets accepted; T10/T12 remain open.** [Delivery receipt](../.artifacts/resume-20260925/delivery-745543fb/RUNTIME-RECEIPT.md).

Newer source admits complete original primary declarations for the legacy 1.16n QVM ABI. Input and equipment use its original command layout; shared updates retain private button bits and counters. Combat reads original team slots and writes only declared health/armor stats. Fixed-pose view height no longer overwrites damage events. Root combined production/changed-fixture strict and focused codec plus original InstaGib input/equipment checks pass (5 tests/73 assertions); the borrowed player-state checks passed separately (4/85). Early cgame rendering remains unverified. Installed executable stays `745543fb`; **21/23 accepted, T10/T12 open**. [Source boundary](../.artifacts/resume-20260925/qvm-legacy-primary/frozen/HANDOFF.md).

Declared QC attacker-scaling regions now supply selected Q1/Q2/Q3 weapons, including modifiers active without Quad. Queries execute the original region and restore private scratch state; attack ownership prevents a second application while original target defenses remain active. Root combined strict and existing original QC checks pass (17 tests/215 assertions); frozen 13-file policy is clean. The boundary supports qualified linear scaling, not nonlinear damage transformations. Installed executable stays `745543fb`; **21/23 accepted, T10/T12 open**. [Source boundary](../.artifacts/resume-20260925/qc-damage-scale/frozen/HANDOFF.md).

Earlier implementation and delivery checkpoints follow.

Primary QuakeC combat now consumes artifact-bound original damage/armor declarations using the same source-call validation and argument lowering as components. Additional source arguments are explicit; persistent source stores remain committed, and saves require matching declarations. Original Copper primary damage, borrowed armor, save/load and changed-declaration rejection pass (1 focused existing test/10 assertions). Frozen six-file policy and root combined production/changed-fixture strict pass. Installed executable remains `1f293c76`; **21/23 accepted, T10/T12 open**. [Unit evidence](../.artifacts/resume-20260925/qc-primary-combat/frozen/HANDOFF.md).

Standalone native protection donors now execute declared original inline regions with their source frame and machine inputs. Source debit/effects remain authoritative; typed retirement unwinds the owning invocation without inventing a guest return value. Root combined production/changed-fixture strict and the actual retail donor plus existing ABI checks pass (9 tests/64 assertions); frozen 13-file policy is clean. Item action/icon integration is preserved. Installed executable remains `1f293c76`; **21/23 accepted, T10/T12 open**. [Source and integration evidence](../.artifacts/resume-20260925/native-inline-protection/HANDOFF.md).

Primary QuakeC weapon/client stages now accept artifact-qualified declarations through the same qualifier used by components. Original spawn, typed spawn selection and explicit objective calls retain source ownership; saved declarations must match on restore. Actual original id1/QW checks pass (2 tests/85 assertions), frozen nine-file policy is clean, and root combined production/changed-fixture strict passes. Installed executable remains `1f293c76`; **21/23 accepted, T10/T12 open**. [Unit evidence](../.artifacts/resume-20260925/qc-primary-weapon-stage/frozen/HANDOFF.md).

Installed executable `1f293c76` includes native inventory/drop/HUD and equipment integration, composed mod item actions/icons, declared original QuakeC pickup callers, and checked native memory-access allocation reductions. All 2836 archived source blobs match. Build/help and the existing compiled original Threewave GL movement/fire/armor/save-load-save/quit check passed; three frames and source/canonical saved state were inspected. The previous executable is preserved locally. Broader mod interface declarations and standalone native inline protection donors remain active. **21/23 targets are accepted; T10/T12 remain open.** [Delivery receipt](../.artifacts/resume-20260925/delivery-1f293c76/RUNTIME-RECEIPT.md).

Earlier checkpoints below retain their source and installation status at the time.

Primary QuakeC compatibility metadata now declares additional original pickup callers with exact artifact/function/instruction qualification. Existing shared admission preserves original player guards, targets, coop retention and respawn. Combined production/changed-fixture strict and actual id1/QuakeWorld pickup checks pass: 9 tests/268 assertions, including new original key and powerup caller paths. The frozen six-file policy is clean. T10/T12 remain open; installed executable remains `ab3e3adf`. [Source and declaration limits](../.artifacts/resume-20260925/qc-pickup-callers/HANDOFF.md).

Declared QC/QVM/native mod items now use their original use/drop functions through ordinary controls, including original QVM commands and native inventories that retain the world's weapons. Original item guards, names, icons and source counters remain intact. Cursor restoration follows mod admission; name collisions respect explicit IDs and the current original-item owner. Unified peers negotiate `qts:snapshot-v7` with frame version 9; captured versions 2–8 remain readable. Combined production/changed-fixture strict and scoped integration policy pass, alongside focused existing item/HUD/binding/frame checks and an actual Classic native component-only command check. T10/T12 remain open; installed executable remains `ab3e3adf`. [Integration evidence](../.artifacts/resume-20260925/item-actions-integration/EVIDENCE.md).

Native module scalar memory access now reuses checked mapping views while preserving permissions, alias writes and synchronous observers. The same bounded original rerelease Init/Spawn prefix measured 5.537 seconds before and 5.091 seconds after, with matching instruction count and CPU state. Existing 56 checks/780 assertions, strict compilation and scoped policy pass. Full retained-travel performance remains unresolved; no FPS improvement is claimed. Installed executable remains `ab3e3adf`. [Source and measurements](../.artifacts/resume-20260925/native-load-cost/HANDOFF.md).

Selected Q3/Team Arena equipment now has original native/QVM player services, scoped QVM movement, native input-edge handling and saved pending item use. Original health/spawn/objective routines remain authoritative, and Guard points initialize through the destination's armor binding. Combined production strict and 32 existing checks with 310 assertions pass; frozen source policy covers 19 production files. Separate actual checks cover both native DLLs, Threewave and id1 QC. Inventory-menu and component item actions continue. Installed executable remains `ab3e3adf`; **21/23 accepted, T10/T12 open**. [Equipment integration](../.artifacts/resume-20260925/equipment-integration/EVIDENCE.md).

Current source connects selected arsenals to original native Q2 inventory navigation, use and drops. Original command guards, quantity decisions and allocation cleanup remain source-owned. Exact dropped item identity survives saves, and selected HUD icons/names use the actual content. Classic and rerelease retain separate layouts and source timing. Combined strict/policy and the existing cursor/HUD/cargo checks pass. Actual original DLL drop acceptance/refusal and Classic save/re-pickup passed separately; rerelease return-map cargo remains unverified after a timeout. Selected equipment and general component item actions continue. Installed executable remains `ab3e3adf`; **21/23 accepted, T10/T12 open**. [Integration evidence](../.artifacts/resume-20260925/native-inventory-integration/EVIDENCE.md).

Original Classic and rerelease Q2 Pmove now consume selected equipment speed and fixed-pose effects through their existing movement boundary. Neutral nested movement cannot inherit another call's speed multiplier. The two existing original-DLL movement checks pass37 assertions; inventory and remaining equipment ownership work continue. Installed executable remains `ab3e3adf`; **21/23 accepted, T10/T12 open**.

Newer source applies selected equipment protection to original damage calls and restores original rerelease compound-pickup power-armor behavior. Temporary ammo calculations are excluded from protection publication; committed grants and original damage retain their observers. Existing authority/composition checks pass28/239; the original retail/Xatrix pickup witness passed11 assertions, and the final pickup source passed strict/policy. Native inventory, drops and equipment movement remain active; **21/23 accepted, T10/T12 open**. Installed executable remains `ab3e3adf`.

Installed executable `ab3e3adf` includes qualified native selected arsenals, native component items/cameras/HUD transport, and authored held models. Exact archived source, build/help and compiled original Threewave GL movement, firing, armor and save/load passed. Native lifecycle and two-DLL travel have separate source checks. The previous executable is preserved locally. **21/23 targets remain accepted; T10 and T12 are open.** Native inventory navigation/display, rerelease HUD drawing and broader artifact/composition coverage remain active. [Delivery receipt](../.artifacts/resume-20260925/delivery-ab3e3adf/RUNTIME-RECEIPT.md).

Newer source implements rerelease localized HUD layouts, score tables, health bars, inventory drawing and source timing while retaining Classic behavior. Unified remote players also receive public held-weapon metadata without other players' first-person poses. Combined production strict and the existing HUD/held/frame checks pass. No new test framework was added. Native inventory selection and wider composition remain active; **21/23 accepted, T10/T12 open**. [Integration evidence](../.artifacts/resume-20260925/native-presentation-integration/EVIDENCE.md).

Earlier checkpoints below record their source and installed-build status at the time.

Qualified original Xatrix and retail rerelease worlds now use selected Q1/Q2/Q3 arsenals through the existing factories. Original movement, weapon eligibility/modifiers, pickup acceptance, cheats, drops and respawn retain source ownership. Selected state survives save/read and retained native DLL map travel; old hooks retire before rebind and original travel writes use asynchronous loading continuations. Native ammo/icon HUD retains source layout, health and armor. Root combined strict and 26-file policy pass; existing HUD checks pass five tests/25 assertions. Actual source lifecycle/factory/pickup evidence and final two-DLL retained travel are recorded separately. Native inventory cursor/display and broader artifact coverage remain unfinished. Installed executable stays `434a4d3e`; **21/23 accepted, T10/T12 open**. [Native primary integration](../.artifacts/resume-20260925/native-primary-integration/EVIDENCE.md).

Native component HUDs now reach remote players through the existing reliable component stream, preserving source configstrings, layout, inventory, per-frame stats, camera and recipient identity. The remote uses one owner for HUD/camera and retires stale source generations. Root combined strict and eight-file policy pass; the original Xatrix help/inventory/save/retirement witness plus existing codec checks pass seven tests with 73 assertions. No native module runs on the remote client. Missing rerelease drawing commands and native-primary inventory UI remain active. Installed executable stays `434a4d3e`; **21/23 accepted, T10/T12 open**. [HUD transport integration](../.artifacts/resume-20260925/native-hud-integration/EVIDENCE.md).

Source-held weapon declarations now work through QC/QVM/native item metadata, saves and unified presentation. Native Q2 carrier attachment uses original animated MD2 landmarks or skeleton joints, with the same LOD/shadow model choice as rendering. Root merged the camera no-world policy with the actual resolved held entity. Combined strict passes; seven focused existing checks pass 117 assertions (two opt-in graphical checks skipped). Native-primary publication/lifecycle and its complete inventory UI remain active. Installed executable stays `434a4d3e`; **21/23 targets accepted, T10/T12 open**. [Held integration](../.artifacts/resume-20260925/held-integration/EVIDENCE.md).

Native component cameras now consume original API3/API2023 end-frame output locally and through authenticated remote frames. Selected movement prediction, source view locks, kick, FOV, blends and weapon visibility retain their owners. Classic does not receive rerelease damage blend. Source no-world rendering skips BSP/fog/shadows while preserving authored model/effect submissions. Root combined strict and 15-file policy pass; actual native camera callbacks, six codec checks and the focused world-order check pass. The broader existing Q1 dynamic-light test fails identically on main before this change; it is recorded separately. Native HUD transport and primary arsenal integration remain active. Installed executable stays `434a4d3e`; **21/23 accepted, T10/T12 open**. [Camera integration](../.artifacts/resume-20260925/native-camera-integration/EVIDENCE.md).

Native component items and weapons now join the shared inventory and weapon selection. Original API3/API2023 code retains firing, buffered attacks, ammo, refusal, pending switches and saves. Shared capacity fields publish changes for every affected item. Retirement during an original attack closes ownership before guest cleanup. Root combined production strict and scoped policy pass; original Xatrix has 33 focused assertions, q2eaks has 35 including its queued attack, and the combined shared-capacity check has 11. Full native-primary arsenals, camera integration and remaining artifact/composition coverage remain active. Installed executable stays `434a4d3e`; **21/23 targets remain accepted, T10/T12 open**. [Native item integration](../.artifacts/resume-20260925/native-items-integration/EVIDENCE.md).

Newer source transfers original component body materials to selected geometry and connects original Xatrix/retail Q2 pickup supply. Source ownership tests and per-pool ammo limits run through the original DLL; temporary source counters are restored before selected grants and map effects. Root combined production strict and five-file pickup policy are clean; both native source checks pass 24 assertions, and the combined original Threewave body witness passes 9 checks / 138 assertions. Body policy passed on its 18 production files. The complete native-primary arsenal join and native component item/camera work remain active. Installed executable stays `434a4d3e`; **21/23 targets remain accepted, T10/T12 open**. [Pickup/body evidence](../.artifacts/resume-20260925/native-primary-pickups/EVIDENCE.md).

Installed executable `434a4d3e` includes selected arsenals in the qualified original Threewave game and native component world drawings. All 2,799 archived Git blobs match; build/help and compiled original Threewave GL movement, firing, armor, save/load and normal quit passed. The restored frame and decoded original/canonical save values were inspected; all owned processes exited. Prior `55c82c88` is preserved. **21/23 targets remain accepted; T10/T12 stay open.** [Delivery receipt](../.artifacts/resume-20260925/delivery-434a4d3/RUNTIME-RECEIPT.md).

The following entries record their source and installed-build status at the time.

Newer source integrates selected Q1/Q2/Q3 arsenals into the qualified original Threewave primary game and native rerelease component world drawings. Original weapon decisions, source modifiers, pickup/drop lifecycle, held attachments and HUD visibility retain their owners. Root's exact combined candidate passed full production/changed-test strict compilation, 32-file policy, 39 focused checks with 441 assertions, and the actual Q2 Application lifecycle/HUD check with 37 assertions. This is source progress beyond installed `55c82c88`; **21/23 targets remain accepted, with T10/T12 open** for remaining native adapters, component body overlays and original artifact/composition coverage. [Combined evidence](../.artifacts/resume-20260925/arsenal-drawings-integration/EVIDENCE.md).

Installed executable `55c82c88` contains the accepted component client save/cinematic/remote joins, engine command ownership, original shader handling and both Classic Q2 corrections. All 2,793 archived Git blobs match; build/help and compiled original Threewave GL movement, firing, armor, save/load and normal quit passed. The restored frame and source/canonical save values were inspected, and all owned processes exited. Previous `0bc6e85` is preserved. **21/23 targets remain accepted; T10 and T12 are open.** [Delivery receipt](../.artifacts/resume-20260924/delivery-55c82c8/RUNTIME-RECEIPT.md).

The following entries record their source and installed-build status at the time.

Newer source joins exact original component client checkpoints to Application saves and active fullscreen component movies. Unified remote viewers run their own original cgame from admitted source publications. Component configs retain their source cvars/files while engine binding/input commands reach the real input handler. Root's combined candidate passes full production strict, 46-file policy, 25 local checks with 207 assertions and the original-mod two-channel check with 41 assertions. A full foreground remote multiplayer playthrough remains unverified. Native component item adapters and remaining original-primary arsenal integration are still required. **21/23 targets remain accepted; T10/T12 stay open.** Installed executable remains `0bc6e85`. [Combined integration evidence](../.artifacts/resume-20260924/component-runtime-integration/EVIDENCE.md).

Source commit `80ff26fe` also rejects Quake II's rerelease-only `target_poi` under the Classic entity provider, independently of selected movement or character. Both edition cases pass. This was an admission mismatch found during the Classic fidelity audit, not another observed visible feedback leak.

The following entries record their source and installed-build status at the time.

Source commit `801ce92b` preserves original component client memory, resource handles, fonts, scripts, source publication history and clocks. Root's exact combined candidate passes full production strict, 13-file policy, eight continuation checks and six shared renderer checks. Actual Threewave's next frame matches all 8,388,608 VM bytes after save encoding/restoration without client initialization replay; real generated font atlases also match. Restoring an unpublished client cannot release active provider audio or alter the native world's temporary hulls. Application saved-client rows and active system cinematic transitions were still in progress at this checkpoint. [Integration evidence](../.artifacts/resume-20260924/client-checkpoint-integration/EVIDENCE.md).

Installed executable `0bc6e85` includes the classic Q2 feedback correction, retained original mod shaders, exact local client retirement and cinematic decoder/audio checkpoints. All archived source blobs match the commit. Build/help and compiled original Threewave GL movement, firing, armor, save/load and normal quit passed; the restored frame and original/canonical save state were inspected. Full component client/cinematic saved-game admission, remote component presentation and original QVM/native primary selected arsenals remain open. **21/23 targets remain accepted; T10 and T12 are open.** [Delivery receipt](../.artifacts/resume-20260924/delivery-0bc6e85/RUNTIME-RECEIPT.md).

The following entries record their source and installed-build status at the time.

Installed executable `b980fa3` removes the unintended directional damage marker from classic Quake II while preserving its original pain, screen flash and view feedback. The separate rerelease damage indicator remains supported. Exact committed source passes all-production strict compilation and 43 focused checks with 311 assertions; compiled help also passes. This delivery has no new graphical playthrough. **21/23 targets remain accepted; T10 and T12 are open.** [Correction and verification](../.artifacts/resume-20260924/classic-feedback/EVIDENCE.md), [installed binary receipt](../.artifacts/resume-20260924/classic-feedback/installation.json).

Newer source integrates retained source shader remaps and exact local player/client retirement. Foreign shader images render over original Q1 surfaces while their lightmaps remain intact; source/consumer retirement cancels pending work and preserves surviving accepted cues. Root combined strict and 21-file policy checks pass, alongside 68 focused checks, seven original-cgame/order checks and an actual graphical Application retirement check. Component cinematic/client continuation and remote presentation remain unfinished. [Integration evidence](../.artifacts/resume-20260924/presentation-integration/EVIDENCE.md).

Newer source also captures exact cinematic decoder state, numeric movie handles and queued owned PCM. Original RoQ/OGV continuation matches subsequent uninterrupted pixels and audio. Root strict compilation and 24 media checks with 345 assertions pass; three existing optional checks are skipped. Component client/transition saved-game admission remains unfinished. [Continuation evidence](../.artifacts/resume-20260924/cinematic-integration/EVIDENCE.md).

Earlier checkpoints below record their source and installed-build status at the time.

Installed executable `0dc86b6` includes the accepted source changes through original mod client services and QuakeWorld arsenals. Build/help, exact archived source identity, and compiled original Threewave GL movement, firing, armor, save/load and normal quit passed. The restored frame and original/canonical save state were inspected. **21/23 targets are accepted; T10 and T12 remain active.** Shader remapping, component cinematic and remote presentation, player retirement, and original QVM/native primary selected arsenals remain unfinished. [Delivery receipt](../.artifacts/resume-20260923/delivery-0dc86b6/RUNTIME-RECEIPT.md).

Combined source now integrates original QVM HUD/initialization, source command and writable-file routing, retained local music, safe cinematic activation/retirement, and selected arsenals in original QuakeWorld. The exact candidate over `fcb848d` passes all-production strict compilation, 28 combined checks with 212 assertions, and the actual cinematic Application check with six assertions. Original QW command/fire/pickup/travel has separate frozen evidence. T10/T12 remain open, 21/23 accepted; installed executable remains `67f2cfd`. [Combined evidence](../.artifacts/resume-20260923/client-services-integration/EVIDENCE.md).

Original QVM component weapons now use their declared source input, item stores and deferred switching through the shared inventory and weapon slot. Original cgame view weapons and entity-frame expiry/free/unlink/pause clocks are integrated. Root merged over `2a51e48`: all-production strict compilation passes, and the actual Threewave Application witness passes 270 assertions in 28.67 seconds. Graphical seat retirement, full component HUD/initialization and remaining artifact/weapon qualifications stay open. Installed executable remains `67f2cfd`; total 21/23. [Evidence](../.artifacts/resume-20260923/qvm-items-integration/EVIDENCE.md).

Newer source admits selected Q1/Q2 arsenals on qualified original id1, including source pickups, single powerup application, saves, travel and original respawn. Root integrated over `a56d4f0`: strict compilation passes, ten production files have zero policy findings, actual MG3 Application passes 33 assertions and mixed source/component restore passes 15. Frozen worker receipts cover five actual arsenal combinations. QuakeWorld/artifact and original QVM/native-primary support remain unfinished. Installed executable remains `67f2cfd`; total 21/23. [Evidence](../.artifacts/resume-20260923/qc-arsenal-integration/EVIDENCE.md).

Newer source integrates explicit component HUD/camera ownership, native original end-frame stats/layouts and captured QuakeC camera identities. Root's isolated candidate over `0fdebe4` passes all production/changed-fixture strict checks and 22 focused checks with 191 assertions in 11.30 seconds. Actual Xatrix HUD media renders over Q1; original id1 camera save/load and self-release pass. QVM component 2D output, remote transport and full native end-frame view qualification remain open. Installed executable is still `67f2cfd`; the target count remains 21/23. [Evidence](../.artifacts/resume-20260923/hud-view-integration/EVIDENCE.md).

Installed `67f2cfd` includes component item/weapon ownership, original component cgame scenes, persistent presentation retirement and selected Q3/Team Arena arsenals on original id1. All 2,769 archived blobs and all 1,975 production TypeScript files match the qualified source. Build/help and compiled original Threewave GL movement/fire/armor/save-load-save/quit passed; the restored frame was inspected and owned processes exited. Binary SHA-256 `fcf3afc1ed859b8acc403f5281c67a528555d8e897a3b08ea04220abfe463d32`; manifest SHA-256 `168c5b34d258dba0ff6b2c29ba414e59ef9f947fc14a83ded21c70c36f36fdfc`. Previous `698166c` binary/manifest are preserved. **21/23 targets remain accepted; T10 and T12 are active.** Entries below retain their historical source/installation status. [Delivery receipt](../.artifacts/resume-20260923/delivery-67f2cfd/RUNTIME-RECEIPT.md).

Original id1 now runs selected Q3/Team Arena weapons while retaining original movement/world callbacks, pickup lifetimes, backpack cargo, armor/health/reactions, equipment, travel and cooperative respawn. Borrowed projectile references preserve canonical actor identity and saved source row state. Completed outgoing powerup provenance prevents a second original Quad multiplier. Root's exact candidate over `e1e123a` passed production/fixture strict and the public Application witness: one test, 51 assertions, 42.94 seconds. Selected Q1/Q2 on original QC remain explicitly rejected until their actual source joins are implemented. QW/additional QC artifacts and QVM/native primary combinations remain open. Installed executable remains `698166c`; total 21/23. [Evidence](../.artifacts/resume-20260923/qc-primary-integration/EVIDENCE.md).

Newer source gives persistent component output an activation owner. Disable/re-enable, saved ownership, delayed static/media loading, recipient overrides, fog and audio retirement are integrated. The exact candidate over `c4d426b` passed root production/changed-fixture strict and 25 combined checks with 235 assertions in 3.13 seconds; the frozen worker files passed 23-file policy. Unified snapshot negotiation advances to version 5 while captured versions 2–5 decode. Ambiguous legacy primary/component persistence reports an explicit migration limit. Source HUD/view and remaining QVM media services are unfinished. Installed executable remains `698166c`; 21/23 targets are accepted. [Integration evidence](../.artifacts/resume-20260923/component-ownership-integration/EVIDENCE.md).

Newer source admits component-owned items and multiple weapon sources through the existing inventory, weapon slot and UI. The QuakeC adapter uses declared original dispatchers, selection and continuation boundaries. Root's isolated candidate over `754262b` passed production/changed-fixture strict compilation and 15 checks with 211 assertions in 36.41 seconds. Actual id1/Copper code and a public Q2 Application cover firing, source media, pending-switch save/load and hidden primary inventory. QVM/native item adapters and full original-primary QC arsenal admission remain separate work. Installed executable remains `698166c`; 21/23 targets are accepted. [Integration evidence](../.artifacts/resume-20260923/source-items-integration/EVIDENCE.md).

Newer source connects declared original QVM component snapshots to each seat’s cgame. The original scene owns entity and player events, looping sounds and effects without duplicate raw event delivery. Root’s isolated candidate passed full production/changed-fixture strict compilation, 11-file policy and eight checks with 88 assertions in 22.56 seconds. Original Threewave speaker/teleport behavior and public Application regeneration/save-load are covered. HUD/camera, persistent presentation and foreign-character overlay fidelity remain open. Installed executable remains `698166c`; T10/T12 remain open, with 21/23 targets accepted. [Integration evidence](../.artifacts/resume-20260922/component-presentation-next/integration/EVIDENCE.md).

Installed `698166c` adds expansion arsenal selection, base-Q3/Team Arena equipment and input ownership, independent expansion ammo timers, and compound original pickup operations. Archive2757blobs match, all1969 production TypeScript files match the strict-compiled source candidate, and build/help plus compiled original Threewave GL movement/fire/armor/save-load-save/quit passed. Binary SHA-256 `f29eccd86cf96d83d74fc905a9d5d738688e415700b9ef709aaa1287a0bfed1d`; manifest SHA-256 `b2fb78efa8016cb3ddc37b801365782e71522ac7d9dbc38538278ea16e692dd3`. Previous f96d9a9 binary/manifest preserved. [Delivery evidence](../.artifacts/resume-20260922/delivery-698166c/RUNTIME-RECEIPT.md). **21/23 targets remain accepted; T10 and T12 are active.** Earlier checkpoint entries below retain their historical source and installation status.

Newer source exposes supported expansion arsenals in Custom Game, keeps base-Q3/Team Arena equipment and weapon input with their actual owners, and gives every selected expansion ammo pool an independent original-rule regeneration counter. The isolated combined candidate passed production/changed-fixture strict,22-file policy and six actual menu/Application checks with117assertions, including both same-family directions and all12 rerelease ammo pools with source reset/save-load. These changes are included in installed `698166c`. Original QC/QVM/native primary selected arsenals remain open under T12. [Combined source evidence](../.artifacts/resume-20260922/t12-team-arena/integration-5/EVIDENCE.md).

Newer source adds compound original pickup writes across QC/QVM/native adapters, exact nested QVM store-effect scope, and qualified Xatrix Bandolier/Pack map callers. The isolated candidate passed production/changed-fixture strict,11 focused checks/84 assertions and actual original hangar2 pickup/save-load1/8. Retail donor count/capacity behavior is separately qualified; its primary autoshield consumer remains unfinished. These changes are included in installed `698166c`. [Integration evidence](../.artifacts/resume-20260922/mod-pickup/compound/integration/EVIDENCE.md).

Installed `f96d9a9` includes original QVM client-frame callbacks, base-Q3 equipment and legacy projectile saves, persistent Team Arena powerups, and original damage/cadence effects on selected Q1/Q2 weapons. All2753 archived blobs match the commit; production strict, build/help and compiled original Threewave GL movement/fire/armor/save-load-save/quit passed. Binary SHA-256 `71b61891f0b58d4c79a7a6d8d402b062e33dd65ea38683ab5bd6e777416b3982`; manifest SHA-256 `bd03f7dbe65414a2c3ebbd39d81e386357f567625d0c9793c6d72d3c17106b1b`. Previous e7a9167 binary and manifest are preserved. [Runtime and installation evidence](../.artifacts/resume-20260922/delivery-f96d9a9/RUNTIME-RECEIPT.md). Current total: **21/23; T10 and T12 remain open**. Same-family arsenal ownership, expansion ammo regeneration and general compound pickup work are active.

The entries below record earlier source and delivery checkpoints; their historical installed versions and remaining-work statements do not override the current status above.

Original Team Arena damage and firing cadence now reach selected Q1/Q2 source weapons, including expansion leaves. Quad/Doubler and Scout/Ammo Regen/Haste use the original Q3 calculations. Classic Q2 preserves fractional firing progress across saves; source grenade fuses and world time remain unchanged. The exact candidate passed production/changed-test strict compilation, 15-file policy and the real Team Arena/Rogue pickup, damage, cadence and save witness. Worker source checks also cover Q1/Q2 expansion deadlines, fuse timing, switches and actor removal. This is newer source, not a new installed binary. [Evidence](../.artifacts/resume-20260922/t12-team-arena/integration-4/EVIDENCE.md).

Newer source unifies selected base-Q3 and Team Arena equipment execution and preserves existing base-Q3 saves. The exact candidate passed four real saves created by installed `e7a9167`: a live grenade, an existing explosion, and attached hooks before and after their scheduled think. Actor identities, trajectories, damage, complete inventory and deadlines survive import and a second save/load; impacts do not replay. Persistent source checks also cover Guard regeneration, Scout movement, canonical base Q1 ammo regeneration and original item return/save state. This source is newer than the installed executable. T12 remains open for foreign-weapon damage/cadence, expansion periodic ammo, menu exposure and native combinations. [Combined evidence](../.artifacts/resume-20260922/t12-team-arena/integration-2/EVIDENCE.md).

The primary Team Arena give-weapons/media check is corrected: it now lets original `G_UpdateCvars` refresh the cheat setting before issuing the command. The earlier rejection was original cached-cvar behavior, not a missing inventory route. Existing media/selection assertions and the public `giveall` check pass (1 check / 15 assertions). No production workaround was added.

Installed `e7a9167` now includes the qualified original QuakeC/native pickup callers, full native inventory and selected Team Arena gameplay/presentation. Exact2752-blob archive verification, build/help and production strict passed. Compiled original Threewave GL movement, firing, armor and save/load/save/quit passed; the Team Arena graphical source check remains separate from that binary check. Binary SHA-256 `57510ac0d90a7d6b46a0f6f9a1230c5eeea8e99a9a9f947bbe0989e9592f39d6`; manifest SHA-256 `de5bd00a05ea5cd4da7ef10e1350cc64b3569af656f46141b5451d3776313b12`. Previous3d11fe7 binary/manifest preserved. [Installation receipt](../.artifacts/resume-20260922/delivery-e7a9167/installation.json).

Newer QVM source adds optional original `clients.frame` callbacks after actor updates. Full original Threewave ClientEndFrame executes on server frames independently of incoming commands, preserving original mode-specific expiry and publication rules. The original Application witness passed4checks/58assertions, including public saves before and after expiration; focused lifecycle/parser checks passed7/68 and six-file policy reported0 findings. Full T10 remains open. [Evidence](../.artifacts/resume-20260922/qvm-client-frame/HANDOFF.md).

Selected Team Arena weapons and holdables now execute original source behavior in mixed ported worlds, with original per-seat projectile/effect/audio presentation. Invulnerability changes collision posture and blocks damage; original teleport velocity survives the movement/weapon boundary. Public `+use` reaches the selected arsenal without a prior weapon selection. Source event baselines prevent replay after save/load. The isolated candidate passed strict compilation and 44-file policy; its real Q1 graphical Application check passed (1 check / 16 assertions), plus reverse Team Arena pickup checks for selected Q1/Q2 weapons (2 / 8). The original portal model paths are absent from the installed official archives; the original default-model fallback remains, with no invented replacement asset. T12 remains open for persistent powerups, menu exposure, base-Q3 saved-projectile migration and additional native combinations. Installed source is still `3d11fe7` pending delivery. [Gameplay and presentation evidence](../.artifacts/resume-20260922/t12-team-arena/integration/EVIDENCE.md).

Original Xatrix and retail Q2 rerelease armor/ammo pickups now delegate selected grants while retaining original item feedback, targets and lifetime. Shared native inventory binds the complete qualified source item table and live ammo limits; save restoration validates current source agreement and migrates older partial coverage. Actual original-DLL physical pickup and public save/load checks passed in both editions (2 checks / 24 assertions). The separate classic inventory check covers changed capacities, foreign supplemental items, inconsistent-save rejection and legacy migration. The rerelease physical-pickup/save/load check took 218 seconds; this is functional evidence, not a performance claim. Additional artifact layouts and pickup kinds remain unfinished. Installed source remains `3d11fe7`. [Evidence](../.artifacts/resume-20260922/mod-pickup/native-primary/HANDOFF.md).

Original classic id1 QuakeC armor and ammo callers now delegate selected recipient grants while retaining their authored map behavior. The exact candidate passed strict production/fixture compilation, five-file policy and ten focused checks with 88 assertions. Actual e1m1 touches ran original Threewave grants; public save/load restored a fresh actor, preserved hidden primary armor and inventory, and did not replay a hidden pickup. This also fixes offline dedicated save eligibility using admitted simulation players. Other QuakeC pickup kinds/artifacts and native primary callers remain unfinished. Installed source is still `3d11fe7`. [Evidence](../.artifacts/resume-20260922/mod-pickup/qc-primary/HANDOFF.md).

T19 is accepted under the project owner's September 22 clarification: a working extension point for a future ranking service is sufficient. `ApplicationHost.rankings` already supplies a `RankingServiceProvider` to the source reports and account UI; no provider gives an explicit unavailable state. The ranking backend is the project's **only permitted placeholder**. Six focused ranking/progress checks passed with 24 assertions. The original online service is no longer a completion blocker. [Provider contract](ranking-services.md). Current total: **21/23; T10 and T12 remain open**.

Installed `3d11fe7` adds original-primary QVM pickup callers, original source-backed weapon/ammo inventory and older-save migration. The exact candidate passed strict production/changed-test compilation, 14-file policy, 17 focused tests / 120 assertions and two actual Application checks / 29 assertions. The archived executable source passed strict/build/help; compiled original Threewave GL movement, firing, armor and save/load/save passed with source inventory agreement. Binary SHA-256: `16054f388fd22d7f82586b4e5a36c0894caffe7cd1e26bda732946e83aac494e`. Previous `9ddbb8d` binary and manifest are preserved. [Delivery receipt](../.artifacts/resume-20260922/delivery-3d11fe7/RUNTIME-RECEIPT.md).

Installed `9ddbb8d` adds original mod pickup operations to the prior QuakeC client-frame and component-presentation work. Root verified the exact candidate with strict TypeScript, policy on 39 files and 65 focused tests / 854 assertions. Original native checks passed five tests / 95 assertions. The archived executable source passed the public Q1 original pickup/save-load check; compiled Threewave GL movement, firing, armor, save/load/save and normal quit also passed. Executable SHA-256: `b1476455f6d35d20447c99899f1236e88ee3da6e9327e1953fba1511cc950265`. Previous `7bd9461` binary and receipts are retained locally. [Current delivery evidence](../.artifacts/resume-20260922/delivery-9ddbb8d/RUNTIME-RECEIPT.md). T10 remains open.

Installed `7bd9461` adds original QuakeC client frames and scheduled callbacks to the component cgame sounds/effects, source context and saved event-cursor restoration delivered in `8b80467`. The exact archived source passed strict compilation and five original QuakeC checks with 102 assertions, including Hipnotic held fire, private animation progression and checkpoint continuation. The compiled executable passed original Threewave GL movement, firing, armor, save/load/save and normal quit. Executable SHA-256: `6507c3ceb581132ecd9aa8793939c3ed6aa06d630bd07c86ede9e82d263252f1`. Previous `8b80467` binary and receipt are retained locally. [Current delivery evidence](../.artifacts/resume-20260922/delivery-7bd9461/RUNTIME-RECEIPT.md). The earlier public original Threewave component check in a Q1 Application covered original regeneration audio and save/load without replay: [8b80467 evidence](../.artifacts/resume-20260922/delivery-8b80467/RUNTIME-RECEIPT.md). T10 remains open; full campaigns, physical audio and whole-game FPS remain unqualified.

## Earlier installed executables

Current binary: `edcfada8987463e477c67828c975854cd4a54b9d`, tree `90392f9a33d2181efe9c02eda5aa213ea5591192`, 132,929,664 bytes. SHA-256 `7c732723b50000e74a8569bb9e32ad9e14ab75a1804b73903c000a7b82a1508a`; manifest SHA-256 `76f59d727e99e6e324a4110b2122b4e031f78a3c1631c44cfbf97ba6e2bea77b`. All 2,858 tracked blobs match the committed archive. The existing compiled GL movement/fire/armor/save-load-save/quit check passed; source/canonical saves and played/restored frames were inspected. Previous binary and manifest are preserved locally. Physical audio, full campaigns and whole-game FPS are not claimed. [Delivery receipt](../.artifacts/resume-20260925/delivery-edcfada8/RUNTIME-RECEIPT.md).

Earlier source delivery checkpoints:

Current pickup source also connects qualified original Q3/Threewave primary callers to declared grants, preserving original feedback, targets and respawn timing across asynchronous execution. Canonical inventory now borrows their original weapon bits and ammo counters. The public Threewave Application check covers physical armor/ammo touches and save/load; older checkpoints gain the inventory view from restored VM storage. Installed `3d11fe7` contains these original-primary joins and the preceding ported Q1/Q2/Q3 map integration. Original QuakeC/native primary callers, map-coupled objectives and arbitrary new inventory items remain separate work. [Pickup declaration contract](mod-compatibility.md#original-pickup-operations).

QuakeC component source now admits original per-client frame calls and source think deadlines. The original Hipnotic player code passed held fire, consumed input, private animation progression and checkpoint continuation in a foreign world fixture: six focused tests, 128 assertions, strict TypeScript and seven-file policy checks. The source frame runs after destination movement; complete original client physics ordering and destination animation presentation are not claimed. This change is installed in `7bd9461`. [Evidence and limits](../.artifacts/resume-20260922/qc-client-frame/HANDOFF.md).

Accepted source now connects declared QVM component player events to each mod's original cgame, with separate viewing-seat state and component-owned audio channels. Original transient frame calls supply geometry and lights to the shared destination view. Source serverinfo comes from the original registry and actual map; restored event cursors follow restored actor identities. The public Application check used original Threewave timer code to emit its regeneration sound in a Q1 world, then saved, loaded and resumed without replay. Separate original-client graphics checks rendered the original teleport model/shader over Q1 geometry and verified its source lifetime. [Qualification](../.artifacts/resume-20260922/component-client-integration/EVIDENCE.md). This change is installed in `8b80467` and does not close T10.

Current source adds before-input outputs, original native held-fire execution and raw QVM borrowed-player event publication. Controls reach the selected movement and weapon continuation while received input remains immutable. Original source and five-profile checks pass. The combined input suite passed 46 tests with 579 assertions; original native and QVM receipts cover the source clients separately. Shared Q2 transport also retains ownerless water, rail and BFG trails. Original command owners no longer infer one-shot impulses from an old receipt. This unit is installed in `af904e4`. Component cgame effects and remaining full player/weapon services remain unfinished.

Current T10 work generalizes independent protection to regular and powered channels across QuakeC, QVM and native components. The original donor owns each calculation and debit; declarations can explicitly replace the destination primary without replacing another component. Source observers preserve atomic stores to both layers and nested hits. Saves retain current hidden primary armor and original module checkpoints. The combined source passed 125 checks with 1,260 assertions, strict TypeScript compilation and policy on 53 changed files. Review corrections cover transitive QuakeC helper inputs, nested committed armor records and save capture of noncombat actors. Public Application discovery, activation, damage and save/load ran original Threewave armor in a Q1 world. This source is installed in `5ce1a80`. [Qualification](../.artifacts/resume-20260922/regular-integration/EVIDENCE.md). Complete source player/weapon loops, foreign pickup operations and remaining services are still unfinished.

Committed `cf70f95` connects qualified original QuakeC and QVM damage to independent powered protection at the original armor stage. The original code retains regular armor, source rounding, empathy/self-hit branches, momentum and reactions. QVM calls share one observed damage scope whether invoked internally or from the engine. QuakeC uses an admitted inline continuation, including declared helper layouts. Public health/armor writes and component removal update the same damage cursor; original free/spawn slot reuse cannot apply the old hit to the replacement actor. Rogue primary armor uses its original `items2` masks. This change is also included in installed `5ce1a80`. [Integration evidence](../.artifacts/resume-20260922/armor-integration/EVIDENCE.md).

The QVM committed-write foundation is implemented in source. Interpreter stores, memory intrinsics, game records and migrated host outputs share one allocation owner. Range observers capture committed bytes in order; retired allocations reject writes and restore/restart invalidate registrations. Review corrections cover retired interpreter entry and legacy bot output overlapping its syscall arguments. This change and the original QVM/QuakeC damage-stage integration are included in installed `5ce1a80`.

Source checkpoint `c8d45da` corrects qualified original QVM armor metadata and its HUD/save projection. Exact immutable tree `815c51e9dae42dc530eaa1bf1cb297da1e0cfcc6` passed full production/private-fixture strict compilation, policy on four changed files, and six original-module checks/105 assertions. The initialized Threewave Simulation covers HUD values, point writes preserving tiers, current and legacy saves, unchanged VM bytes and rejection of corrupted new saves. This checkpoint is included in installed source `ffd745e`. Original QVM powered-stage integration is now implemented in newer source. [Evidence](../.artifacts/resume-20260920/next-original-armor/ROOT-EVIDENCE.md).

Earlier checkpoint `10f9150`, retained in the installed executable, lets declared native components supply independent powered protection through original activation and absorption routines. Shared Q1/Q2/Q3 combat and qualified original Q2 classic/rerelease damage stages preserve their regular armor and health owners. Source admission, save restoration, explicit owner conflicts, disable and nested fuel writes are covered. Strict compilation and policy on 31 changed TypeScript files passed. The frozen behavioral source passed 64 tests/560 assertions; correcting the obsolete network continuation field then passed the existing event roundtrip with 31 assertions. Original QC/QVM power stages, borrowed regular armor and full source movement/weapon loops remain unfinished. [Qualification](../.artifacts/resume-20260920/borrowed-power/EVIDENCE.md).

Source checkpoint `23b3a90` adds declared QuakeC, QVM and native component callbacks on applied player input. These callbacks are installed. Root qualified immutable tree `b35d342d86afcf3b6d09de9a2beb0f5918dd7e38`: strict source/fixture compilation and policy on 26 changed files passed. The combined behavioral run passed 17 tests/238 assertions in 42.42 seconds; after correcting an iterator type and a test error boundary, the affected QVM/common checks passed nine tests/72 assertions. Original module routines, native working ground state, nested input/removal and source save behavior were exercised. Complete original movement/weapon loops, borrowed regular armor and unqualified original power stages remain unfinished. [Callback evidence](../.artifacts/resume-20260920/component-applied-input/EVIDENCE.md).

The installed executable contains production source from `5ce1a8040d8d0ef26314c67f907be343cb502b79`, installed at `2026-09-22T23:27:55.814000+00:00` in `~/Projects/qfiles/quake-typescript`. SHA-256: `d0f833d41f193fa2a4c70e9f13149dfc430411a4aa2a30c09f22dbf32b1e2f56`; size: 130,623,616 bytes. Immutable build tree `5f0c570ba0e724c92038820ad42e4aed5773dabe` exactly matches the accepted commit. The compiled original Threewave GL run covers movement, firing, armor and save/load; decoded saves and inspected frames agree. [Installation receipt](../.artifacts/resume-20260922/delivery-5ce1a80/installation.json), [runtime qualification](../.artifacts/resume-20260922/delivery-5ce1a80/RUNTIME-RECEIPT.md).

The exact compiled binary ran original Threewave on `q3ctf1` under private GL. Actual input moved the player, fired five shots, granted original armor, saved, loaded, saved again and quit normally. Source and canonical saves agreed on position, ammunition, armor points and protection. The restored HUD was inspected. The run used explicit `--movement q3-classic-threewave`; generic `--movement q3` resolved to base-game content and failed native ownership validation. Original cgame also reported a `Com_sprintf` overflow warning at initialization and restore. Neither observation is claimed fixed. [Compiled receipt](../.artifacts/resume-20260922/delivery-ffd745e/RUNTIME-RECEIPT.md).

The earlier `10f9150` compiled binary ran Q1 rerelease `start` with Q2 classic movement, male character and weapons, plus the original Threewave Morning Star. It loaded an existing `QTSAVE2`, selected and fired the hook through actual GL input, saved one hook and three chain links as `QTSAVE3`, released them, loaded the released save, saved again and quit cleanly. Selected, firing and restored captures were inspected. Existing rerelease saves retain their original grappling-gun package. [Compiled receipt](../.artifacts/resume-20260920/delivery-borrowed-power/RUNTIME-RECEIPT.md). The previously observed held-attack reset on loading remains a separate limitation; this delivery qualified released-state continuation.

The Q3 cable correction is included. Its unchanged source passed an earlier compiled GL check with attachment, a 339-unit pull, release and inspected rendering without the crossing triangles. [Q3 cable receipt](../.artifacts/resume-20260920/delivery-hook-appearance/RUNTIME-RECEIPT.md).

The end-map stall fix is retained. The preceding `437bc9d` binary ran the user's copied Q1 classic `end` save with Q2 classic movement, male character and monsters, plus Q3 weapons. That private GL run completed 38 seconds, 1,053 timed gameplay updates, a save and clean quit. Maximum simulation, presentation and render times were 24.914, 19.034 and 36.990 ms; repeated multi-second simulation stalls did not recur. This benchmark was not repeated for the subsequent hook and mod-service changes. [End-map receipt](../.artifacts/resume-20260920/delivery-live-fixes/end-runtime/receipt.md).

The installed binary retains Q2 camera/BFG recoil, authored monster teleport and rocket-jump direction fixes. It also includes the accepted native actor combat and QVM entity-stream integrations below. Frozen source and affected tests passed strict TypeScript and policy checks. These bounded runs do not qualify whole-game FPS, physical audio or complete campaigns. The full repository test typecheck has unrelated baseline errors; the rerelease config's unsupported `scr_usekfont` warning remains.

**21/23 targets are accepted in source. T10 and T12 remain open; T19 uses the explicitly approved ranking-service extension point.** General mod composition and cross-runtime equipment remain in progress. See the [current target list](functional-targets/status.md) and [mod compatibility boundaries](mod-compatibility.md).

Source checkpoint `ce3c9ba` separates regular armor from powered protection throughout authority, gameplay, source bindings, saves, networking and presentation. It also connects authored screenshake cleanup to source-owned view roll without resetting input or punch. Root qualified the exact immutable build tree: strict source/fixture compilation, 137-file policy with zero findings, and 50 focused checks with 448 assertions passed. Original API3/API2023 tests cover damage/store ordering, save continuation, power selectors/masks, separate fuel reservoirs and depleted enum-selected armor. Original MG1/QC/native checks cover view-roll cleanup and subsequent input. This representation change does not yet insert foreign powered protection into original module damage stages. All these changes are installed.

Source checkpoint `06ba62f` connects original LRCTF/Threewave applied-input boundaries, source body writes, retained commands and safe retirement. Root qualified immutable tree `30b978c73019d12b12ac6da29ff804292bec4727`: strict source/fixture compilation and 10-file policy passed; two original-module checks passed 116 assertions with offhand grenades enabled. Review caught and corrected ground-state overwrite and retired-player equipment cleanup. A private fixture narrowing correction changed no production code. Internal spawn settling deliberately publishes no application event; other profiles and authored component input callbacks remain open. This checkpoint is installed.

Source checkpoint `a155ba6` connects Q1 addon punch and weapon recoil to one source-owned vector, preserves source decay and save continuation, and composes that vector with native Q2/Q3 cameras. Native inventory declarations also preserve paired count/capacity changes for qualified owners. Immutable tree `91f0b53f989de868f933fac3cac924765f4f8338` passed strict source/fixture checks, 14 focused tests/95 assertions, and two original native camera tests/13 assertions. Policy found unsafe inference only in the legacy-save test; the final tree `7a52913afefca1a985322f701d39fa5a8fb96d61` corrects that one test, with strict checks, its policy check and the legacy case passing. Production source is identical between those trees. This checkpoint is installed. Fixed-limit source inventory owners remain unsupported; addon view-roll follows in the armor/view-roll checkpoint below.

Source checkpoint `0808144` connects original native API3/API2023 input execution, actual movement-body writes and deferred client retirement. It also adds scoped QVM call cancellation needed by the upcoming QVM input adapter. Root qualified immutable tree `55f4ebd8cc2138c1add394bdf9387077f3e22fab`: strict source/fixture compilation and 15-file policy passed; two original native tests passed 70 assertions, and 40 QVM tests passed 269 assertions. Callback-failure teardown covers original API2023 close and API3 discard. Native internal bot callers and authored component input callbacks remain unqualified. This checkpoint is installed.

Source checkpoint `ca78b15` adds declared QuakeC client lifecycle callbacks, canonical userinfo fields and source-correct QuakeWorld client builtins. Ordinary actors retain their private fields. The frozen source passed strict TypeScript, eight-file policy, and five focused tests with 44 assertions, including original Copper and QuakeWorld code, saved client state and generation reuse. This is included in the installed executable.

Source checkpoint `f2b3dc4` adds shared component client identities, raw userinfo storage, actual accepted commands, saved input and generation-checked disconnects. Declared QVM components reserve their own client rows and execute explicit original lifecycle callbacks. Directed messages retain their recipient through network, bots and demos; private configstrings stay with their component. Root checked the frozen source and affected tests: strict TypeScript and 31-file policy passed, eight focused tests passed 118 assertions, disconnect ordering passed seven assertions, and an actual original LR/Q1 Application save/load and stale-client-drop run passed 16 assertions. QuakeC client adapters followed in `ca78b15` and native clients in `5728dea`; shared movement input and QuakeWorld media followed in `a0de426`. These checkpoints are included in the installed executable.

Source checkpoint `5728dea` connects original native API3/API2023 client admission, userinfo, commands and disconnects. Client pointers come from each original edict; rejected clients retain their source row until actual disconnect while callbacks are suppressed. Saves preserve admitted state and reject pending disconnects. Root qualified immutable tree `a998c3b4f1c31ffc33aa94e40870fcb04b73dbb5`: strict TypeScript and eight-file policy passed, and four original-DLL tests passed 110 assertions. This checkpoint is included in the installed executable.

Source checkpoint `a0de426` adds actual applied-input events across the five shared movement profiles and original QuakeWorld component media routing. Input events preserve source subdivisions, timing, aim, nested callbacks and actor removal; no-listener movement retains its existing behavior. QuakeWorld effects use destination visibility and captured recipient generations, with signon and queued ownership retained through saves. Root qualified immutable tree `0f9449a9775f763d59d531646b5309fd06b9ac7c`: strict TypeScript and 30-file policy passed; 44 focused tests passed 610 assertions in 19.19 seconds. Native/QVM execution writers, authored component input callbacks, armor projection and remaining QuakeWorld services are unfinished. This checkpoint is included in the installed executable.

Installed checkpoint `0619295` adds native owned armor storage/mutations and shared original QuakeC aiming. Original API3/API2023 damage and save continuation passed four tests/50 assertions, including preservation of other held armor. QuakeWorld aim/defaults and original NQ attacks passed three tests/204 assertions. Root corrected two forbidden test assertions; strict checks and policy on their final versions passed. Production source is unchanged between the compiled tree and final accepted tree `7d467d3a751e582778ef10dec3f5c0cefe31146c`. Borrowed armor/capacity and source input callback integration remain open. An additional original API2023 diagnostic identified missing declared client classname initialization, rather than a generic actor-pointer failure; the existing address binding supports the required original literal.

The current source runs declared QuakeC, QVM and native components together through public launches, disk save/load, and map travel. Original Copper monster spawn, targeting and scheduled AI use shared world services. Independent original API3/API2023 native callbacks retain separate source saves; authored sound, text and borrowed-actor model changes also cross into the shared world. Original QVM rotating, shootable and breakable brushes run against destination Q1 geometry and retain source behavior after save/load. Native components now retain allocated actors, original scheduled updates and collision across saves. Their authored looping sounds and supported transient effects keep component ownership. Component commands now retain their own cvars, scripts and execution dialect through the shared console; a public Q1 console check exercised original QuakeC, QVM and native commands together before save/load and travel. QVM components have separate persistent output directories, shared read/list/script access and restored file handles; original LR logging and resumed writes were checked. Direct local and dedicated startup now retain the real demo-command service before executing `quake.rc`. Component server-info/configstring imports now share the primary QVM implementation. Q2-character aiming now uses live movement angles instead of waiting for the 100 ms character tick; death and intermission keep source-controlled views. Classic Q2 weapon recoil now expires by source time in both primary and selected arsenals; the BFG and ordinary weapon kicks recover instead of retaining their firing pose. Quake 1 target-activated teleport holding areas now retain foreign monsters through deferred startup and saves; original e1m1-to-e1m2 travel and encounter activation were checked with both Q2 monster editions. Quake 1 knockback now uses causal projectile position for foreign splash damage instead of the world bounds; actual downward Q3 rocket jumps were checked with Q1 and Q2 movement. Stock Q3 weapon-10 hooks now use the original launch, attachment, pull, cable and release behavior across all five supported movement profiles; an attached hook also resumes through save/load. Long foreign-body traces in Q1 worlds now bound clip-only reconstruction at the first drawing-surface hit while preserving earlier hidden collisions and start-solid handling. The captured end-map replay improved from 8.66 seconds to 0.15 seconds for 60 simulation frames, with worst frame 3.08 seconds to 23 ms; this is a simulation measurement, not a rendered-FPS claim. These changes are included in the installed executable. The installed source lets declared native API3/API2023 owned actors receive shared attacks through original damage routines, with use/touch/pain/death callbacks composed once and deferred source damage retaining foreign attack provenance through saves. Actual Xatrix and Q2Eaks callbacks were checked; a first-use participant projection defect found during independent review was fixed and rechecked against original Xatrix execution. QVM component source supports explicitly declared entity streams using the primary token parser and buffer ABI. Original LR parser/spawn calls created one button, saved, then resumed at a second button without duplicating the first; no destination BSP entity lump or second whole game world was supplied. Native client inventory and remaining host/presentation services remain in progress. The installed source also adds the original Threewave 4.0 package and selects its Morning Star for the curated Q1 hook style across worlds. Existing rerelease saves keep their original source identity. Actual Application rendering and save/load were checked. The selected Q3 cable uses the active character’s eye height while native Q3 retains its original default; matching GL captures verify that the crossing geometry is gone. These are bounded integrations, not closure of T10.

Native equipment enters original damage routines for admitted Xatrix, retail Q2 rerelease, LRCTF, and Threewave artifacts. Hook and grenade save/restore checks passed on native worlds, including original grenade inventory debits. Threewave's shared hook model now matches the original cgame position and axes at the same pose and field of view. Full Threewave GL startup also exposed and received general QVM return-control, item-catalog, and Q3 WAV-policy fixes. Both Q3 client implementations now use the shared equipment renderer; actual GL checks cover selection, firing, sound and returning to the primary weapon. Declared body replacements suppress original body submissions while retaining source effects.

### Earlier e689603 delivery

At this earlier checkpoint, installed source was `e689603b91800a1211d8dc3b36f8b8285e3a5f51`, tree `85687a7a23a81fd2057095bfa8d6c144f3a718b1`. Installed at `2026-09-18T11:21:20.337Z` to `~/Projects/qfiles/quake-typescript`: SHA-256 `52337644ff67cccdf1f6e30e6fa82ec2afee6926105746d43225bff482634366`, 128,997,504 bytes, mode `0755`. [Installed receipt](../.artifacts/resume-20260918/final-e689603/installed-build.json). The preceding `71f0e4` executable and receipt are preserved in [the delivery directory](../.artifacts/resume-20260918/final-e689603/previous-71f0e4/).

**Historical checkpoint: 21/23 targets were then accepted; T12 was active and T19 was externally blocked.** T12 was reopened after finding missing expansion-arsenal menu choices and explicit Team Arena cross-arsenal/supply rejections. Its retained expansion firing/save checks remain valid, but did not exercise that complete public selection surface. T10's external native declaration gap is closed: API2023 Windows x64 mod packages can supply artifact-pinned profiles, and saves retain the normalized full declaration. The public `declare-native` workflow installed an external profile, ran actual Q2Eaks code on a Q3 rocket launcher, and preserved exact saved state and 500 ms continuation. [Runtime evidence](../.artifacts/resume-20260918/public-native-declaration/receipt.json). The native declaration extension and MD3/MD2 preparation optimizations are included in the current executable.

The earlier native-declaration composition passed strict TypeScript and source-policy checks over 22 changed TypeScript files and 2,005 loaded dependencies. Nine focused tests passed with 78 assertions; native execution used unchanged guarded source. That checkpoint still required an original GRank transport/provider; the September 22 scope clarification above supersedes it. [Target status](functional-targets/status.md), [integration scope](functional-targets/integration-20260918.md).

The current executable includes the player-preference registration fix, bounded GL array-layout reuse, direct native scalar stores, and movement selection by family, exact installed product, or `qw`. Catalog, menu, and restored recipes use the same normalized movement selection. Build/source guards and `--help` passed. The GL cache has [source pixel/depth qualification](../.artifacts/resume-20260918/gl-layout-native/RESULT.md), and scalar stores have [source native component save/load qualification](../.artifacts/resume-20260918/scalar-store-native/receipt.json); these remain separate from compiled gameplay.

Installed `e689603` adds exact-envelope collision-cache reuse and the public projectile picker for all declared QuakeC, QVM, and native rerelease providers. Their strict/policy and focused source qualifications are recorded in [performance evidence](functional-targets/performance.md#exact-envelope-clip-cell-reuse) and [picker evidence](functional-targets/integration-20260918.md#projectile-behavior-picker-correction). Build/source guards and `--help` passed on this exact delivery; no gameplay run of this executable is claimed.

The preceding `71f0e4` passed the latest compiled gameplay check: direct `--movement qw` on Q1 classic `e1m1` with Q3 Ranger and the default Q1 weapons. Ten seconds of fresh movement/jump/fire completed; a public save retained Q1 classic world content and `q1:quakeworld:id1:installed` movement with the actual QuakeWorld command clock. Gameplay and QUAKE Main captures were inspected, duplicate startup player-variable warnings were absent, and normal Quit exited successfully. Binary/source/assets/runtime guards passed and owned processes were reaped. [Direct QW runtime evidence](../.artifacts/resume-20260918/compiled-qw71f0e4/RESULT.md). This bounded private-display run does not establish whole-game FPS, physical audio, or every movement combination.

The earlier `065259c` passed a compiled gameplay check, a separate GL follow-up on the requested Q1 classic / QuakeWorld / Q3 Ranger and weapons / Q2 rerelease monsters recipe. The full Custom summary was visible; two five-second movement/jump/fire segments ran (11.151 seconds including dispatch/capture), the scene rendered correctly in the inspected capture, and Custom → Play a game → Main → Quit exited normally. Binary/source/assets guards passed, with all owned processes reaped. Root inspected summary, gameplay, and Main images. [Installed runtime evidence](../.artifacts/resume-20260918/compiled-mixed-065259c/RESULT.md). This run also exposed repeated registration warnings for player preferences. The shared helper now reuses retained Q1/QW declarations while preserving values, defaults, flags and early configuration upgrades; that qualified fix is included in the current executable. The core duplicate-declaration diagnostic remains intact. Dummy audio, private display, and bounded gameplay do not establish physical audibility or current desktop FPS.

The compiled `4e82c5a` GL workflow completed 29.467 seconds of movement/jump/fire input with Q1 classic geometry, actual QuakeWorld timing, Q3 Ranger/weapons and Q2 rerelease monsters. Its two public saves retain the same complete recipe and advancing source state. The quit harness selected the wrong retained submenu; that failed attempt remains preserved. [Gameplay result](../.artifacts/resume-20260918/compiled-mixed-gameplay/RESULT.md).

The previous `07d54ae` executable passed a separate short GL supplement: the complete Custom game summary is visible, a 5.933-second interval of fresh controls works, disconnect returns to Custom game, Back reaches Play a game and Main, and ordinary Quit exits successfully. Root inspected the summary, gameplay and Main captures. Binary/source/assets guards passed and processes were reaped. [Supplement result](../.artifacts/resume-20260918/compiled-mixed-supplement/RESULT.md). These are bounded runs on a private NVIDIA display with dummy audio; full campaigns, physical audibility, retail peers and current desktop FPS remain unqualified.

All following checkpoint statuses describe their recorded revisions. Current source completion and installed identity are recorded only in the top section and the [current integration record](functional-targets/integration-20260918.md).

### Previous 5781b0d delivery

Previously installed source was `5781b0db0d074bb85002c471427a8d9f1bbe7347`, tree `14e0a8f61667f4ce839a3b5166f1aa94f95caa45`. Installed at `2026-09-17T14:44:08.929Z`: SHA-256 `c31f8c7bd87abfc28991b2b7878cbe879a9475dae3481e2c6ff27a2218cab84a`, 127,223,936 bytes, mode `0755`. [Installed receipt](../.artifacts/resume-20260917/final-5781b0d/installed-build.json). The compiled delivery passed `--help` only; the approximately 30-second two-seat workflow is separate source evidence, preceding the reviewed final two-file content-publication correction.

T05 input/local-player and T06 console/profile implementations were accepted and installed at that checkpoint: **9/23 targets complete in source, 14 open; T10 active**. The installed executable includes both units. The new Q2 two-peer workflow passed input, promotion/rejoin, map travel and restart; a discovered telefrag collision hang is fixed. [T05 details](functional-targets/status.md#t05-input-and-local-players). T06 adds dedicated binding persistence, actual save/exec feedback and Q1 startup command ordering. [T06 details](functional-targets/status.md#t06-console-cvars-and-profiles). T05 native source verification passed 39 assertions in 31.7 seconds; T06 existing checks passed 96 tests/918 assertions in 195 ms. The compiled delivery passed `--help` only; no compiled gameplay, physical-audio or FPS qualification is claimed.

T04 audio/music implementation is accepted and installed. Source qualification passed strict/policy, 6 focused tests/37 assertions, and actual three-family Application audio plus authored Crash `play` (1 test/33 assertions). Runtime was checked before compilation; no final-binary full-session claim follows. Earlier 45 jump routes and TA fallback coverage do not establish all-model gameplay. Quiet-rocket listening remains unresolved despite correct source-position/lifetime traces. Physical output and broader CIN/video QA remain open; native QC/QW foreign-character/movement admission belongs to T10/T11. [Audio evidence](../.artifacts/resume-20260917/t04-audio/command-audio3.log).

The earlier `30ea85d` delivery completed T02 content/assets and T08 downloads. That compiled executable installed the cached verified Base To Hell package, played 30.311 seconds with movement/fire, saved and loaded the world, restored the exact saved player position, and accepted fresh input. A separate same-binary run inspected the authored Training/Crash/5-frag picker, returned to Main and quit normally. Root inspected all five gameplay, restored/resumed and menu captures. [Gameplay evidence](../.artifacts/resume-20260916/final-30ea85d/runtime-proof.json), [menu supplement](../.artifacts/resume-20260916/final-30ea85d/menu-supplement.json).

The initial external checks raced asynchronous Install/Play and arena publication; those failures remain preserved. The gameplay runner passed its save decoder but did not exit normally at the final arena navigation; the separate menu supplement closed that boundary. This combined evidence does not claim a full campaign, FPS improvement, physical audibility or complete rendering fidelity. T03 is delivered; the later T04 delivery joins source audio commands and Crash `play`, while listening validation stays open.

Accepted `333f0118bb92698d93202af516c67c76b78fdb19` previously brought source completion to **4/23 targets, 19 open**; that delivery is superseded by `ed1ceac`. Check5 passed strict/policy qualification. The compiled check4 worker→serial workflow passed 30.199 seconds of controls, retained recipe/binding, fresh input, capture and Main/Quit; root inspected four captures. The check4 proof uses identical worker/Application code, while the final two-path candle delta was qualified separately. The exact installed binary passed `--help`; no full-session run of that final binary is claimed. Shared CPU/GL worker execution, restart/capture and live hardware getters are implemented; worker execution defaults off and no FPS gain is claimed. [Compiled receipt](../.artifacts/resume-20260917/t03-compiled-check4/proof-worker4/receipt.json).

T07 now includes mixed-game UDP composition negotiation, movement prediction, compressed public snapshots, reliable events and retained local-seat travel/restart, in addition to selectable DOSBox IPX. The existing two-seat source workflow passed in 29.86 seconds; a final reviewed correction prevents secondary seats from borrowing content before the primary commits its new world. The native IPX backend is accepted in later source; KEX 2023 lobby transport and public rerelease connections are now joined in newer source. Broader native-peer testing is deferred while implementation proceeds. The unified and DOSBox IPX slices are now installed in `5781b0d`; no compiled gameplay, physical-audio or FPS result is claimed. [T07 evidence](functional-targets/status.md#t07-mixed-game-connections).

Previous delivery identity: Source was `3b1c9b2b2cfdea1b7f47c29463e4e8a2a6f69a75`, tree `d7f828d435a4e4822e82249f354efe4f3cea33d3`. Installed at `2026-09-17T13:17:58.832Z`: SHA-256 `db90db706ca45b08d59439941b436d0fe503a002a643305205404783daab7c0e`, 126,724,224 bytes, mode `0755`. [Installed receipt](../.artifacts/resume-20260917/final-3b1c9b2/installed-build.json).

Native IPX socket bindings are now implemented in accepted source for Linux/glibc and Windows/Winsock, with explicit OS-provider requirements and lazy library ownership. The actual Linux capability bind reported `EAFNOSUPPORT (97)`; successful IPX exchange and Windows execution remain unverified. This source update is newer than installed `5781b0d`. The later KEX integration completes the known T07 implementation work; installed identity above remains the delivery authority. [Native backend evidence](functional-targets/status.md#native-ipx-socket-backend).

## Historical 6103c97 delivery

Previously installed source was `6103c976f2abd315176206ebe190661173204fb4`, tree `ec2b8e3352e7fd2b3ebfa4fe6af5e7b43ea5e532`. Installed at `2026-09-17T03:57:00.369Z`: SHA-256 `04bf9f0d533d36ddc939bbaea148ff1e8c6c30a1f66aa42a3ee65282c493f0c7`, 124,565,632 bytes, mode `0755`. The previous executable and receipt are preserved for rollback. Receipt: [installed build](../.artifacts/resume-20260916/final-6103c97/installed-build.json).

The compiled public check selected a Q1 classic world, QuakeWorld movement, Q2 classic weapons/monsters and Q3 Ranger. It passed 8.251 seconds of controls, an `e1m5` round trip, named save, Main return and normal Quit; root inspected the captures. This is separate from the 30.416-second source proof, not a compiled 30-second claim. Earlier failed harness attempts remain recorded. A tank actor exists in the checkpoint, but the pictured monster was not identified as a tank; no tank appearance-parity claim follows. Evidence: [compiled proof](../.artifacts/resume-20260916/final-6103c97/runtime-ready4-proof.json).

This delivery includes the earlier Application integrations and `451a905` model-material preparation optimization. Its 19.55% aggregate component improvement across three pairs does not establish an FPS gain. Generalized jump audio remains in progress: a private lookup check covered 136 model entries across 25 installed products, but newly found Q3 event-routing and Q2 voice-selection defects remain private work, not installed fixes or all-model gameplay qualification. Physical audio, full campaigns, FPS and broad visual/native-peer parity remain unqualified. The earlier pillar observation is now attributed to authored BSP underside geometry and baked darkness; no renderer correction is justified. Functional completion at that checkpoint was **3/23 complete, 20/23 open** in accepted source; see [target status](functional-targets/status.md).

Root accepted T02 content/assets and the separately qualified T08 downloads in check13 source. Public add-on installation, 15 seconds of GL gameplay, death recovery, Main/tier-picker navigation and Quit passed; the identical authored Crash fixture also passed 30 seconds of CPU gameplay and Endgame → Main → Quit. The composition generalizes map-only add-on program inheritance/configuration discovery and nested music listing. The later `30ea85d` delivery above installs this composition. Absent Quake64/demota media, physical audio, visual fidelity and steady performance remain unqualified; the authored Crash source `play` routing gap stays open under T04. See [completion evidence](functional-targets/status.md).

## Historical 6d6f60b recovery proof

Previously installed source was `6d6f60ba0096c43c455341cb6582c27d334f6ea0`, tree `a97e64cc7fcdd8dbd95e441434b4d083a2006292`. Installed at `2026-09-17T02:49:29.109Z`: SHA-256 `0af03c500c2912e051feea9be3064c08e4da2fcf7c26a1e3aefa17860965be8f`, 124,557,440 bytes, mode `0755`. The previous executable and receipt are preserved for rollback. Receipt: [installed build](../.artifacts/resume-20260916/final-6d6f60b/installed-build.json).

This includes the accepted `af7be7f` Application integrations, `189118b` LLM stream correction and subsequent delivery fixes. Content, local input, HUD, progression, modes, campaign, saves and tools now have installed consumers. This is bounded delivery, not closure of those functional targets: **1/23 complete, 22/23 open**. See [functional target status](functional-targets/status.md).

The corrective compiled run exercised 8.162 seconds of movement/jump/crouch/fire, named save/load, exact saved player-position restoration and two seconds of fresh post-load controls, then returned to Main. Its original final navigation clicked Library instead of Quit and timed out; that failure remains recorded. A separate same-binary Main/Quit check exited normally with guards passing, without replaying gameplay. Root inspected all four gameplay/restoration/Main captures. Evidence: [runtime proof](../.artifacts/resume-20260916/final-6d6f60b/runtime-proof.json), [Quit supplement](../.artifacts/resume-20260916/final-6d6f60b/quit-supplement.json), [visual acceptance](../.artifacts/resume-20260916/final-6d6f60b/root-visual-acceptance.json).

The earlier [30-second gameplay proof](../.artifacts/resume-20260916/final-189118b/contained-runtime-proof.json) belongs to `189118b`, whose load failed; it is not a 30-second proof of this corrected executable. Older-save compatibility was not exercised. The black triangular pillar observation was later attributed to authored BSP underside geometry and baked darkness, rather than a proven renderer defect. Physical audio, FPS, complete campaigns and the required native-peer matrix remain unqualified. Real existing-subscription LLM ask/validated echo execution passed separately; fresh browser sign-in remains unverified.

## Historical 68219cc audio and gameplay proof

Previously installed source was `68219cce77608429be92462a377ccbb07854a524`, tree `170115bdecb96fb87bad5841aa9947a57bfec6b2`. Installed at `2026-09-16T23:20:17.969Z`: SHA-256 `0246a9ab79b236d8ba0de08bd0c5db9f5ca3f3bf7df3abdd650c50bd34cb7dca`, 123,504,768 bytes, mode `0755`. The previous executable and receipt are preserved for rollback. Receipt: [installed build](../.artifacts/resume-20260916/final-68219cc/installed-build.json).

This adds retained music selection/shuffle and the scrolling Sound menu to the earlier shared audio/video/settings controls. It also includes accepted inventory, model preparation, cvar snapshot, actor registry, model-light and Q1 collision component optimizations. Component measurements do not establish an overall FPS improvement.

The compiled mixed-world check completed 30.169 seconds of controls, map restart and public settings persistence. Its later menu driver navigated the wrong menu and failed; that original result remains recorded. A separate check on the same binary restored the exact saved audio settings, showed `music/D_DDTBLU.ogg` and Shuffle On in the full Sound menu, returned to Main and quit normally. Root inspected the gameplay and menu captures. Evidence: [original run](../.artifacts/resume-20260916/final-68219cc/runtime-proof.json), [menu supplement](../.artifacts/resume-20260916/final-68219cc/menu-supplement.json), [visual acceptance](../.artifacts/resume-20260916/final-68219cc/root-visual-acceptance.json).

## Historical 74119b8 audio and startup proof

Previously installed source was `74119b8d58fa1a9832bbb1464d8e32aaf0d6d725`, tree `d1cbffd518156cde6fbbbc36be930c8cc34c337e`. Installed at `2026-09-16T12:02:42.748Z`: SHA-256 `275a9970fe87a1b895a733ea6ca6e1b0e315d0c2397d26875be9bff6858eaf88`, 123,480,192 bytes, mode `0755`. The previous executable and receipt are preserved for rollback. Receipt: [installed build](../.artifacts/resume-20260915/final-74119b8/installed-build.json).

This installs shared output-rate, mono/stereo, sample-format and device settings with persistence, restart and session transfer; the SDL3 audio adapter; Main recovery when the frontend menu stack is empty; and the accepted PCM conversion, material projection and lazy clip-plane changes. SDL2 remains the fallback when SDL3 cannot be loaded; its native fallback execution is not qualified by the SDL3 tests.

The compiled GL check changed actual playing output to 8000 Hz mono 8-bit and restored 44100 Hz stereo 16-bit through public console commands, then completed 10.165 seconds of controls, public saves, Main return and normal Quit. Save-to-save source time advanced 17,900 ms against 17,914.939 ms of host time. Runner, game exit and integrity guards passed; processes were reaped. Root inspected all four audio/gameplay/Main captures. Receipts: [runtime proof](../.artifacts/resume-20260915/final-74119b8/runtime-proof.json) and [visual acceptance](../.artifacts/resume-20260915/final-74119b8/root-visual-acceptance.json).

Separate source checks exercised the audio menu, world/travel/frontend transfer and persistence, plus all 16 combinations of 11025/22050/44100/48000 Hz, 8/16-bit and mono/stereo on SDL3. These used dummy audio. Physical speaker output, full campaigns, whole-frame performance and the native-peer matrix remain open. The earlier CPU source-clock lag below remains unresolved. `s_info` still displays floating-point noise in volume values.

Accepted source `b416aef` additionally reduces Q1 cap-membership allocations. All 5,000 retained complete traces matched; three component pairs reduced aggregate elapsed by 3.34%. This change is committed and pushed but is **not included in installed `74119b8`**. See the [comparison](../.artifacts/resume-20260915/cap-membership8b10/RESULT.md).

Accepted and pushed source also includes inventory count reads (`20e1411`), model vertex construction (`d4a0888`) and canonical cvar snapshot reuse (`fcc6cbb`). None is included in installed `74119b8`; its receipt and executable hash above are unchanged. Component measurements found 93.82%/95.53% lower inventory count time for 8/32 entries, 13.012% lower MD2 preparation time, and 15.27% lower actual settings-signature time. MD5 results were mixed, including one pair 5.070% slower; no stable MD5 gain is established. See [performance evidence](functional-targets/performance.md#current-evidence-and-remaining-work).

The separate fcc6 profile matched retained workload clocks, query counts and state apart from documented private mount identities. Presentation still averaged about 27 ms in that private setup; this is not a paired FPS result. GL finite-validation unrolling and normal-scan candidates were rejected for regressions. Actor-registry snapshot work and model-light ownership/music work remain pending, not accepted changes.

## Historical 94d7f5b renderer-transition proof

The previously installed source was `94d7f5be60312aca3e558f7f7c3684719a0933b6`, tree `a25800d45847ead67842d9c23f45698f1ff1dbe9`. Installed at `2026-09-16T10:59:30.174Z`: SHA-256 `333dcb8b44f1468936526bcaa1880d7f79c694f4fe69dfd555ac1640d45f7b10`, 123,328,640 bytes, mode `0755`. Receipt: [installed build](../.artifacts/resume-20260915/final-94d7f5b/installed-build.json).

Shared renderer restart is installed. The public run completed all three ten-second control segments, renderer transitions, captures, Main return and normal game exit. The formal runner returned 1 because CPU rendering accumulated source-clock lag beyond the unchanged 200 ms gate. The catch-up work budget retains that debt; GL subsequently repaid part of it. The workflow and reviewed images are accepted, but the run is not a clock-parity or performance pass. Receipts: [public proof](../.artifacts/resume-20260915/final-94d7f5b/runtime-proof.json) and [visual acceptance](../.artifacts/resume-20260915/final-94d7f5b/root-visual-acceptance.json).

MD2 topology reuse (`c77cdca`) and MD5 joint-row reuse (`f8dbf98`) are also installed. Component measurements showed about 27–28% lower MD2 preparation time and 18.29%/17.81% lower MD5 soldier/parasite preparation time, with exact geometry comparisons. These are component results, not whole-frame or FPS gains; see [performance evidence](functional-targets/performance.md#current-evidence-and-remaining-work).

The separate native LRCTF test reached all 109 body assertions, then returned 1 on its final CPU-swap diagnostic. Root reviewed both captures; the whole test did not pass. Full clock parity, FPS, hitches, physical speaker output, full campaigns and the native-peer matrix remain open.

## Historical 80b8351 music and clock proof

The previously installed source was `80b8351f3c2a4b325065a662375cdf3dbb876f9f`, tree `01bad719779563b32dd0eef48af3ea87f9d07c13`. Installed at `2026-09-16T09:47:09.564Z`: SHA-256 `4702ce74eac6ed655b19b1e585818450ee91fdf9415de5b308a98dfdf7379536`, 123,279,488 bytes, mode `0755`. The accepted source matches the compiled projection; the previous binary is preserved for rollback. Receipt: [installed build](../.artifacts/resume-20260915/final-80b8351/installed-build.json).

That public Q2 rerelease check preserved CD disabled/remap state from Main into the world and exercised loop, play and stop. It completed 30.160867 seconds of controls with the flashlight on, returned to Main and quit normally. Save-to-save host time advanced 37,699.844 ms against 37,700 ms of source time. That interval includes console/save overhead, not just controls. Exit and integrity guards returned zero, owned processes were reaped, and root inspected eight images. Receipts: [runtime proof](../.artifacts/resume-20260915/final-80b8351/runtime-proof2.json) and [visual acceptance](../.artifacts/resume-20260915/final-80b8351/root-visual-acceptance.json); the install receipt supersedes earlier pending status fields.

Heatbeam precaching (`293d3fc`), Q1 fog (`2cea88f`) and retained CD controls (`80b8351`) are now installed alongside the earlier clock, body, timer, music, flashlight and travel fixes. Fog was checked separately in nine CPU/GL pixel cases; the compiled Q2 run above does not demonstrate Q1 fog. The separate [5bbc933 fixed-work profile](../.artifacts/resume-20260915/current-stage-cost5bbc/RESULT.md) measured 65.108 ms mean application time, including about 27 ms in presentation, 16.5 ms in simulation and 7.5 ms in model preparation. These spans overlap and include uncalibrated instrumentation overhead; they are not a current gameplay FPS measurement.

Renderer restart and alias optimization were unfinished at that installation. FPS, hitches, physical speaker output, full campaigns and the native-peer matrix remain open. The GL depth/blend cache remains removed after no measured elapsed benefit. No whole-target closure is claimed.

## Historical 40c9042 clock proof

The previously installed source was `40c9042bbe9aad838bd3037feb5ec5b1f2371d9d`, tree `81a1736eb9d1c634021af9287e1a18fdad6e24e1`. Installed at `2026-09-16T09:13:34.122Z`: SHA-256 `51b88fccd563442c24859486eeb2bd654814190173ff45efc1a6fd85e37cf176`, 123,242,624 bytes, mode `0755`. The accepted source matches the compiled projection; the previous binary is preserved for rollback. Receipt: [installed build](../.artifacts/resume-20260915/final-40c9042/installed-build.json).

That public Q2 rerelease check completed 30.155607 seconds of controls with the flashlight on, returned to Main and quit normally. Between saves, host time advanced 37,811.956 ms and source time 37,800 ms, a difference of 11.956 ms. This interval includes console/save overhead and is not an FPS measurement. Exit and integrity guards returned zero, owned processes were reaped, and root accepted three images. Receipts: [runtime proof](../.artifacts/resume-20260915/final-40c9042/runtime-proof.json) and [visual acceptance](../.artifacts/resume-20260915/final-40c9042/root-visual-acceptance.json). Their earlier pending-installation fields are superseded by the install receipt.

The Q2 clock now advances on native 25 ms rerelease and 100 ms classic ticks, with a 200 ms catch-up work budget that retains outstanding time debt. This binary also includes local body storage (`91fe177`), Q1/Q3 powerup timers (`7ede98a`), music pause/resume/info (`392cf3b`), rerelease flashlight lighting (`a16da1e`) and Q2 fallback travel correction (`4a9b72c`). The separate [a16 public run](../.artifacts/resume-20260915/final-a16da1e/runtime-proof.json) showed music reporting Currently/Paused/Currently on track 9 at volume 1 and flashlight off/on/off images. That earlier run's clock anomaly remains recorded; it is not the current clock proof or evidence of physical speaker output.

At that installation, heatbeam precaching, fog and broader CD commands were not yet delivered. This evidence remains separate from the later 80b8351 check.

## Historical 275406fa console and capture proof

The previously installed source was `275406fac0cb5f8962a55b5d3d65ccd57797a7cd`, tree `6be581787d454928b5d4f48e782e7da5a0333e48`. The executable was installed at `2026-09-16T07:38:42.521Z`: SHA-256 `e66de06e5be24a8fc4c43c003324a1792ac69377c61d9ca3cb1fa6e57bc35baa`, 123,230,336 bytes. Accepted source matches the compiled projection, and the previous executable is preserved for rollback. Machine-local receipt: [installed build](../.artifacts/resume-20260915/final-shared-console16/installed-build.json).

The compiled public check confirmed that Q1 `--menu` stays on Main without starting an attract demo. Before any map, `clear` removed prior output, `condump` confirmed it, and `writeconfig` saved the selected binding in seat 0's namespace without creating a seat 1 file. A screenshot queued before `map` captured Main, then loading and the command tail continued. The run completed 8.306 seconds of controls, progressing demo playback, Main Load, resumed saved-world play, frontend history/replay and normal quit. Saved recipe/client slots matched, save bytes were unchanged, integrity guards passed, and owned processes were reaped. Root accepted the images. Receipts: [runtime proof](../.artifacts/resume-20260915/final-shared-console16/runtime-proof.json) and [visual acceptance](../.artifacts/resume-20260915/final-shared-console16/root-visual-acceptance.json); their preinstallation status fields are superseded by the installed-build receipt.

This build installed frontend utilities and initial menu-attract suppression. This advances T01/T06/T20 without closing those targets or qualifying every native peer. This binary removed the GL depth/blend state cache after the [matched-work comparison](../.artifacts/resume-20260915/gl-depth-blend-elapsed2/RESULT.md) found no net elapsed benefit. No FPS improvement or physical-audio qualification is claimed; broader engine/source coverage remains open.

## Historical 142a1826 lifecycle proof

The earlier installed `142a1826` build passed the mixed local → Q1 demo → menu → Main Load → resumed saved world → frontend console workflow with 35.196 seconds of controls and 119 frames after restoration. Saved recipe/client slots matched and save bytes were unchanged; exit/integrity guards passed, processes were reaped, and root accepted the images. This longer run remains evidence for that build, separate from the historical 275406fa eight-second check and the current Q2 clock proof. Receipts: [runtime proof](../.artifacts/resume-20260915/final-shared-borrower11/runtime-proof.json), [visual acceptance](../.artifacts/resume-20260915/final-shared-borrower11/root-visual-acceptance.json) and [installation verification](../.artifacts/resume-20260915/final-shared-borrower11/installation-verification.json).

## Earlier bounded precache, HUD and d1b437e evidence

- The [instrumented gunner encounter](../.artifacts/gunner-stutter-20260914/precache-pair/receipt.md) retained the same three attack starts and nine grenade events. General precaching changed the first grenade frame from 90.24 to 46.61 ms; the later selected-character correction changed first damage from 143.64 to 69.12 ms. An 81.53 ms step remained. These source measurements do not establish an installed FPS gain or resolve all stutter.
- [HUD readability checks](../.artifacts/hud-readable-values/native-final4/receipt.md) covered Q2 classic normal/compact layouts on CPU and GL, including values 200/1234 at text scale 2 and HUD scale 1.5. Settings used existing bindings, not physical menu interaction. Rerelease compact failed its weapon-selection precondition and remains unqualified by that receipt.
- The [d1b437e compiled check](../.artifacts/resume-20260913/final-d1b437e/runtime-proof.json) loaded a private autosave with Q1 movement and Q3 Sarge on base1, exercised 6.124 seconds of controls, and quit normally with owned processes reaped. It is historical sanity evidence, not current gameplay, FPS or physical-audio qualification.

## Historical 77db74b snapshot

Accepted and installed source is `77db74b89998f8ebb0dc75111e7b0809b6747215`. The capture lifecycle ordering and fatal-cleanup correction is now delivered. Installed tree `ec3cab2baa941854410d6c6bd1ee163ca5df7cfb`, SHA-256 `6a7ff5d136a82a518b31f6963086b3daf768bf8d1ea0257142c734401c880478`, 123,029,632 bytes, mode `0755`, installed at `2026-09-16T00:26:58.682Z`. Root verified the installed receipt and previous-binary backup. Machine-local receipt: `.artifacts/resume-20260915/final-77db74b/delivery-receipt.json`.

The exact compiled public proof used Main → Play → Custom with Q1 classic, Q3 Ranger/weapons and Q2 rerelease monsters, then traveled from start to e1m1. It completed 35.276 seconds of controls, decoded saves showing 1,610 additional frames and 98.912 seconds of source time, and quit normally with all integrity guards passing. Source time includes console/save intervals and is not an FPS measurement. Root reviewed the PNGs. Controlled music PCM evidence below carries forward; physical output and sustained FPS remain unqualified.

Accepted successors preserve console state while changing gameplay profiles, select mounted menu-music fallbacks and Q2 edition/expansion soundtrack cues, retain source-round bot resources and ordered reliable delivery, preserve Q3 peers across round admission, retain effects/audio owners and settle Q3 rounds through the shared actor loop. The server-browser control now stays in the Custom game column. Application restart/input integration is now accepted in `4b25a1c`, with six-client Team Arena warmup, manual/scheduled restarts, held input/fire, restored raw angles, centerview, wire epoch/snapshot ordering, resumed remote movement and cleanup checks. The wire test used `sv_pure=0`; pure admission is not qualified.

Controlled compiled gameplay music PCM passed for Q1 start, Q2 base1 and bot-free local Q3 deathmatch: each measured three roughly three-second windows at music gains 0.25, 0 and 0.25, with nonzero, exactly zero and nonzero PCM. Effects were muted. These results establish gain-controlled PCM, not physical speaker output or exact track identity. The Q3 single-player attempt was invalidated by death and UI input diversion; it does not establish a music defect or fix the death flow. The earlier no-capture source run also had no empty queues across 35.064 seconds/509 pumps.

Source-specific Q3 death policy is accepted: native Q3 uses kill/attack respawn instead of automatic campaign recovery, while tested Q1/Q2 autosave/recovery behavior remains. The invalid Q3 model-registration fallback is also accepted without weakening shared path validation. Persistent-shell integration, demo playback including `startdemos`, broader source parity, physical audibility and sustained performance remain open. The earlier final00 capture failure is corrected by `77db74b`; strict/policy passed, eight native cases passed 164 assertions, and the retained core suite passed 44 tests/495 assertions. Guest-command ownership remains pending: focused tests and strict checks passed, but the local team fixture lacked a CTF precondition on both candidate and accepted baseline; the corrected fixture/rebase is not yet qualified. Earlier wrapper/OCR failures remain retained. No FPS improvement is claimed. Earlier initial bot stalls were finite; navigation improvements did not remove all warmup/frame costs. Machine-local review and evidence index: `.artifacts/resume-20260915/root-review-985-successors.md`. Earlier receipts below retain their original scope.

```sh
/home/buzzkill/Projects/qfiles/quake-typescript --content-root /home/buzzkill/Projects/qfiles --renderer gl --menu
```

Usability is the highest product priority across client menus and console commands. Acceptance requires sensible defaults for selected content, visible current values, direct mouse and keyboard editing, familiar commands, clear feedback, persistent settings and an actual menu/runtime check.

Committed source through `8ec0b014e8340173eba489cd20ff8e0776f4997d` includes five native game/edition presets with campaign selection, startup access to the shared binding table, selected-content defaults, canonical mouse names, readable binding queries, visible slider values, archived settings and shared weapon-selection commands. It also includes ordered `exec`/`writeconfig`, restored scoreboard behavior, QVM append recovery and Q1 music fallback. Existing profile bindings are preserved. Team Arena remains disabled pending its authoritative team-skirmish/progression integration; automatic original `default.cfg`/`autoexec.cfg` startup execution remains open. These updates are included in installed `8ec0b01`.

Actual StartupApplication checks reached Bindings before any world existed, edited and reopened a profile, and passed 58 assertions on each CPU/GL backend. Numeric sensitivity changed visibly from 3 to 10.1, with 15 assertions on each backend. Three actual Application archive/selector cases passed 42 assertions. The final QVM console/gravity check passed full compilation across 1,949 TypeScript files and scoped policy across six files with stable inputs. Evidence: `.artifacts/resume-20260913/startup-bindings-native/receipt.json`, `startup-slider-values-native/native-receipt.json`, `bindings-archives-selectors-native-final/receipt.md` and `qvm-console-gravity-check/receipt.json` under the same artifact root. The earlier `8ec0b01` binary passed four public menu Load → at least 30 seconds of input → diagnostic save → normal exit flows, preserving the source autosaves. Root viewed the actual captures. Startup checks at 960×600 passed binding add/reopen/clear and visible numeric values. The Q2 gameplay run at 640×480 separately verified bare `bind mouse2` query output and an initially empty console. Compiled persistence evidence covers menu reopen and disk writes at normal exit; the separate source test covers profile reopen in a second process. Receipts: `.artifacts/resume-20260913/final-8ec0b01/runtime-proof.json` and `build.json`. Q2 black triangular patches were also visible in the older df5 fixture and remain a visual finding under investigation. No rendering parity, audible-output or FPS claim is made.

The installed `7b8a193` adds settings scrolling, startup control persistence, binding aliases, and the script-drain prerequisite. Fresh compiled startup checks at 960×600 verified scrolling values, bindings, and clear persistence in a second process; root inspected five images. Source CPU/GL settings-reopen checks passed 31 assertions each. The four gameplay flows above remain historical `8ec0b01` evidence and were not rerun for this installation. New Team Arena, console, and startup-script completion work is not installed; original startup-config integration remains open. The in-flight Team Arena test advanced only 250 ms before delayed bot insertion, so it does not establish a routing bug. Receipts: `.artifacts/resume-20260913/final-7b8a193/build.json` and `runtime-proof.json`.

This installation also includes Q3 model-material preload `003521b`, redundant packing-zero removal `b2aeb48`, reproducible monster-table generation `8e53aef`, and scalar model transforms `46d1468`. The transform pair retained bit-exact native color and shadow-atlas output; mean time changed from 59.589 to 59.049 ms while p95 worsened from 88.707 to 92.538 ms, so no reliable speed gain is established. The later QW, Q2, shared remote-content, and indexed shadow-atlas updates recorded below are included in this installation.

The included `5dc48dc` batch completes the accepted shared source-ordering and transactional image-refresh changes, fixes subscription SSE parsing when MIME metadata is absent, preserves Always run preferences, and joins firing/actor-death behavior with death presentation, recovery UI, and supported level autosaves. Full repository TypeScript passed across 1,893 files and policy passed for all 50 changed files, with 1,897 input hashes stable. Native death checks passed 17 assertions on each backend, image-refresh checks passed five tests/111 assertions, and actual Q3 ordering checks passed 102 assertions on each backend. Quiet explosion volume was not reproduced or fixed. The later checkpoint work below supersedes the QuakeC save limitation.

Accepted `a43cd7c` restores full QuakeC checkpoints for the admitted Hipnotic, id1 NetQuake and QuakeWorld programs. Actual disk tests close the original Application, open a fresh one, load the checkpoint and repeat 24 gameplay commands with matching VM and host state. QuakeWorld uses its real native command queue. Restore does not replay spawn or client-connect callbacks. The same commit makes Application world replacement transactional and preserves held inputs, controller settings and audio resources across successful replacement and exercised failures.

Native Q3's `a43cd7c` source checkpoint foundation survives an actual q3dm2 disk save, close and fresh simulation restore with matching rocket, item and door continuation. The newer pushed source `a66729335260e40721cbddd723c9536796cc2023`, tree `d7cdcb2d60000d0eee2d94976eeb8833e6d3ccfb`, joins Application native-Q3 saves and complete attached-bot state through the shared save boundary. It is included in the installed `8ec0b01` executable.

Actual Q3 Application tests run a human and two bots for 80 frames, save to disk, close the original Application and restore fresh, then match 20 continuation frames of native presentation state and RNG. Saved bot clients are reused, extra bots retire, and failed candidate publication closes drafts while preserving the old world and human client. Native local level autosaves now wait for the first completed frame. Q1 rerelease dm4 and Q2 rerelease q2dm1 bot tests each match 12 continuation frames after a 24-frame prefix and fresh disk restore, including bot commands, player state, RNG and source cvar metadata. Application and recovery checks passed 30 tests/198 assertions; the final full compiler covered 1,913 TypeScript files with zero strict diagnostics and zero policy diagnostics across 55 changed files.

Included source `a6cb2e0bf3243c7a09d3cf94c5c9238c05a8ef57` also saves and restores dedicated offline QVM games through the common checkpoint boundary. Actual LRCTF Application tests save to disk, close the original Application, restore fresh without init/connect/begin replay and match 12 continuation frames of VM memory and server state. Failed publication preserves the old guest. The common simulation independently matches native command continuation and validates guest provenance. Full immutable compilation passed across 1,916 source files, with zero strict diagnostics and zero policy diagnostics for the 15-file QVM change. Evidence: `.artifacts/resume-20260913/shared-save-next/common-qvm/`, `application-qvm/` and `qvm-check-final2/`.

Included `25eb5d9` corrects the earlier graphical native-bot crash through zero-axis rocket-item transform handling, plus WP_NONE/CS0 corrections. The exact compiled `a6cb2e0` now passes bounded native Q3 bot save/load: a saved file, completed map load, post-load input and ejected casing, a second save and normal exit. Saved state retains human slot 0 and bot slot 1, advancing frame 254 to 422. The early `restored.png` precedes load completion; the later save/state supplies restoration evidence. Deaths and respawns prevent treating this as continuous ammunition consumption. Proof: `.artifacts/resume-20260913/final-a6cb2e0/runtime-proof.json`.

Newer accepted source adds local two-seat QVM play and disk restoration through `19bf026`, including transactional world replacement between native and guest worlds. The save browser restores saved human seats without treating bots as humans. Actual local restoration checks passed 97 assertions and the inert common restore check passed 22 assertions. Earlier local-play evidence covers 1,150 ms of simulation with 650 ms of input; its separate remote LRCTF run supplies the 30-second evidence. These changes are included in installed `8ec0b01`.

Accepted startup restore `2ef9f519f48f95ee178d1fa5c16aaf627aa24e0f` constructs directly from `initialSave`, retaining the save's state without overwriting its autosave or replaying source startup callbacks. Actual QVM restoration passed 115 assertions; Q1/Q2 checks preserve save bytes and exercise ten movement frames with user FOV 55 and 110. Source bot restoration order was reviewed; the graphical bot failure branch was not reproduced at runtime in this unit. Saved state and current user preferences keep their separate ownership.

Accepted `16a10ec9ee5215ddf85812d2b1b7ef1b9f9d9ca2`, tree `19b7682fe1307bc77087c0fc96f7afca703d6a9d`, carries speaker loop distances through lower-level decoded protocol 1038/2023 snapshots. Four wire tests passed 44 assertions. This does not admit normal rerelease/KEX Application hosting. The final immutable check passed with zero strict and scoped policy diagnostics and stable inputs. Receipt: `.artifacts/resume-20260913/initial-save-wire-check-final2/commit-receipt.json`. These source changes are included in installed `8ec0b01`.

Other accepted source changes add eight shared debug shapes with CPU/GL checks and ten headless native no-op imports in `87ac5b4`, live `cl_timeNudge` in `6040d56`, synchronized Q2 loop-gain limits in `65975de`, and classic/rerelease speaker loop distances in `c7a717f`. The earlier `8084b42` restores missing Q2 dry/water rocket and grenade effects sounds. The original quiet Q3 rocket report remains unresolved: the launch-origin correction is included, actual Application PCM matched 36 queued blocks and input origins were correct, but no audible fix was established. Full rerelease Application and normal rerelease/KEX hosting remain guarded.

Live-network saves, guest bots/travel, native DLL/SO execution, source-native save formats and full campaign recovery remain open. Exact continuation, failure injection and QVM restoration have source-test evidence; compiled native-Q3 recovery retains the narrower scope above. Earlier save evidence remains under `.artifacts/resume-20260913/shared-save-next/`.

Included `af9f412` fixes failed Q3 shader handles and builtin white/default texture resolution, with actual LRCTF CPU/GL checks. The death camera/models, Load/Restart UI and supported level autosaves from `5dc48dc` have native CPU/GL recovery captures in `.artifacts/resume-20260913/player-death-flow/native-capture-summary.json`. Quiet rocket explosion volume remains unresolved.

Recent accepted fixes are included:

- `ef4da27` samples integer time at three Q3 presentation clocks without rounding gameplay time, correcting the exercised startup and firing failures.
- `baa9f85` preserves Q2 catalogue command ownership during input setup, avoiding duplicate registration.
- `ddc06c6` restores default FOV 90. `b7ce186` adds General Punctuation glyph coverage to mounted menu fonts.
- `f5df800` packs shadow positions directly into one Float64Array. All packed bytes and digests matched across 130 actual geometry groups. Paired digest time fell about 24%, and one bounded fixed-step profile fell about 6%. Sustained gameplay FPS remains unqualified.
- `e3ef2f0` restores shared menu sound cues and the global Q2 rerelease `music/track77.ogg` theme when installed. Saved gains and the selected device are preserved.

The installed build includes three further render optimizations:

- `23702ce` reuses MD5 skinning across color and shadow passes within each view. Root passed 11 tests with 498 assertions, and actual geometry matched across 2,864 color and shadow calls. One bounded profile reduced mean fixed-step time from 95.658 to 91.730 ms, about 4.1%; p95 did not improve.
- `c607ded` hashes only casters used by fitted shadow lights. Eight complete actual-scene comparisons, including cached replays, remained equivalent. The exercised scene avoids 65 caster digests covering 33,071 vertices and 1,769,944 SHA input bytes per preparation. No timing gain is claimed.
- `dd6d062` reuses unchanged shader uniform values. Root passed four actual offscreen GL tests with 219 assertions, and final scene color and full shadow-atlas captures matched exactly. One bounded profile issued 95% fewer uniform uploads and reduced mean fixed-step time from 91.728 to 88.362 ms, about 3.7%.

These measurements used separate bounded comparisons. Their percentages cannot be added into an overall performance gain, and they do not establish sustained gameplay FPS.

The compiled `a6cb2e0` Q2 classic and rerelease `base1` recipes used QW movement, Q3 Sarge and weapons, and Q1 monsters. Both fired for 30 seconds, consumed ammunition from 100 to 0 and quit normally. Root inspected the configured, loaded and 30-second attack images. Offhand equipment was not exercised. Proof and build receipt: `.artifacts/resume-20260913/final-a6cb2e0/`. Earlier compiled receipts retain their dated scope.

Lighting, HUD and full fidelity remain open. The investigated beam patch did not justify a renderer fix: classic uses an authored dark lightmap, and the rerelease opaque beam blocks an authored cone light. Evidence: `.artifacts/resume-20260913/q2-beam-patch/next/report.md`. The dark-impact donor/material comparison likewise did not establish a defect or justify a fix. The current build includes accepted material identity/admission and bounded native source ordering with CPU/GL checks; broader ordering and material fidelity remain unqualified.

The historical menu-audio proof remains bounded to its tested source. The installed engine now uses the 200 ms startup queue correction described below. Separate native menu checks exercised real input events, decoded music and cue PCM, mute and unmute, saved gains, and device release before game initialization. Those checks and the compiled runs used dummy audio output; audibility through the user's speakers remains unverified. An earlier 30-second trace of the `379086a` source covered 320 frames with dummy audio; this is not a measurement of the latest installed executable. A rare 192 ms pump interval exceeded roughly 137 ms of queued coverage, leaving an approximately 53 ms gap. A cold frame queued two seconds of audio; this is queued audio duration, not two seconds spent mixing. Audible fidelity and overall FPS remain unqualified. The compiled checks do not establish universal renderer parity or HUD fidelity; the bounded classic-lighting comparison below retains its seam difference.

This build also includes explicit dedicated offline id1 and Hipnotic QuakeC admission at `5f760f3`, native monster-damage accumulation across save and restore at `dcb4da6`, and supported Hipnotic QuakeC weapon inventory at `708417b`. These bounded paths do not establish general QuakeC or full native-mod compatibility.

Earlier HTTP/timer yielding, OAuth response flushing, quoted binding semicolons, the provider model picker and reasoning effort, and the Q2 `oldFrame=-1` correction remain included. Prior loopback provider checks do not establish live ChatGPT account connectivity, which remains unverified. The engine remains incomplete; this update does not re-audit the 477 requirements or change their counts.

Also included in this installation, `7807426623c8c61ac28ab84a7a7181187bde4b51` culls irrelevant MD5 shadow bodies before skinning. Root passed seven focused tests with 100 assertions and a separate actual GL test with four assertions. Complete shadow and color buffers matched, and one bounded profile reduced mean fixed-step time from 84.230 to 79.041 ms, about 6.16%. Accepted `22137b6b93f00d50dfd6014cd2eb073bbd154139` reuses unchanged brush-model lightmaps; five retail cases matched the forced-rebuild image and frame results. No brush timing gain or overall FPS improvement is claimed. Full donor lighting parity remains unqualified.

Accepted source `2150599b49163b8eac5812f8bedfcd0fd593e4ef` retains shadow casters spanning a spotlight axis. Root passed eight CPU and GL tests with 66 assertions, including the corrected shadowed receiver, outside-cone rejection, and cache reuse. Accepted `379086a1a923f57fd9183b299ed1152b93dbc3d9` uses shared Q2 rerelease lighting modulation 2 for classic content. The required `../quake-2-re-ts` donor at `0d73750` uses that default for classic content too; the earlier archival donor with modulation 1 was the wrong comparison basis. Root passed the retail world and external-brush test with four assertions. The fixed default frame exactly matched the diagnostic modulation-2 frame, and 99.99967% of pixels were within two channel values of the required donor world-only classic-lighting capture. One floor-edge seam outlier remains; this donor comparison is not pixel-exact. These are bounded lighting and shadow proofs, not full renderer parity. Both fixes remain included in the installed executable.

The installed executable also includes these four source units:

- `de838c7` borrows ordered readers from existing mounted archive owners. Twelve readers opened no additional archives and performed no additional archive hashes. Root mount tests and two actual cross-edition Q1/Q2 weapon-launch cases passed, the latter with 755 assertions. No end-to-end loading-time gain is claimed.
- `fe229b0` caches immutable world shadow geometry. Focused cache tests and eight actual native frames retained exact color, full-atlas, and command equivalence. An existing saturated color-ratio assertion failed identically on the baseline and candidate; that suite was not wholly green at this checkpoint.
- `4fe1c6d` clamps CPU fragments before blending to match normalized GL targets and makes the color-ratio fixture explicitly unsaturated without changing its thresholds. Root numeric and CPU checks passed 16 tests with 282 assertions; the broader real-world, material, and shadow checks passed 17 tests with 257 assertions.
- `c07d296736e53f502dcf0618fb9ada368d9f0a03` caches decoding for four menu images and gives each caller independent pixels. Root UI checks passed nine tests with 119 assertions, with one opt-in SDL test skipped. In one actual Play transition, menu-art preparation fell from 901.514 to 9.857 ms. Returned-menu pixels matched exactly, and both native runs exited normally. Loaded frames were visually reviewed; their first simulation steps used uncontrolled wall-clock timing, so no full pixel-parity claim is made. A remaining maximum audio pump interval of 820 ms is unresolved.

The installed executable includes these three additional source units:

- `6e858cc` precaches effect skins and materials before gameplay. Root passed five effect-cache tests with 26 assertions, including failure and lifetime behavior. One 30-second artifact comparison moved an approximately 80 ms effect-resource stall into loading, with gameplay resource work at most 0.080 ms and about 2.105 seconds added to loading. The final source includes later type, freeze, and lifetime changes beyond that measured artifact. That effect-precache unit did not change queue policy. The subsequently accepted startup correction now limits the initial queue to 200 ms.
- `81eb805` reuses GL packing buffers. Root passed two actual GL and byte tests with 402 assertions. Four color and shadow-depth captures and 5,813 packing calls covering 13,230,732 bytes matched; backing allocations fell from 40,691 to five. This is not an FPS claim. A whole-world command-hash difference remains unresolved; swap-only hashes do not prove world equivalence.
- `ec8fd304c263bc3041d1d72a5785510346c84be0` reuses mounted archive owners for scoped content providers. Root passed all 13 mount and Q3 client-content tests with 514 assertions, including actual retail pure admission. Mount shapes matched, and a paired trace avoided 12 archive opens and six hashes. No end-to-end loading-time gain is claimed.

Accepted Q3 media warmup `3648f289d71f49f9ee2a015b08e33f97a64a9071` and startup queue correction `512b9295cc0bf4d36b841aef8b81af3c00171652` are now included in the installed executable. Independent Q3 checks passed eight tests with 46 assertions; startup PCM, Vorbis, lifecycle, and bootstrap checks also passed. A combined 30.056-second native dummy-output run covered 330 frames, 216 play calls, and 2,523,240 nonzero PCM samples. The first queue held 200 ms instead of two seconds; the next pump arrived after 166.89 ms with 37.46 ms still queued. Q3 effect construction ran during loading for 485 ms. First-event effects preparation took 12.87 ms versus about 143 ms in the earlier candidate; first-use model materials still took 9.34 ms.

Neither later empty-queue observation proves an audible gap. Row 51's queue-only shortfall of 21.664805 ms is below the obtained 1,024-frame output block of 23.219955 ms, which SDL's queued-byte count excludes once handed off for playback. Row 212 likewise does not prove a gap. These checks do not establish gap-free playback, physical speaker fidelity, or universal FPS.

Accepted source `4477db29f195080cf73cbfceb66d1e543a30f9fc` projects each opaque lightmapped vertex once while retaining separate draws. Root passed all eight material tests with 166 assertions; targeted retail lightmap and offscreen CPU/GL world checks also passed. No FPS gain is claimed. This change is included in the installed executable.

Accepted source `2662c66e64142c4d7a40c201d2cf17c877f55214` retains resolved uniform locations in program-owned records. Root passed the native uniform-state test with 154 assertions; frozen native checks passed eight tests with 442 assertions. All 32 captures and the actual final color and full shadow atlas matched. In one isolated pair based on `ec8fd30` plus the eight audio files, excluding later legacy projection reuse, program-use time fell from 7.400 to 4.049 ms and location lookups fell from 916,651 to 84 across 16 warmup frames. Whole-frame mean fell from 68.631 to 63.522 ms, but changes outside the edited span prevent attributing that entire difference to this unit. No sustained FPS gain is established. This source change is included in the installed executable.

Accepted source `9683d9dc9a153f00515c27f6e370521f61527a7a` caches immutable local MD5 UVs, positions, and normals within the same view; shell extrusion remains separate. Root passed all 11 model tests with 1,087 assertions, including retail resources, cached-versus-uncached results, four interpolation values, shell isolation, bounds, and attachments. Retained-shadow tests also passed; opt-in CPU/GL checks were skipped. No FPS gain is claimed. This source change is included in the installed executable.

Accepted source `58a3448bbac8c35842d6be5a97881517ebcb8439` reuses model-lighting yaw rows and directions once per entity, retaining the numeric expressions and the required rerelease TypeScript donor behavior. Root passed the retained test with 34 assertions; retained family-lighting checks and actual CPU/GL model captures also passed. No timing gain is claimed. This unit is now included in the installed executable. The ceiling-object identification is resolved below.

The frozen `9683d9d` ceiling probe identifies the tan detail as static world-model-0 faces 6047, 6270, and 6044 using `e1u1/broken2_3`. The globes match authored `target_explosion` positions with model scale 1; recorded models and events do not support Scrag or machinegun-fire causation. This establishes object provenance, not donor pixel parity for lighting or animation. Evidence: `.artifacts/resume-20260913/classic-ceiling-effects/report.md`.

Accepted `820173d068c92dfb9e735a39d43d0df82029b6f4` retains server-selected ASCII QuakeWorld mod directories through the common catalog, with mod → qw → id1 lookup, case-preserved writes, shared skins, travel, cancellation, and reopen behavior. Root passed 11 native client/download and catalog tests with 187 assertions. This does not qualify retail peers, arbitrary mods, or full hosting. Accepted `751193f85d6a8abfffed6d818eab6531fade72ce` replaces a synthetic Q2 warmup pose with the shared renderer’s `preloadModel(asset)` call; three existing failure/retry tests passed with 12 assertions. Both changes are included in this installation.

Accepted source `33026a68ebf6cec4b7fb40e41ee78d7d0d1706a5` submits shadow-atlas geometry through indexed GL draws. Actual color and full 2048² shadow-atlas bytes matched; 72,573 immediate vertex calls and 6,516 begin/end pairs became four indexed draws with 72,573 indices. Root passed the focused native test with 17 assertions; broader native checks passed 11 tests with 303 assertions. Paired mean frame time fell from 58.993 to 56.813 ms, but median and p95 worsened, so no general speed or FPS gain is claimed. This source change is included in this installation.

Accepted source `18bedc4bb83621676cef289494c588c5d488c7f9` shares remote content selection and a mounted owner before world creation across QW, Q2, and Q3. Stock metadata, base lookup chains, case-preserved roots, independent active-world ownership, same-map refresh, and cancellation are retained. Root passed actual QW mod admission, Q2 UDP directory transitions, Q3 non-base package admission through real QVM and authentic pure checks, and catalog/base cases: five tests with 103 assertions. Q2 retains one HTTP queue and baseq2 player assets; Q3 uses selected/base physical prefixes and preworld packages. This does not expand arbitrary-mod QVM or wire ABI support; the Q3 proof used known baseq3 modules and game types 0–4. Configurations remain owned by the launch base. The unit is included in this installation.

Accepted source `729b1dde51a0fcebdb5a269f91e042b24dda1fd7` binds cgame entity-token and PVS queries to the existing loaded Q3 world owner. Root passed five tests with 3,196 assertions after reviewing the donor token, leaf, and visibility behavior. The known-baseq3 admission gate is unchanged; this does not qualify broader QVM or mod compatibility. These bindings are included in the installed `75d26d8` executable.

Accepted `077c73452384605231cc2391c0ed59afa0656801` shares browser polling while preserving borrowed application ownership; root passed two hidden-window, dummy-audio, real-UDP checks with eight assertions. Accepted `e76943bada4997cbf12821fc5e7aa3e044727f30` tracks discovery requests independently; root passed 11 focused tests with 80 assertions and retained browser sort/persistence checks. These changes are included in the installed `75d26d8` executable. Existing admission gates remain unchanged; this does not qualify the full Q3 browser, native vanilla peers, or general mods.

Accepted source `5258ee0d499909f8bc2bd6cd00f705f959fb1c8a` binds all 18 Q3 UI LAN calls to shared browser ownership, master discovery, cached views, and independent request state. Root passed 31 tests with 467 assertions; a separate known-baseQ3 guest travel check passed with 41 assertions. Scope retains IPv4, JSON cache storage, and existing native admission gates. The bounded LRCTF follow-up below resolves the earlier UI trap 71 blocker without changing production admission gates. This unit is included in the installed `75d26d8` executable.

Accepted source `1aa524d29d614ad035c5a2d4b8de08bb86f021f7` reuses browser reads while preserving favorite updates and polling invalidation. Root passed all 19 persistence and QVM-browser tests with 254 assertions. This source change is included in the installed `75d26d8` executable.

The LRCTF experiment loaded UI API 6 and cgame from the actual mounted `pak02.pk3`, completed UI71 cache dispatch, initialized both modules, and reached one active frame with no recorded failure. Root inspected the 160×120 world, weapon, and HUD readback. The artifact bypassed the known-baseq3 gate and used a baseq3 server advertising `fs_game=lrctf` with `sv_pure=0`; production gates remain unchanged. This does not qualify LRCTF qagame/server rules, pure mods, full gameplay, missionpack, or general unknown-module compatibility. Local evidence: `.artifacts/resume-20260913/lrctf-module-probe-browser/report.md`.

Accepted source `d5ca68f490de8789baa33b69b63c80c5c4d493f3` admits parsed, previously unknown Q3 client bytecode through normal production execution. It supersedes the source admission restriction described in the earlier browser and artifact-probe receipts above; those receipts retain their original scope. Known wrong-product/role rejection, actual UI API validation, pure filtering, explicit unbound-service errors, and module lifetimes remain. Root passed 33 QVM and mount tests with 568 assertions. Normal production loaded LRCTF UI/cgame from mounted `pak02.pk3` with API 6/ABI 4 and ran 415 frames over 30.060 seconds of wall time, about 20.75 simulated seconds, with authoritative movement of 317.78 units and ammunition 100→69. Root inspected the 320×240 frame; each module retired once. Known-base pure travel also passed. This used a baseq3 server advertising LRCTF with `sv_pure=0`; that client-only proof did not exercise LRCTF qagame/server rules. Full mods and missionpack remain open. This source unit is included in installed `75d26d8`; the client gameplay evidence above is source-run evidence.

Accepted Q3 server prerequisites are `babe71b`, the optional shared session async step; `fae018f`, common and filesystem services shared across QVM roles without compatibility aliases; `57083ac`, ordered awaited Q3 callbacks and lifecycle cleanup; and `dded3ea`, the server guest ABI with borrowed records and shared collision. Root passed the combined server-ABI and guest-record checks with 43 tests/522 assertions; the combined scoped check reported zero strict and policy diagnostics. The callback worker also passed actual known-BASEQ3 pure admission and q3dm1→q3dm2 client travel with 41 assertions. Evidence: `.artifacts/resume-20260913/q3-guest-combined/receipt.md` and `.artifacts/resume-20260913/q3-server-await/receipt.md`.

Accepted source `a8a842fa4415267513d77d5632f3dd600504cce7`, tree `ba9241ba8db7ce07ddb547ef99229b93d9afa099`, composes dedicated selected-Q3 qagame with the shared scene, body table, clock, mounted content, and network. Earlier accepted `1eb7387` supplies shared awaited commands. Actual LRCTF qagame, UI, and cgame ran together with `sv_pure=1` for 30,037.81 ms wall time and 13,650 ms supplied simulation time. Movement reached 171.5947 units, ammunition fell from 50 to 0, and all three VMs retired after disconnect and awaited shutdown.

Root passed 22 tests/192 assertions and the existing known-BASEQ3 pure client travel check with 41 assertions. Combined strict compilation reached 1,368 source/declaration files with zero diagnostics; scoped policy also reported zero. Evidence: `.artifacts/resume-20260913/q3-guest-integration/witness-final/result.json` and `.artifacts/resume-20260913/q3-guest-final/root-verification.md`.

This accepts bounded dedicated LRCTF server execution. Bots, local seats, mixed mod providers, saves, restart, and guest-server travel remain unsupported. Native DLL/SO execution and full mod compatibility remain open. The primary material-ordering correction below restores the flash over impact marks; remaining image differences are not qualified. This source is included in installed `75d26d8`; the sustained LRCTF gameplay evidence above remains source-run evidence, separate from the compiled dedicated startup check.

Accepted `75d26d87ae4d1aca2a123354f9c47fcbc49ee37e` shares material-group finalization across Q1/Q2/Q3 views before CPU or GL submission. Authored primary sort places impact marks at 4 before explosion stages at 9, preserving multipass groups, render state, and the legacy draw subsequence. Final strict compilation passed across 1,885 source/declaration files, with zero policy diagnostics on the 18 changed files. Separate opt-in native checks passed 10 tests/282 assertions. The Q1 menu-fixture failure reproduced on the baseline; repaired CPU/GL cases passed two tests/40 assertions.

Actual LRCTF CPU and GL captures each passed 102 assertions over 60 ticks/61 frames and 3,050 ms supplied simulation time. CPU marks occupied draw ordinals 10–74 before flashes 75–94; GL marks occupied 11–88 before flashes 89–110. Root viewed the baseline and both candidate images and confirmed the restored flash. Different live positions and material counts prevent paired pixel-parity or FPS claims. At this historical checkpoint, dark impact coverage, the missing radar asset and native secondary ordering awaited follow-up. The current findings and accepted ordering/fallback changes are recorded above. Evidence: `.artifacts/resume-20260913/lrctf-visual-review/candidate-report.md`. This source change is included in the installed `75d26d8` executable.

Accepted source `4a85967cd155b5b23ca8c8c420b750c6cdc01ed7`, tree `6147af618218857dc3c714f2e8f9ae672b132124`, retains scene-wide shader identities across providers and image reloads, original refentity/poly slots including RT_POLY, and mixed-frame admission metadata. Expected image-replacement rejection now uses existing discard and settings-restoration cleanup. Full strict compilation passed across 1,886 files with zero policy diagnostics on 23 changed files. Local CPU/GL console/menu image refresh passed four tests/92 assertions; a separate actual remote Q3 refresh preserved the connection, and root cross-game effects checks passed two tests/18 assertions. This is a metadata prerequisite: the primary comparator is unchanged, while native RT_POLY error/view gates and complete shader/entity/fog/dlight source sorting remain open. Evidence: `.artifacts/resume-20260913/shared-material-order/secondary-combined/verification.md` and `secondary-review.md` in its parent directory. This source is not included in installed `75d26d8`.

## Historical execution records

All entries below retain their original source cutoffs, counts, and installation claims. Their references to the installed binary describe those historical checkpoints, not the installation above.

## Accepted world-text controls, source movement and guest preparation

Source cutoff: `e5e51456d04a911528da8b52af9a50b794738318`. Accepted `200493b6` joins live `gl_debug_distfrac` to common world-text culling; root passed CPU/GL two-seat checks, 6 tests/82 assertions. Accepted `ef4d34d1` joins the actual native DLL's two world-text imports to the common store, root 1/16. The other eight debug-shape imports remain open.

Accepted `e0409dc0` routes the exercised native QW commands through the shared ordered command phase, root 5/206. Spectators, pause, complete administration and arbitrary mods remain open.

Accepted `98074c6` corrects native authored Q1 soldier/dog gib and head construction using existing helpers in source order. Root passed 14/301 and the actual dedicated rerelease e1m1 witness: heads clear ground, retain source bounds and signed spin, fly and remain alive at 23.5 seconds; gib pieces accelerate, bounce, settle and expire. Rerelease corpse-extension collision policy remains open.

Accepted `3176aee` joins selected Q2 source movement profiles and native server configuration publication. Root actual authored bio/base travel checks passed 1/68. Rerelease remote-client admission and matching native prediction remain missing; local source/profile and decoded configuration checks do not qualify that remote path.

Accepted `e5e51456` prepares the native Win64 Q2 guest source lifecycle, root 7/109. Complete bindings and normal application guest admission remain open. This does not turn the accepted world-text imports into full native-mod compatibility.

The finite ledger review changes only the reasons for `q1.physics.rerelease-gibs-corpses` and `q2.rerelease.q64-movement`; both remain open. All 477 verdicts/methods and the other 475 records carry unchanged from `7964fc31bfa186e538994eea3ed71cb6b8d035f6`, whose verdicts carried from `39ea4c0cab9910c6b78d5f8d18d3bc36494dd519`. Counts remain 288 Done/189 Not done, with no new full audit. The installed binary remains `3a073e87ec7a4126f396e15833b8afb9df569403`; no rebuild is claimed. The delivered [project-status snapshot](project-status-current.md) retains its earlier `98074c6e` cutoff.

## Accepted native QW hosting and shared SOCKS clients

Source cutoff: `7964fc31bfa186e538994eea3ed71cb6b8d035f6`. Transport `7b2d38b` and QC messages `d4b9f8c` precede shared native QC `60bbc638` (root 9 tests/718 assertions, including actual source execution and NQ retention) and dedicated QW host `88f84c10` (root 3/132 across source/host/transport). An independent donor protocol-28 client exercised firing (shells 25 to 23), authoritative movement (x 528 to 434) and travel from `e1m1` to `dm2` with servercount 2; evidence is in `/tmp/qw-host-donor-peer.log`. The accepted host uses base protocol 28 and the pinned CRC-54730 QW program, without spectators. Local graphical QW hosting, arbitrary mods, other commands/admin, master/rcon and full parity remain open. Later native-QW command work is outside this cutoff.

Accepted Q3 print `3d6589c5` restores native message-body reading (root 1/9). Shared SOCKS `7964fc31` connects the existing transport before native RemoteApplication sends, using archived/latched settings from the existing client configuration owner. Root passed the exact five cases/55 assertions: authenticated Q2 CPU signon, travel on the same association, reopen/settings, plus NQ/QW/Q3 native handshakes and decoded replies. Cancellation and proxy-loss checks use controlled loopback endpoints; configured proxy loss cannot silently send directly. Evidence is in `/tmp/root-socks4-final.log`. These tests do not establish GL, physical-network or all-family full-play qualification. Browser/listen-server proxy configuration remains separate.

Four existing reasons are narrowed; no verdict changes. Counts remain 288 Done/189 Not done across 477 unique IDs, with the other 473 records carried forward. The installed binary remains `3a073e87ec7a4126f396e15833b8afb9df569403`; no executable rebuild is claimed.

## Accepted audio devices and world-text culling

At source cutoff `39ea4c0cab9910c6b78d5f8d18d3bc36494dd519`, accepted audio `0065d73b` passed root 31 tests/263 assertions: actual dummy SDL queued PCM/clock/music retention, CPU/GL menu/restart, and native NQ remote named-device travel from `e1m1` to `e1m2` in the same window. Device names/default selection and device/effects/music settings persist per product through ConfigStore. Output remains 44100 Hz, stereo, 16-bit. Physical audio quality/performance and injected remote failure races are not qualified.

Accepted world text `39ea4c0c` passed root 6 tests/60 assertions through CPU/GL units and actual two-seat Q2 rerelease `base1`; root inspected images from both changes. Shared culling uses the native strict condition `cellSize < dot(origin-camera.origin, camera.axis[0]) * distanceCullFactor`, with producer default `0.004`, signed forward distance and rejection before glyph lookup. Native `size * 8` remains. Live `gl_debug_distfrac` control and guest debug imports were unjoined at this historical cutoff; the later control and two world-text import receipts appear above. Both frozen source units passed scoped strict/policy checks.

The two reviewed ledger requirements remain open: configurable output formats/complete source audio workflow and the full display/video restart workflow are not established by these receipts. Counts remain 288 Done/189 Not done across 477 IDs; the other 475 records were not re-audited. The installed binary remains `3a073e87ec7a4126f396e15833b8afb9df569403`, without a rebuild.

## Accepted NQ profiles and device persistence

Accepted `5a0e1848` supplies classic-id1 host protocol selection and shared-client negotiation of NQ 15/666/999, limits, alpha/scale, movement/travel and version flags. Root passed 9 tests/209 assertions and strict/policy checks; independent donor servers exercised signon and authoritative movement over all three protocols through the hidden shared client with no local world. Root inspected three images and donor-codec entity updates of 9/15/21 bytes matched inside actual UDP payloads. Rerelease, expansion/mod admission, private extensions, full bidirectional qualification, static visual-field production, nondefault lerpfinish and a menu protocol selector remain open.

Accepted `36f395ee` (tree `2358be0ff3ec42658264ccc02017c372f4657371`) supplies shared device/tuning persistence: root 8 tests/99 assertions and strict/policy checks covered two-seat restart, serial/explicit-none choices, keyboard fallback, custom bind remaps 42→77→88, gyro profile fallback, mouse/history and haptic enable/strength. That 8/99 total includes remote CPU/GL checks (2/60). Physical hotplug remains unqualified; identical GUID/ordinal devices without serials cannot be disambiguated after reordering. Only device persistence becomes Done/shared-replacement; counts are 288 Done/189 Not done across 477 IDs. No audio acceptance or installed-binary rebuild is claimed.

## Accepted native QW remote admission

Accepted `66e6136` joins native protocol 28 `--connect-qw` with default qw/id1 mounts, challenge/connect lists, sound/model download admission, checksum2 spawn/begin, shared Q1 presentation and prediction history. Root final checks passed 4 tests/46 assertions; earlier receiver/prediction/application checks passed 8/123. An independent TypeScript donor loopback server exercised retail start signon, CPU rendering and authoritative movement; root inspected its screenshot. Strict/scoped policy passed on snapshot `qw16-WMnsLt` (source `8b4ee3e6c1f4dc3f251a5e0f7daa9125bf606ae8419d92cb5e9f721e6e31684d`, accepted tree `c8c1a6ce7e93089bca0ebcf38ecb26d2534a9534`).

Scope is base Q1 aliases and one remote seat. Spectator/extension profiles are rejected; other-player two-pass/fractional prediction, full effects, demos, hosting and split-screen remain open. The subsequent accepted skin work and its remaining policy limits are recorded below. At that earlier cutoff counts were 287 Done/190 Not done; current accepted counts are given above. The installed binary remains source `3a073e87` with no rebuild.

### Accepted skin and identity follow-up

Accepted `0c0499c3` joins shared indexed MDL skins/colors and proper PCX source cropping while retaining native geometry and MD5 fallback (root actual CPU/GL Q1 rerelease and Q2-map checks: 6 tests/68 assertions). Accepted `066ab042` joins QW application skins and saved pre-connect name/skin/colors/rate (root 6/106). An independent protocol-28 donor server verified an exact 913-byte skin, live colors 2/9 without a kick and authoritative movement; root inspected screenshots. Accepted `a8f9e2f` forwards local Q1 source colors through two-seat update/save/load (root 1/5); this is source-setter evidence, not an editable local color-console route or visible local-color proof.

At combined cutoff `a8f9e2f` (tree `644449d48b72b9be7592f1047cf92b5c4faa6342`), editable local setup/color console and cross-source appearance ownership remain open. QW downloads remain limited to base qw/id1; arbitrary mod gamedirs and application server-host download denial policy remain unjoined. Prediction, hosting, spectator, split-screen and demo limits above remain unchanged. The subsequently accepted NetQuake update is recorded above. At that earlier cutoff counts were 287 Done/190 Not done; current accepted counts are given above. The installed binary remains `3a073e87`.

For the item-by-item implementation Done/Not done list, see [completion status](completion-status.md). It uses the fixed accepted source cutoff recorded there, separately from formal verification acceptance and installed-build status.

Finite capture review at source cutoff `a5c4da3` moves `q2.capture.screenshots` to Done/shared-replacement and narrows `q3.recording.screenshots-levelshots` to remaining recording-clock/FPS-timescale/restart work. The ledger now records 281 Done and 196 Not done (Q2: 109/193); all other verdicts carry from `27c17a9`, without a new inventory-wide audit. The priority appendix removes the completed Q2 row. Multi-seat and resized-window screenshots remain an explicit unexercised verification case, not an identified capture implementation gap.

Accepted `fc0c15e` joins shared screenshot/levelshot commands to active application capture; `3f6be42` adds shared world-text storage and CPU/GL scene submission. Final frozen source `9c9bea07c94ee1dfae8ad1c591e31f87107e397deb01e28984a18ef7cc43c719` passed strict TypeScript and policy checks. Root independently executed 17 world-text tests/144 assertions, including the separately enabled GL unit, and retained unchanged capture checks (5/77) and core/config checks (22/266): 44 distinct tests/487 assertions. The final three-file correction keys zero-lifetime text to the authoritative source frame. Root inspected actual two-seat CPU/GL images with legible text at the intended world locations. This used controlled source-entity spawning on retail `base1`, not proof of naturally authored campaign text; background lighting differs between renderers, so full-frame parity is not claimed. Guest debug imports and donor distance-culling remain unfinished; depth behavior was tested. Local prompt checkpoint `d07700b` passed an actual application proof with 25 assertions and CPU image review; private native remote prompt transport remains unsupported. These receipts do not change the 477 requirement verdicts. The installed executable remains `3a073e8`.

Accepted `7b7110e` extends shared cvar commands while preserving Q1 custom-variable promotion/defaults; `8637652` adds verified parallel HTTP ranges. Root passed the combined cvar/input/range/HTTP suite (60 tests/557 assertions), existing common network checks (8/49), and actual ordinary/range-enabled remote-shell probes. The range probe downloaded a 1,991,612-byte PAK using one HEAD and four GETs at peak concurrency four, with no whole GET, matching installed/mounted SHA and native WAL fallback; the session retained no local authoritative world. GIF checkpoint `e05343b` passed root installed-image checks (17/139, no skips) and existing cinematic/world checks (25/417), including CPU/GL world movies. Original/captured frames and a matching CPU/GL pair were inspected; blanket renderer parity is not claimed. Exact frozen strict/policy checks passed for both source batches. The extraction baseline/manifests and 477 requirement verdicts are unchanged; local prompt work was still in progress at that checkpoint; its later acceptance is recorded above. The installed executable remains `3a073e8`.

Accepted source `936d475`/`ab0f90a`/`1fa5d62`/`9356b07` joins user-content overlays, contained QVM writes, shared audio commands and download ownership checks. Root passed storage 19/134, audio 2/78, actual VM travel 1/23 and shared sink 1/5; isolated strict/policy passed. Q3 application download/settings checkpoint `2b025db` passed the full source application file (4 tests/76 assertions), retaining the existing rate default of 25,000. HTTP checkpoint `30f824f` adds Q2 package discovery/reload, PKZ mounts and HTTP transfer with native fallback. Root passed 17 tests/168 assertions, reran the typed HTTP service (5/25, overlapping that suite), and ran the actual remote shell through a downloaded PAK map, HTTP WAL 404/native UDP fallback and rendered output with no local authoritative world. Final strict/policy passed on source `c5b8d2271999ed6bab1f1720c5f2bca51313aaeafb655d9a81d449cc8d04596e`. These bounded checks do not qualify every server, transfer failure or mod. The installed executable remains `3a073e8`; completion verdicts are unchanged.

Checkpoint `f7e331b`, with picture registration `93aed0e` and native ammo sentinels `9c89418`, joins stock Q3 client VMs to the shared application services and input path, including awaited guest shutdown before orderly travel/close invalidates the old generation. Root independently passed the exact CPU application files (5 tests/105 assertions) and the actual GL remote-client fixture (1 test/23 assertions). Strict/policy passed on the tested source; the final snapshot `e58928ef3c1fe10935c33d24a7334268bc47eaff38633edae8088a60ad50fb20` differs only in two reviewed software-renderer branding strings. This is bounded stock pure-client execution evidence, not arbitrary-mod, retail-peer or complete client-trap compatibility. Application download followthrough remains separate work. The installed executable remains `3a073e8`. Accepted gyro core/UI `221547a`/`27c17a9` passed root 22 tests/237 assertions plus the separately enabled SDL menu check (1 test/5 assertions); physical-controller qualification remains unclaimed. The finite gyro review changes one requirement verdict to Done: the completion inventory now records 280 done and 197 not done, with other verdicts carried from its stated prior cutoff.

Checkpoints `e46e924`, `ad0375d`, `1af767c` and `080c829` add native Q2 discovery, exactly-once remote Q1 sound delivery, native Q3 remote clients/content checks, and startup server browsing. Root retained the unaffected passing cases from the combined browser/client suite and independently reran the corrected Q1 file (4 tests/124 assertions). Final strict and policy checks passed; native retail-peer compatibility remains unqualified.

Checkpoints `1b50a46` and `11e6098` add asynchronous QVM client calls and common engine trap services. Root independently passed 36 tests/333 assertions; corrected strict/policy checks passed. Checkpoints `7c44237`, `47b358d` and `89d5c20` add shared script preprocessing, cinematic traps and staged Q3 downloads. Root passed 30 tests/436 assertions across the extension suite, including retail cinematic, CPU/GL movie, bot/parser and download checks, then rechecked the corrected cinematic API (4 tests/20 assertions); these overlapping counts are not added together. Final strict/policy checks passed on `c45f35493cfdccf9270b0ba490cae60dda3523ec5a573bafca5136a688498b91`. At that checkpoint, stock pure-server VM application/input integration remained uncommitted; the primitive-only evidence did not establish complete VM initialization or pure-server support. See the later accepted application checkpoint above. The installed executable remains `3a073e8`, and requirement completion counts are unchanged.

Preceding checkpoint `508b2be` adds a native NetQuake protocol-15 remote client, shared full-character opacity and host settings; retail-peer compatibility remains unqualified. Accepted source checkpoints `df44a12` and `af49a69` add authored material lighting across CPU/GL rendering and native protocol-34 Q2 client asset downloads with cancellation-safe loading. Root independently passed material checks (22 tests/278 assertions) and download, storage-integrity and existing remote-frontend checks (12 tests/123 assertions). Final isolated strict and policy checks passed on source snapshot `347dce8ec02c43d25655e4ca8f3611a39bde7dfbd5eafc95937c9ba48f3e98c2`; final type-only corrections retain that runtime evidence. This does not establish rerelease-wire or retail-peer compatibility. The installed executable remains `3a073e8`; these source checkpoints are not installed. Requirement completion counts are unchanged.

Implementation is authorized through the full engine until the user explicitly stops it. This ledger records the lead's dispatches and handoffs. Historical planning-only source assessments remain historical evidence. The current canonical graph contains 74 packages and 303 acceptance dependency edges after the user-directed code-first sequencing change.

The implementation phase started from `6439acd6c5f0d38ca93867ae0f6100ecf4cdbee4`. Checkpoint `1fbc44b` adds shared attached-body transport and save format 2. Checkpoint `7521ab4` makes shared body ground authoritative and shares Q3 gauntlet/lightning execution. Root checks cover foreign support, cleared support, a retail moving platform carrying a player/item/foreign grenade, suspended-item preservation, native/selected attacks, reflection and saves. Checkpoint `83b4dfe` shares Q3 shotgun execution and source impact effects; actual native/selected fire, Team Arena reflection, binary saves and shooter-release cleanup pass. Checkpoint `db11bbd` shares native and selected Q3 machinegun/chaingun execution and preserves actor identity through Q3 trigger/impact dispatch. Checkpoints `b078c5c`, `11eab2e` and `2a434c4` add shared Q3 mover, damage/use, radius/spawn and target-laser observations; `368e179` fixes Q1 CTF program admission. Root checks cover real foreign barrels, native Team Arena reflection, LMCTF rules, selected bullet saves and contact ordering. Checkpoint `3d59ed6` adds shared grapple weapon slots; `a036455` fixes semantic anchor lifetime and `39ba6f8` fixes Q3 brush collision roles. Earlier `edb5b35` adds selected offhand grapple execution. Earlier checkpoints `e3437e2`, `f8999c4`, `c6c25b6` and `0ac29b3` supply shared offhand grenades, native primary handoffs and explicit map-content identity. The executable installed in `~/Projects/qfiles` now contains verified source through `3a073e8`; see the [installed build record](#installed-build). W03 remains accepted only for its recorded early tooling scope. Other package states record work underway, not full acceptance. The diagram records acceptance dependencies and cannot supply a completion percentage.

Checkpoint `69f33f9` shares Q3 rail execution; `0cbad5d` shares radius damage and preserves projectile provenance. Native/selected attacks, reflection, temporary rail unlinking, binary saves and source effects have bounded checks. Checkpoints `c74d205` and `cb89c28` attach selected Q3 world weapons and preserve Q2 SKY surfaces carrying NODRAW. Root inspected CPU captures from the production scene composition. Checkpoints `b640bb4` and `5a1d7a6` publish the initial Q1 view weapon and apply native MDL skin-background preprocessing; same-camera captures verify the missing gun and blue edge corrections. Checkpoint `e8cc40c` shares native and selected Q3 projectile continuation, including bounded damage, bounce/fuse, owner-release saves and reflection checks. Checkpoints `4ee713a` and `674a0b0` preserve special missile contacts and lifetimes across actual actor release. Complete visual fidelity remains open. Checkpoint `0652053` admits selected classic Q2 infantry on Q1 maps and classic Q1 army on Q2 maps through the same actor, body and source behavior services. Root checks cover natural attacks in both directions, binary saves with explicit lifetime remapping, and base1's actual pickup-triggered activation, combat-point route and death target. Source and authored item drops remain distinct. This does not establish the full bestiary, rerelease interchangeability, Q1 path-corner traversal or all mission scripts; finer unsupported source cadences reject explicitly. Checkpoint `bc17271` registers classic dog and berserk through existing source services. Root checks cover untouched authored berserk placement/attack/save on Q1, selected dog bite/leap/save on a fitting Q1 recipe, and native startup drop/flags/callback ordering. Actual all-dog replacement on Q2 base1 now refuses obstructed source bounds explicitly; no creature is moved or shrunk. This is not universal dog placement or rerelease berserk acceptance.

Checkpoint `ed91729` runs selected Q1 weapons and native supply pickups on Q2/Q3 maps. Independent checks cover first shots, pickups, quad damage, native Q3 movement, Q2 live-grenade save/travel and grapple-slot resume. Checkpoint `550c7b0` shares map-owned silencer charges, shot/impact noise and selected-character attack presentation; six actual-map probes cover 31-shot exhaustion, saves, coop projectile impacts and Sarge animation. Checkpoints `b70af97` and `bf94ded` preserve map lighting colors, Q1 model surfaces, view-weapon depth and local lightning placement; the lead inspected frozen CPU captures on both maps. Checkpoint `d6c6131` adds display gamma after CPU/GL scene rendering. Frozen Q2 shotgun and Q3 lightning captures verify the exact shared lookup table against raw framebuffer pixels, unchanged alpha and a byte-identical neutral redraw; the actual GL driver was NVIDIA, not Mesa. Default-framebuffer versus FBO rasterization differences remain a fidelity limit. Native audio and complete visual fidelity remain unproved.

Checkpoint `8bf2485` separates Q2 weapon owners from native player entities and preserves actual-actor impact noise and lifetime checks. Checkpoint `f17a688` runs the reusable Q1 creature controller on shared map actors, with source startup, mission routes, contact goals, projectiles and backpack supplies. Independent frozen checks cover classic enforcer attacks on Q2, flying-projectile and death/drop saves, ammo-only backpack behavior, existing army/dog regressions and explicit obstructed-placement refusal. The combined source candidate passed 73 tests with 1,558 assertions and ten actual gameplay probes. Root inspected the enforcer CPU captures and native attack-frame geometry. This is bounded classic enforcer evidence, not full-roster, rerelease or campaign acceptance; native army/dog controller ownership is unchanged.

Checkpoint `064d1bf` admits selected base-Q2 weapons on Q1/Q3 maps through shared actors and actual classic/rerelease source cadence. Independent checks passed 13 actual gameplay probes, 55 source tests with 1,718 assertions, two adapter tests with 339 assertions and the corrected BFG visibility test. Evidence covers firing, pickups, saves/travel, source timing, quad changes during projectile flight and offhand quad behavior. Root inspected the production CPU Q1/Sarge/Q2-shotgun capture at gamma 1 and 1.3: native view-model frame 9, kick and flags 21 were present, with no unhandled effects. Checkpoint `3ccaeb0` preserves proximity-mine attacker credit for live native and foreign owners, with released-owner fallback; its source callback check passed 36 assertions. Expansion weapon admission, complete foreign target semantics and native visual/audio fidelity remain open.

Checkpoint `af35a0f` restores native Q1 barrel box collision, stationary movement and source drop behavior. Checkpoint `7cbab8d` reads actual target-owned solid and Q2 flags for rail/BFG, and dispatches dodge to the target's actual Q2 monster context. Independent actual-source checks cover the untouched e1m1 barrel at slot 322 taking scheduled BFG laser damage from 20 to 15 health, and admitted infantry switching from stand to its native duck move. The existing Q2 weapon suite passed 23 tests with 328 assertions. These checks do not establish complete foreign-target semantics; the exact `misc_explobox` BFG policy remains, with `misc_explobox2` adaptation deferred.

Checkpoints `8f91da4` and `6d7c253` add shared pickup-grant previews and remove unsupported Q1 crouching from bot navigation. Independent checks passed seven supply tests with 147 assertions and three posture tests with 94 assertions. Checkpoint `eb0d55f` runs the same Q3-derived bot core on Q1 standard deathmatch with selected base-Q2 weapon knowledge, preserving existing Q2/Q3 behavior. Frozen checks passed the Q1/Q2 application tests (2 tests/42 assertions), native Q3 application tests (2/15) and catalog tests (3/22). Actual probes cover respawn resetting rail decision 10 to starter shotgun 2, teleport retaining weapon preference, actor lifetime reuse, and unsupported bot arsenals leaving the game loaded. Q1 dm4→dm5 travel retains the exact admitted composition, module artifacts and asset precedence while resolving the new map. At that checkpoint, native Q1 and foreign Q3 bot arsenal support, health/armor/powerup goals and remaining source-ID tactical comparisons were incomplete. These source commits are included in the installed `b071a5c` development executable.

Checkpoint `d771286` adds source-owned weapon/ammo pickup goals to the shared bot controller, following observer `2aba7e5` and grant-preview helper `0625ca0`. Independent actual probes on the final frozen source cover Q1 dm4 with selected base-Q2 weapons and native Q2 base1: bots start in verified supported approach lanes, then ordinary movement/touch collects the untouched authored pickup, matches previewed inventory gains and observes source respawn. Q1 collected a rocket launcher plus five rockets and five grenades; Q2 collected a shotgun plus ten shells. Q2 checks also preserve a full-ammo refused pickup, value ammo for an empty owned weapon and retire a removed pickup identity. The library fixture covers generation reuse; physical same-slot reuse was not established by the map probe. Reachable trigger-volume targets and generated-edge progress fix the observed route failures without changing native AAS steering. Final strict/policy checks passed, with focused arena/source checks (6 tests/268 assertions). At that checkpoint, health, armor and powerup goals, remaining native-Q1 weapons/foreign-Q3 bot arsenals and broader modes remained open. This source checkpoint is included in the installed `b071a5c` executable; formal acceptance counts are unchanged.

Checkpoint `55e0b80`, following native preview `00b08bf` and weapon-intent routing `2c632a4`, admits native base-id1 Q1 deathmatch weapons through the same bot controller. The bounded Q1 rerelease dm4 fixture uses a Q2 character and source teleport into a verified supported 96-unit approach lane: ordinary bot movement collects the untouched authored rocket launcher with exact previewed ammo, observes respawn, switches from axe to a ranged weapon and deals actual damage, then retains its client through dm5 travel and removes the bot cleanly. Root independently passed the full shared-world fixture (3 tests/59 assertions); final isolated strict/policy checks passed. Existing native Q3 behavior remains supported. Grenade-launcher tactics, other Q1 programs, team modes and all-map coverage remain unqualified. These commits are included in the installed `b071a5c` executable; formal acceptance counts are unchanged.

Checkpoint `24fe385` reads actual Q1/Q2 weapon owners independently of Q1/Q2 map ownership, using engine join `daee0a8` to preserve repeated selected-Q1 fire. The bounded new case uses Q2 rerelease base1, Q2 movement/character and selected classic Q1 weapons. From a supported source-teleport approach lane, ordinary bot movement collects the untouched Q2 shotgun pickup into the previewed Q1 inventory, observes source respawn, reads map-owned quad, then switches from axe to shotgun and consumes ammunition while dealing damage. Root independently passed all four shared-world cases (4 tests/77 assertions); final isolated strict/policy checks passed. At this checkpoint, Q3-map foreign arsenals, remaining weapon tactics, team modes and all-map coverage were unqualified. The installed executable now includes this checkpoint through `b071a5c`; formal acceptance counts are unchanged.

Checkpoint `f8447c6` admits selected base-Q2 weapon bots on baseq3 standard deathmatch maps through the same controller and actual arsenal observations. Detached movement prediction no longer executes native Q3 weapon callbacks for a foreign arsenal. The bounded actual Q3 fixture uses a supported approach lane and deliberately empties starter bullets to verify alternate-weapon choice, then checks mapped shotgun pickup, damage provenance and native medkit use to 125 health. Root independently passed the frozen shared-world and arena tests (6 tests/311 assertions); full isolated strict/policy checks passed. At that checkpoint, broader modes and other foreign arsenals remained unqualified. The installed executable now includes this checkpoint through `b071a5c`; formal acceptance counts are unchanged.

Checkpoint `ebc68d0` preserves map-owned combat feedback and bot powerup observations with foreign weapons. Actual Q3-map selected-Q2 shotgun damage updates native hit count and attacker feedback; Q1/Q2 quad and environmental protection are read from the map owner after arsenal inventory refresh. Expiry checks query source-clock boundaries; they do not prove timer execution. Root independently passed the frozen affected shared-world cases and native Q3 arena (4 tests/301 assertions, two unchanged cases intentionally filtered), plus source/authority checks (14 tests/146 assertions). Full isolated strict/policy checks passed. At that checkpoint, health/armor/powerup pickup goals, broader modes and complete source feedback remained open; the installed executable and formal acceptance counts are unchanged.

## Recent monster and bot checkpoints

Checkpoint `bec7c29` expands the classic Q1 ordinary roster through the existing source creature services. Root's actual authored-map checks passed four tests with 278 assertions. Checkpoint `b85c499` adds ordinary classic Q2 species, source-equivalent authored placement, selected-source cadence and projectile save continuation. Checkpoint `7146aff` updates shared bot combat observations and aim. Independent frozen checks passed nine monster tests with 645 assertions, two bot tests with 37 assertions, one native arena test with 217 assertions and 54 affected native tests with 799 assertions. Same-source Q1 wizard and Q2 mutant placement preserves native geometry behavior; different selected bounds still reject. Mutant jump continuation uses an explicit source attack invocation, so this evidence does not establish natural AI jump initiation.

Checkpoint `e20b6d6` adds ordinary Q2 rerelease roster admission and shared monster muzzle audio, including extended rerelease flash IDs. The common effects consumer now handles berserk slam and plain explosion; classic monsters emit the actor-bound typed muzzle event. Actual resource closure checks and independent roster, native source, dummy-audio and CPU effects tests passed 36 tests with 615 assertions. This source checkpoint is included in the installed `b071a5c` executable.

Checkpoint `5c0bb90` adds rerelease heavy monsters and reads host gravity in source callbacks. Independent retail and native-source checks passed 29 tests with 426 assertions.

Checkpoint `053f70e` renders source-modeled beams through the shared model path and admits the ordinary Q1 rerelease roster. Independent catalog, authored-map, model and parasite checks passed 16 tests with 962 assertions; after three narrow typing corrections, focused beam and rerelease-ogre checks passed two tests with 121 assertions. Root compared the seven unchanged files and inspected the three corrections, actual CPU/GL beam captures and native source algorithm. Rerelease Q1 e1m2 evidence covers natural ogre grenade flight, fresh restore, attributed damage and authored kill credit.

Each checkpoint passed strict TypeScript and policy checks on its frozen source. The heavy-monster/gravity and modeled-beam/Q1 rerelease checkpoints are included in the installed `b071a5c` executable.

Checkpoint `7b1fd47` admits Q1 fish and classic/rerelease Q2 flippers through existing shared actors and preserves source-equivalent rerelease flying placement. Independent final checks passed the full Q1 application file (5 tests/391 assertions) and Q2 aquatic plus rerelease flying placement (3/64); the unchanged catalog checks passed 5/152. Actual water movement, chase/bite and fresh save continuation are bounded to those authored maps. Strict TypeScript passed before policy; both passed on the final snapshot. This checkpoint is included in the installed `b071a5c` executable.

Checkpoint `8f5e9a3` joins selected Brain power armor with actual foreign Q1/Q3 weapon damage. Root independently passed eight catalog/actual-brain tests with 260 assertions, including unchanged authored placement, front/rear damage, native rerelease armor arithmetic, source cells and fresh saves. A synthetic one-damage probe is separate from the real weapon checks. Strict TypeScript passed before policy; both passed on the frozen source. This source is included in the installed `b071a5c` executable.

Checkpoint `0d4d3c4` preserves Shambler rocket resistance across native and foreign source weapons through the actual victim service. Root independently passed the actual fixture and existing authority suite (14 tests/198 assertions): native Q1 halving remains, actual Q2/Q3 rockets deal 53/50 in the fixture, rail damage stays unchanged, and attached/restored victims retain the source rule. Final isolated strict TypeScript and policy passed. The installed executable now includes this source through `b071a5c`.

These bounded results do not establish a complete bestiary, boss/finale or mission-script parity, all-map composition, full audio/rendering fidelity or formal package acceptance.

Checkpoint `7ecc497` makes native Q3 damage use the same victim-owned armor resolver as shared attacks. Root reviewed the unchanged production extraction, passed the prior snapshot checks (9 tests/202 assertions) and reran the corrected fixture (2/50). The final frozen source passed strict TypeScript followed by policy. This checkpoint is included in the installed `b071a5c` executable.

Checkpoint `ff08df0` preserves native Q1 placement when the original floor drop fails, using source setup facts rather than moving the authored creature or relaxing different-source geometry checks. Root independently passed the exact placement fixture (2 tests/39 assertions).

Checkpoint `6331b05` rejects application launch choices whose QC, QVM or native execution path is not joined, before creating a world. This makes the existing execution boundary explicit; it does not implement a guest application path. Checkpoint `d4ed30f` preserves the modeled beam endpoint in Q2 network presentation. Root independently passed catalog and network checks (6 tests/65 assertions), including native UDP. The explicitly synthetic lower-level rerelease endpoint probe does not establish supported rerelease application wire admission or full source rendering parity. The combined eight-file snapshot passed strict TypeScript followed by policy. These checkpoints are included in the installed `b071a5c` executable.

Checkpoint `bd4308b728ee125fe60f28cb9bf40b2b6f06aa3a` preserves Q1 authored deathmatch inhibition by extending the selected-monster source mask from `0x700` to `0xf00`. Root independently passed the exact e1m5 fixture (1 test/14 assertions), covering native/selected filtering and rejection of an unknown behavior flag. The frozen source passed strict TypeScript followed by policy. The installed `b071a5c` executable includes this source correction.

Checkpoint `be01725` publishes already-committed QC source damage observations through the common authority without replaying source writes. Root independently passed 24 QC/authority tests with 255 assertions on the final typed fixture. The witness uses a known-id1 fixture mapper; it does not join application guest lifecycle or establish arbitrary mod behavior.

Checkpoint `43924e30c290fa1d657ff9a7b6c62617f52352af` joins base Q2 classic/rerelease arsenals in both edition directions, including canonical catalog/UI selection, source resources, active weapon presentation and shared pickup overlap handling. Root independently passed nine runtime tests with 239 assertions and seven catalog/pickup tests with 928 assertions. Actual base1 checks cover independent source cadence, authored shotgun touch, active kick/loop state, map-owned silencer/noise, live projectile and fresh-save continuation. The grenade item case explicitly uses a synthetic native spawn when that map edition lacks an authored grenade item; it does not prove natural pickup navigation. This is bounded base-Q2 edition mixing, not expansion arsenals, arbitrary multi-provider primary weapons or every map/movement permutation. Both checkpoints passed strict TypeScript followed by policy on frozen source. The new compiled candidate passed the standard build gates and silent help/menu checks before atomic installation.

Checkpoint `40726d0` joins base-id1 Q1 classic/rerelease arsenals in both edition directions. Root independently passed the full runtime/catalog/startup checks (18 tests/1,349 assertions), then the corrected deadline-presence fixture (2/86). The frozen source passed strict TypeScript followed by policy. Expansion and QuakeWorld arsenals remain outside this evidence.

Checkpoint `46ca5f0` joins the QC walkmove adapter to existing shared monster movement/collision and trigger dispatch, including source ground-entity projection. Root independently passed QC and shared-physics checks (20 tests/231 assertions); isolated strict and policy passed. At that checkpoint, an unchanged e1m1 source probe progressed through walkmove to the still-unimplemented checkclient builtin. This is a bounded library/host join, not an application guest lifecycle or campaign claim.

Checkpoint `d1a96b0` preserves map-defined native monsters beside explicit per-class replacements, including native bosses and separate source ownership, counters and fresh-save continuation. Root independently passed the full Q1 application/catalog/save checks and new actual Q2 mixed-roster case (22 tests/649 assertions). The integrated QC-plus-roster snapshot passed the focused mixed-roster, QC spatial and shared-physics checks (9/121). Strict and policy passed before a final two-call awaited-close cleanup; root reviewed that exact cleanup and tested the final integrated tree. At that checkpoint, the startup roster editor was separate work; and Q3-map creature admission remains open. These three source checkpoints are included in the installed `b071a5c` executable.

Checkpoints `a39c254` and `4db8011` separate Q1 weapon recoil from movement and expose per-class monster roster choices in the startup menu. Root independently passed recoil, menu/source and integrated roster checks (23 tests/451 assertions) and inspected CPU/GL roster screens. Strict and policy passed on the frozen source after a catch-parameter type annotation. The recoil visual evidence is camera-only; it did not establish complete viewweapon rendering.

Checkpoints `e973079` and `c12153d` fix common selected-weapon presentation and share Q1 client visibility between native and QC hosts, including saved cache state and source field projection. Root independently passed runtime and render/HUD checks (33 tests/1,144 assertions, with one pre-existing SDL skip) and inspected eight CPU/GL e1m1/q3dm1 captures. The final frozen source passed strict and policy after removal of an unused helper and duplicate import; root reviewed those exact cleanups. At that checkpoint, the controlled Q3 prepared-scene capture predated shotgun animation, and false HUD inventory and overlap remained separate limitations. Visibility sharing does not complete guest frame physics or application guest lifecycle. These four commits are included in the installed `b071a5c` executable.

Checkpoints `38042e2` and `703cc0a` add QC movetogoal execution, correct selected-source prediction angles and project canonical weapon/ammo status and icon resource metadata for native, selected and remote arsenals. Root independently passed 41 tests with 1,753 assertions across source, prediction, catalog/assets and native UDP checks. Five actual retail cases confirmed source provider/content and ammo transitions: Q1 super-shotgun transitions from one shell to zero; Q2 BFG is blocked at 49 cells, ready at 50 and consumes 50. The QC startup probe completed 23 startup callbacks and 12 actual movetogoal calls; it does not establish complete guest frame physics. At that checkpoint, the display consumer remained separate work; canonical HUD data alone did not prove the displayed HUD correct. The frozen source passed strict TypeScript followed by policy. Both commits are included in the installed `b071a5c` executable.

Checkpoints `8b88b8d` and `68c33b9` join QC id1 damage boundaries/shared pusher execution and the common weapon HUD/radial consumer, with source-grounded viewweapon lighting and demand registration. Root independently passed the pusher/movement/QC suite (32 tests/503 assertions). Earlier slice checks passed QC/authority 27/390, render/UI/Q3 application 17/562 with one existing SDL skip, and standalone Q3 presentation 4/35; those overlapping results are not summed. An actual Q3-map/Q1-arsenal radial replay matched one-shell, zero-shell and unmetered-axe states through ordinary commands, with no false ammo sound in 64 checks. Head/gun overlap remains a known presentation limit. The final typing-only fixture cleanup passed strict and policy.

Checkpoints `365634a` and `36c450b` use native Q1 source-local pusher timers, shared ground publication and geometry-edition Q2 lighting, including resolved external-brush provenance. Root independently passed four distinct actual/runtime/model/application files (44 tests/1,495 assertions, no skips) and reviewed four CPU/GL edition captures. Strict and policy passed on the exact 35-file snapshot. This is bounded source behavior and presentation evidence, not full campaigns, guest lifecycle or feature-family acceptance.

Checkpoints `75ee9bb` and `2fabe9c` move Q1 voices into the common Q1/Q3 audio core and admit pinned id1 QC through dedicated `Application.open`, without a TypeScript gameplay fallback. Root independently passed 80 distinct tests with 1,953 assertions across seven files, with no skips: audio 17/164, QC/runtime 33/1,300 and water/movement/foundation 30/489. Strict and policy passed on snapshot source `8eb91805cbcb772ca16e0667b1cf77f1586318a414a36be409967d039d2d173d`. The actual application fixture ran three frames at 100/100/50 ms with 30 PUSH entries and source frametime publication. A separate owner probe observed 258 moving-actor frames over four seconds without raw bounds mismatch; that is bounded review evidence, not campaign acceptance. At that checkpoint, Q2 audio cutover remained in flight; QC client execution, saves and unknown programs were unsupported. These commits are included in the installed `b071a5c` executable.

Checkpoint `484b651` moves Q2 voices into the same common audio core as Q1 and Q3 and removes the separate mixer implementation/export. Root independently passed 22 audio tests with 201 assertions using dummy audio/offscreen video; strict and policy passed on the exact frozen five-file snapshot. One core does not establish complete feature union, audible fidelity or a performance advantage; environmental reverb, universal Doppler/application joins and broader source behavior remain open.

Checkpoints `3f31e53` and `93c3248` join pinned QC attacks/shared aiming and prime startup audio from measured preparation cost. Root independently passed 52 tests/862 assertions across four files (QC/native foundation 29/656, audio 23/206). The final snapshot retained those tested bytes except reviewed type-only Map/target-array annotations; strict and policy passed. Root normal-Application CPU/GL probes ran 32 frames each with dummy audio/offscreen video and no empty queues. Initial/final queued audio was CPU 651/450 ms and GL 639/152 ms. The buffering trades latency for startup coverage: a separate owner run still observed a later 6.7 ms CPU shortage. These bounded silent probes do not establish that all choppiness is fixed or qualify audible latency/fidelity. At that checkpoint, source was pushed through `93c3248` and QC client lifecycle remained uncommitted. The attack/startup commits are now included in the installed `b071a5c` executable.

Checkpoints `b895d2e` and `5dcdd93` preserve dedicated QC client source ordering/live body shape and join environment sampling to common application audio. Root independently passed 26 distinct tests/725 assertions: clients 6/391, movement 8/138, audio/collision 10/144 and native/selected bot cases 2/52. Strict and policy passed on frozen source `5324ef9e1bf5eb8a3292922ad0544ec2e365801c001269a7c182cbfe7163bd9a`. Corrected source-think ordering, changed hulls and removal without PostThink are bounded lifecycle evidence; graphical/network QC clients, QC saves, unknown programs and general world hazards remain unsupported or unqualified. Player checkpoint subrecord version 1 rejects older unversioned records. At that checkpoint, reverb loaded genuine Q2 rerelease environment data from selected audio content; the foreign Q1 proof selected Q2 rerelease audio, and independent environment-versus-sound selection remained in flight. Reverb sampling includes actual floor/air/sky contacts and transformed inline geometry; sky evidence is point-based, unknown floors remain explicit, and audible/EFX parity is unproved. At that checkpoint these source commits were outside installed `b071a5c`; they are included in the current `3a073e8` build.

Checkpoints `406689c` and `b6cc48e` select environment data independently from game sounds and preserve provenance at all nine pinned id1 environmental damage sites. Root independently passed 18 distinct tests/739 assertions: environment audio/menu/recipe checks 15/231 across three files and final hazards 3/508. The exact frozen source passed strict and policy; root inspected three CPU and two GL menu captures. Selected environment data retains native Q1/Q3 sound assets, missing data fails explicitly, and menu plus recipe encode/decode are covered. A full application save/reload for this selection was not separately run. Hazard proofs cover nonfatal damage, source bytes and velocity; lethal obituary behavior and nested hazards remain unverified. At that checkpoint both commits were outside installed `b071a5c`; they are included in the current `3a073e8` build.

Checkpoints `3a4b23f`, `17c87c6` and `67f73c7` fix singular Doppler hangs, extend the generic QC broadcast host, and join standard Q1 point effects and sounds to the common consumers. Root independently passed 30 distinct tests/691 assertions: Doppler 23/190, QC 3/371 and effects 4/130; strict and policy passed on the exact seven-file snapshot. Doppler uses intentional large-span averaging while preserving ordinary captured PCM; universal Doppler remains open. The QC host covers eight source writers and 14 temporary-entity types for `MSG_BROADCAST` only, without admitting unknown mods. Actual CPU/GL captures and sound checks used mixed e1m1 with Q1 movement/weapons and Sarge, not an all-native configuration. Root inspected active and correctly cleared effects; the earlier mislabeled expiry capture was a probe error. At that checkpoint these commits were outside installed `b071a5c`; they are included in the current `3a073e8` build.

Checkpoints `9196e76` and `fb6f421` join QC localcmd to application command phases and expose source/off Doppler selection. Root passed 41 distinct tests/675 assertions: corrected Kfgrx5 QC fixtures 4/355 and audio/menu/save 37/320 carried from BRSS80 because all 17 Doppler files were byte-identical. Strict and policy passed on corrected Kfgrx5. The installed Hipnotic function runs unchanged. Application evidence uses fixture-created native info_command actors and QC frame-hook injection to check startup, next-frame, wait and unknown-command behavior; it does not show maps naturally triggering those commands, and map transitions remain unverified. Doppler source/off selection is covered by recipe, audio and actual CPU/GL menu evidence, not universal moving-loop Doppler. These two source commits are newer than installed `3a073e8`.

Checkpoints `05ce434` and `3658b90` release native Q1 torches, globes and func_illusionary actors into the persistent shared scene. Alias flames, inline BSP and external BSP routes were exercised, with save continuation and 24 ordered e1m2 torch records in native UDP signon. Sprites use the existing common renderer; separate sprite execution was not proved here. Generic QC makestatic preserves model-string/precache lookup, actor removal and empty-model index 0 semantics. Root independently passed 17 distinct tests/653 assertions; strict and policy passed on exact static10 NfPNYu. The original brush omission was corrected before acceptance. Root inspected actual native e1m2 and fixed-camera e2m1 CPU/GL captures, including an explicit static-consumer-withheld comparison; these are unified-runtime captures, not native reference imagery. QC graphical/network/general-program guards remain, and extra addon override static spawns are not yet migrated. Installed `3a073e8` does not include these commits.

Accepted checkpoints `a8ace02`, `3e57748`, `60cfc8e` and `a5fca35` join per-seat haptics and strength controls, Q1 addon static overrides, and NAV2 provenance/live navigation observations. Root independently passed 37 tests/585 assertions across five files for the final strength/static/navigation snapshot; strict and policy passed. Four haptics requirements and NAV2 loading are now Done through common extensions. Dynamic navigation remains Not done because unsupported mover travel is still rejected. Strength retains authored envelope timing and uses existing frontend preferences; disk persistence and physical-controller qualification are not claimed. Installed `3a073e8` is unchanged and excludes these checkpoints.

## Historical installed build

The Linux x64 executable at `/home/buzzkill/Projects/qfiles/quake-typescript` is built from `3a073e87ec7a4126f396e15833b8afb9df569403`, replacing `b071a5c`. Without arguments it opens the main menu before creating a world, player or game audio. Bun 1.3.14 produced the 119,519,360-byte executable from an isolated tracked snapshot; strict typing, policy and input-stability gates passed. Installation was atomic with mode `0755`. Localcmd and source/off Doppler selection are not included.

- Source tree: `cc81be471ec416658908febd465abeb370f696c0`.
- Source SHA-256: `5f34c8ceb10e00971938eab006e3a5297620d48a7aaecade4ea0834049168673`.
- Executable SHA-256: `c6f3a0df8c03aee584cd45dbd45ea5df1064808b90f2ea38b1955a2a4281ddcd`.
- Provenance: adjacent `quake-typescript.build.json`; previous executable, sidecar and instructions use backup prefix `quake-typescript.previous-20260912T211600531Z`.
- Root independently verified executable SHA, size, mode, sidecar and installed destination/instructions. Help exited 0; the no-argument menu stayed alive eight seconds with offscreen/dummy drivers, then SIGTERM exited 0 with empty stderr. Actual CPU and GL e1m1/Q1-movement/Sarge runs each completed eight frames with exit 0. Both warned that classic id1 music track 6 was unavailable; a loose track exists only in rerelease/id1, so these runs do not prove music playback. All owned processes closed.

Development saves from older builds are incompatible: Q1 foundation version 5 rejects version 4, and Q1 player checkpoint subrecord version 1 rejects old unversioned records. QC saves remain unsupported.

Start with `~/Projects/qfiles/quake-typescript`. The mouse-driven main menu offers Single Player, Multiplayer, Load Game, Options and Quit. Configuration resolves independent supported selections into the exact recipe on Play; failures return to the retained draft. Load Game lists actual saves with metadata and unavailable-save diagnostics. Display Apply changes the actual renderer; Sound/Controls reuse in-game bindings. Independent frozen probes passed actual Play/save/load, preferences, display Apply and runtime-failure return; CPU/GL menu and pause captures were inspected. These results do not establish complete campaigns, arbitrary pickup replacement, Q3 full saves, server/lobby/bot administration, audio fidelity or full rendering parity. Installed Quake III or rerelease font assets are currently required for proportional menus. The current build verification used offscreen video and dummy audio.

## Historical source and runtime state

Offhand Q2 grenades are verified on Q1/Q2/Q3 worlds. Shared grapple slots now use actual source handoffs, outgoing presentation and saved state; root checks cover representative native/mixed selection, rapid switching, gauntlet suppression, saves and travel. Rerelease CTF follows an actual q3dm11 door through 94 units and its stop. LMCTF retains its initial hit and 0.7-second player-contact pulses, while shared attachment saves remap anchor identities. Threewave/LMCTF moving Q3 brush checks pass under Q1 movement; native Q3 movement beside a real LMCTF hook also passes. Active work addresses the remaining monster roster, source cadence, mission obligations and visual fidelity. The close army muzzle capture remains insufficient evidence of firing-frame fidelity. Map BSPs and sidecars stay within logical map content. Current saves use recipe schema 3, the QTSAVE2 container and world:simulation provider schema 11; unsupported versions reject explicitly. Complete sky/render fidelity, general foreign source views and Q3 full saves remain outside this evidence. These checkpoints passed isolated typecheck and policy; they do not accept a complete package.

Verified source checkpoint `423101a` fixes Q2 intensity upload, MD2 face culling, Sarge child-model skin mapping, adaptive audio queuing/frame cost and selected Q3 jump audio under Q1 movement. For selected-Q3-arsenal checkpoint `c19745a`, the lead reports actual shared-actor checks on Q1/Q2 maps with all three movement providers, exact damage, rockets/grenades, saves and travel. These are bounded results, not complete interoperability or installed-binary evidence. Root independently checked real Q1 e1m1 pickups and e1m2 travel, plus Q2 classic/rerelease base1 pickups and base2 travel: pending weapon switches survive saves, Q1 mission keys clear, respawn restores starter capacities, and Q2 power-armor cells remain intact. Old interim Q1 selected saves without a supply profile now reject explicitly. This documentation refresh did not rerun gameplay.

The user's architecture rule applies to every subsystem: coalesce the strongest implementation and add every useful feature from every game to that common system. One engine, world and executable must expose those capabilities across content. Source-specific policies preserve required behavior. The latest user direction requires full independent swappability and a usable startup menu; `ca5003b` supplies the bounded menu workflow above while engine interoperability remains incomplete.

| Work | Confirmed bounded progress | Remaining dependency or evidence |
| --- | --- | --- |
| W73/W74 first playable | Mixed-provider CPU/GL gameplay, firing, local seats and travel have live evidence. | Complete specimen mission, fresh-process and co-op criteria remain subject to formal acceptance. |
| W41–W51 content and composition | Selected-Q3 arsenal checks cover shared actors, movement, damage, projectiles, saves and travel. Named Q1 id1 and classic/rerelease Q2 base-program standard-match supply profiles now admit native pickups into that arsenal. | Ordinary classic/rerelease Q1 and Q2 creatures attach through shared actors and source services, with bounded authored-map attack/save evidence above; remaining species and mission obligations are still open. Arbitrary pickup replacement, unsupported expansion arsenals and supply profiles, remaining composition-specific semantics and full interoperability remain incomplete. |
| W23 saves | Q1/Q2 capture and restoration are implemented; reported application checks cover CTF/LMCTF save restoration and captures. | Full source-private continuation, all original formats and every provider remain open. Q3 and guest save support are not inferred from Q1/Q2 results. |
| W47 CTF/LMCTF | Pause/voting landed in `ec2686b`; match travel/settings landed in `cdc2cc5`. | Complete match, administration, HUD and voting workflows still require combined acceptance. |
| W57/W58 bots and navigation | Checkpoints `060403c` and `7146aff` complete bounded standard-deathmatch checks for the nine basic map-family/arsenal-family combinations through the same controller. Actual authored supply pickup, weapon choice, firing and damage checks use supported approach lanes. `75d7bf1` shares native Q3 item-inventory projection for health, armor and powerup decisions; later supply and arsenal joins preserve those observations. | Broader maps and modes, expansion arsenals, remaining weapon tactics, and complete cross-family pickup/goal semantics remain unqualified. The nine basic combinations are bounded cases, not an all-content compatibility matrix. |
| W18/W19/W59 presentation | Checkpoint `423101a` fixes Q2 intensity, MD2 culling, Sarge child skins, audio queuing/frame cost and selected-character jump audio. | The installed executable is updated through `3a073e8`; see the [build record](#installed-build). Shared startup/pause menus have inspected CPU/GL captures; complete presentation coverage remains open. |
| W30–W40 guests | Interpreted native loaders/execution are committed. A native rerelease retail run passed 36 assertions across 502 edicts through Init, Spawn, ClientConnect, Begin, Think and active RunFrame. | Full-map JSON saving exceeded the 20-million-instruction budget. Full callback, semantic override, gameplay and save coverage remain incomplete. |
| W52–W55 multiplayer | NetQuake 15 and Q3 protocol 68 server application joins landed in `ec2686b`; Q3 pure/archive downloads landed in `cdc2cc5`. | Native wire support covers family-compatible configurations. Mixed transport, connected-peer behavior and full source-wire interoperability remain unfinished. |
| W66–W72 integration and release | W66 application integration is underway, with bounded runtime evidence above. | Complete W66, native gameplay W40 and performance W68 before full W67 configuration accounting, Linux artifact qualification, independent review and release closure. |

The user confirms LMCTF has no soundtrack of its own. The music check verified `lmctf09` requests track 1 and decoded inherited Q2 track 02 without changing playback or files; its warning does not establish missing LMCTF content. The immediate work is remaining creature behavior, foreign-target damage semantics and mission coverage, then combined engine interoperability evidence. W60 has bounded composition/save menu implementation; W61 server, lobby and bot administration workflows remain incomplete. The graph still requires official gameplay → W50/W51 → W66 and W66 plus native execution → W40, with menus required before full W66 acceptance. W67 also requires independent evidence W63–W65, content completeness W07 and performance W68. Broad hardening follows imports, live human play and optimization under the user's code-first sequence. No full package is accepted by this refresh.

## Historical coordination

The lead assigns exclusive paths and uses Astra at low or medium effort for current recovery tasks. `q1_monster_roster` is the sole current Git writer, staging exact frozen snapshots after the lead reviews their scope and evidence. Historical owner names and xhigh dispatches below do not assign current ownership or effort. The lead must assign any new work explicitly.



## Historical handoffs at the core checkpoint

The following table records the earlier core-checkpoint handoff. Agent names are relative to `/root/`. Those dispatches used `gpt-6-astra` with `xhigh` effort and `review_w06_platform` as Git writer. They are retained as history and do not override current lead assignments.

| Work | Current owner | Exclusive paths or bounded handoff |
| --- | --- | --- |
| W23 and W41 state | `w41_actors` | `src/persistence/**`, `src/world/actors/**`, `src/world/gameplay/**`, and corresponding existing checks; real identity and save joins |
| W42 Q1 base | `w42_q1_base` | `src/content/q1/base/**`, `tests/gameplay/q1-base/**` |
| W43 Q1 mission packs | `w59_native_ui` | `src/content/q1/missionpacks/**`, `tests/gameplay/q1-missionpacks/**`; earlier native UI slice handed off |
| W44 Q1 additions | `w49_q3_modes` | `src/content/q1/addons/**` and corresponding checks; running source import |
| W45 Q2 base | `w45_q2_base` | `src/content/q2/base/**` except `player/**` and the completed `entities/**` slice, with matching base checks |
| W45 Q2 player | `w45_q2_player` | `src/content/q2/base/player/**` and matching player checks |
| W46 Q2 mission packs | `w45_q2_entities` | `src/content/q2/missionpacks/**`, `tests/gameplay/q2-missionpacks/**`; earlier 38-entity source slice handed off |
| W46 Q2 mission-pack monsters | `w45_q2_base` | Monster subtrees within `src/content/q2/missionpacks/**` and matching checks, reserved from the W46 parent |
| W48 Q2 rerelease | `w45_q2_player` | `src/content/q2/rerelease/**` except `monsters/**`, and corresponding rerelease checks except monster checks; running source import |
| W48 Q2 rerelease monsters | `plan_validator` | `src/content/q2/rerelease/monsters/**`, `tests/gameplay/q2-rerelease/monsters/**`; source-specific registration over the shared Q2 monster controller |
| W74 Q1 restoration | `w74_q1_foundation` | `src/content/q1/foundation/**` and corresponding existing checks |
| W74 Q2 restoration | `w74_q2_foundation` | `src/content/q2/foundation/**` except the separately owned monster and weapon paths |
| W74 Q2 monsters | `w14_q3_models` | `src/content/q2/foundation/monsters/**` and matching checks; finishing this source slice |
| W54 Q2 network | `w54_q2_network` | `src/network/q2/**`, `tests/network/q2/**` |
| W73 network integration | `w54_q2_network` | `src/app/bootstrap/network/**` |
| W31 PE | `w01_q1_features` | `src/guest/pe/**`, `tests/guest/pe/**` |
| W32 ELF | `w01_q2_features` | `src/guest/elf/**`, `tests/guest/elf/**` |
| W33 i386 | `w02_q1_reference` | `src/guest/x86/**`, `tests/guest/x86/**` |
| W34 x64 | `w02_q2_reference` | `src/guest/x64/**`, `tests/guest/x64/**` |
| W35 floating-point | `w02_q3_reference` | `src/guest/floating-point/**`, `tests/guest/floating-point/**` |
| W36 ABI | `w01_products` | `src/guest/abi/**`, `tests/guest/abi/**`; this dispatch uses `xhigh`, superseding its earlier inventory effort |
| W73 Q3 host | `w49_q3_gameplay` | `src/app/bootstrap/simulation/q3/**`, under the lead's narrow bootstrap handoff |
| W73 effects | `w49_q3_presentation` | `src/app/bootstrap/effects.ts` |
| W73 audio | `w18_cpu` | `src/app/bootstrap/audio.ts` |
| W17 and W15 corrections | `w17_scene_world` | Scene shadow caster and `src/materials/legacy-fog.ts`; other material writes stay with their owner |
| Build and commit writer | `review_w06_platform` | `tools/build.ts`, `src/types/png.d.ts`, and the lead-assigned policy correction. Sole Git writer, staging only reviewed exact paths |
| Execution ledger | Lead-controlled handoff | The bounded refresh by `plan_validator` is complete; that worker now owns W48 monsters. The lead assigns further writes to `docs/work-packages.json`, `docs/project-plan.md`, `docs/execution-status.md`, and `docs/dependency-graph.mmd` |

Q1 network source was handed off by `w53_q1_network`, and Q3 network source by `w55_q3_network`; both workers have left the active pool. W62 artwork was handed off by `w01_products`, now assigned ABI work. `w59_native_ui` completed the art-manifest cleanup and its documentation before moving to W43. Those completed source slices remain running at package level until the lead evaluates integration and acceptance. The lead assigns a current writer before corrections to an unowned completed slice. Historical worker names do not reactivate them.

## Bounded live evidence after the core checkpoint

The lead independently ran this actual source command with exit 0, loading Q2 `base1` with Q1 movement and Q3 Sarge:

```bash
env SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy bun run src/main.ts --renderer cpu --hidden --width 160 --height 120 --frames 2
```

That success preceded the Q2 floater registration error and correction recorded above. The command remains the reproducible startup check; this ledger update did not rerun it during concurrent integration.

The retained `/tmp/w73-interaction.ts` script opens the real CPU application, applies movement and fire input, steps the simulation, checks displacement, collects presentation-event kinds, and captures pixels. Its two preset runs retained `/tmp/w73-q2-classic-baseq2.png` and `/tmp/w73-q1-rerelease-id1.png`. The retained `/tmp/w73-menu.ts` script opens the real native menu, checks settings focus, and captures `/tmp/w73-menu.png` and `/tmp/w73-controls.png`. The files were present and the script contents were inspected for this ledger update. Execution success and visual review are lead-reported observations, not fresh runs by the ledger owner.

After the floater-table correction, the lead reports a successful actual application run of `/tmp/w73-travel.ts`. It reloads `base1` while retaining the native window, seat object, and client identity. The emitted result retained 125 health and nine picked-up bullets, replaced actor generation 10 with 21, and rejected the previous handle. The retained script was inspected and explicitly checks identity invalidation, seat/client/window retention, and the bullet count. This is one bounded source travel case, not save-load completion or acceptance of every campaign transition.

The lead also reports CPU and GL startup for both specimens, a two-seat CPU run, and dedicated startup. Exact retained command output for those additional runs is not recorded here. Temporary captures and scripts can disappear, and none supplies the final original-source, campaign, multiplayer, or compiled-release evidence required at W67–W71.

## Earlier dispatch history

This table preserves earlier ownership records and completed handoffs. It is not a fresh dispatch or permission to edit a path that the lead has reassigned. The current handoffs above and later explicit lead assignments take precedence. The earlier `execution_state` assignment does not grant the current ledger owner permission to change validator code or fixtures.

All workers used `gpt-6-astra`. Effort was `high` for `w01_products`, `w06_audio`, `w06_fonts`, `execution_state`, `w04_manifest_tests` and `additional_fixtures`; other dispatches used the package default `xhigh` unless the lead announced an override.

| Package | Owner | Exclusive paths |
| --- | --- | --- |
| W01 | `w01_census` | `verification/source-manifest.json`, `verification/feature-ledger.json`, `tools/inventory/source-census.ts`, `tools/inventory/aggregate-features.ts`, `tests/inventory/source-census.test.ts`, `tests/inventory/feature-ledger.test.ts` |
| W01 | `w01_q1_features` | `verification/features/q1.json` |
| W01 | `w01_q2_features` | `verification/features/q2.json` |
| W01 | `w01_q3_features` | `verification/features/q3.json` |
| W01 | `w01_products` | `verification/product-manifest.json`, `tools/inventory/products.ts` |
| W01 | `w01_lineage` | `docs/reference-contracts.md` |
| W01 | `additional_fixtures` | `verification/additional-fixtures.json`, `tools/inventory/additional-fixtures.ts`; searches user-provided Downloads and Steam locations for missing fixtures |
| W02 | `w02_references` | `tools/reference/environment.ts`, `tools/reference/schema.ts`, `tools/reference/README.md`, shared runner files directly under `tools/reference/`, `verification/reference-environment.json`, `verification/reference-targets.json` |
| W02 | `w02_q1_reference` | `tools/reference/q1/**`, `verification/reference-cases/q1/**` |
| W02 | `w02_q2_reference` | `tools/reference/q2/**`, `verification/reference-cases/q2/**` |
| W02 | `w02_q3_reference` | `tools/reference/q3/**`, `verification/reference-cases/q3/**` |
| W02 | `w02_native_q2`, dispatch assigned | `tools/reference/q2-native/**`, `verification/reference-cases/q2-native/**` |
| W03 | `w03_policy` | `package.json`, `bun.lock`, `tsconfig.json`, `eslint.config.ts`, `tools/check-policy.ts`, `tools/build.ts`, `.github/workflows/**`, `tests/policy/**`, `.gitignore` |
| W05 | `w05_contracts` | `src/contracts/**` except the peer files below, `docs/contracts.md` |
| W05 | `w05_content_contracts`, bounded content interface complete | `src/contracts/content.ts` |
| W05 | `w05_scene_contracts` | `src/contracts/scene.ts`, `src/contracts/render.ts`; frozen except explicit lead correction handoffs |
| W05 contract correction | `w25_q2_movement` | `src/contracts/movement.ts` for Q2 rerelease fields |
| W05 | `w05_execution_contracts` | `src/contracts/protocol.ts`, `src/contracts/execution.ts`, `src/contracts/ui.ts` |
| W03 review | `review_w03_policy` | Read-only; no write paths |
| W04 | `w04_verification` | `tools/verify/**`, `verification/schema/**`, `verification/suites.json`, `tests/verification/**` except the two files reserved below |
| W04 | `w04_manifest_tests` | `tests/verification/product.test.ts`, `tests/verification/schema.test.ts` |
| W06 | `w06_platform` | `src/platform/sdl.ts`, `src/platform/native-libraries.ts`, `src/platform/sdl-render-context.ts`, `src/platform/gl.ts`, `src/platform/gl-programs.ts`, `src/platform/runtime.ts`, `src/platform/index.ts`, `tests/platform/window-native.test.ts`, `tests/platform/window-headless.test.ts`, `tests/platform/window-runner.test.ts`, `tests/platform/native-libraries.test.ts` |
| W06 | `w06_audio` | `src/platform/audio.ts`, `src/platform/vorbis.ts`, `tests/platform/audio.test.ts`, `tests/platform/vorbis.test.ts` |
| W06 | `w06_fonts` | `src/platform/freetype.ts`, `src/platform/freetype-layout.ts`, `tests/platform/freetype.test.ts` |
| W06 | `w06_controllers` | `src/platform/controller.ts`, `tests/platform/controller.test.ts` |
| W08 | `w08_math` | `src/core/math.ts`, `src/core/numeric.ts`, `src/core/qvm-math.ts`, `src/core/renderer-math.ts`, `src/core/common-parse.ts`, `src/core/common-error.ts`, `src/core/binary/**`, `tests/core/math.test.ts`, `tests/core/binary.test.ts`, `tests/core/parse.test.ts` |
| W08 | `w08_commands` | `src/core/commands/**`, `src/core/cvars/**`, `tests/core/commands.test.ts`, `tests/core/cvars.test.ts` |
| W08 | `w08_sessions` | `src/world/session/**`, `src/world/scheduler.ts`, `src/core/diagnostics.ts`, `tests/core/session.test.ts`, `tests/core/scheduler.test.ts` |
| W09 | `w09_archives` | `src/content/archive/**`, `tests/content/catalog/archive.test.ts` |
| W09 | `w09_catalog` | `src/content/catalog/**`, `src/content/mounts/**`, `tests/content/catalog/catalog.test.ts`, `tests/content/catalog/mounts.test.ts` |
| W10 | `w10_q1_maps` | `src/formats/q1-map/**`, `tests/formats/q1-map/**` |
| W11 | `w11_q2_maps` | `src/formats/q2-map/**`, `tests/formats/q2-map/**` |
| W12 | `w12_q3_maps` | `src/formats/q3-map/**`, `tests/formats/q3-map/**` |
| W13 | `w13_q12_models` | `src/formats/q12-model/**`, `tests/formats/q12-model/**` |
| W14 | `w14_q3_models` | `src/formats/q3-model/**`, `tests/formats/q3-model/**`, except the narrow MD5 correction below; original model slice completed, worker now assigned Q2 foundation monsters |
| W14 correction | `w17_scene_models` | `src/formats/q3-model/md5.ts` for `q1Md5AnimationTiming` and its corresponding check in `tests/formats/q3-model/md5.test.ts` |
| W15 | `w15_images` | `src/formats/images/**`, `tests/materials/images.test.ts` |
| W15 | `w15_materials` | `src/materials/**`, `tests/materials/materials.test.ts` |
| W18 | `w18_cpu` | `src/render/cpu/**`, `tests/render/cpu/**` |
| W19 | `w19_gl` | `src/render/gl/**`, `tests/render/gl/**` |
| W16 | `w16_q1_collision` | `src/world/geometry/q1-solid/**`, `src/world/collision/q1/**`, `tests/world/collision/q1/**` |
| W16 | `w16_collision` | `src/world/geometry/**`, `src/world/collision/**`, `src/world/spatial/**`, `tests/world/collision/**`, excluding the Q1 paths above |
| W17 | `w17_scene_world` | `src/render/scene/**` except `models/**` and `particles/**`, `src/render/commands/**`, `tests/render/commands/**` except `models/**` |
| W17 | `w17_scene_models` | `src/render/scene/models/**`, `src/render/scene/particles/**`, `tests/render/commands/models/**` |
| W20 | `w20_audio` | `src/audio/**`, `tests/audio/**` |
| W21 | `w21_media` | `src/media/**`, `tests/media/**` |
| W21 | `w21_text` | `src/text/**` except `truetype.ts`, `tests/text/**` except `truetype.test.ts` |
| W21 | `w13_q12_models`, completed font slice | `src/text/truetype.ts`, `tests/text/truetype.test.ts` |
| W22 | `w22_input` | `src/input/**`, `src/console/**`, `src/settings/**`, `src/capture/**`, `tests/input/**`, `tests/settings/**` |
| W27 | `w27_qc` | `src/compat/qc/**`, `tests/compat/qc/**` |
| W29 | `w29_qvm` | `src/compat/qvm/**`, `tests/compat/qvm/**` |
| W30 | `w30_guest` | `src/guest/core/**`, `tests/guest/core/**` |
| W41 | `w41_actors` | `src/world/actors/**`, `src/world/gameplay/**`, `tests/world/gameplay/**` |
| W08 numeric correction | `w17_scene_models` | `src/core/game-numeric.ts` |
| W24 | `w24_q1_movement` | `src/movement/q1/**`, `tests/movement/q1/**` |
| W25 | `w25_q2_movement` | `src/movement/q2/**`, `tests/movement/q2/**`; movement contract exception recorded above |
| W26 | `w26_q3_movement` | `src/movement/q3/**`, `tests/movement/q3/**` |
| W52 | `w52_network` | `src/network/common/**`, `src/network/services/**`, `tests/network/common/**` |
| W74 | Q1 foundation lane | `src/content/q1/foundation/**`, `tests/gameplay/foundation/q1/**` |
| W74 | Q2 foundation parent lane | `src/content/q2/foundation/**` except `monsters/**` and `weapons/**`; `tests/gameplay/foundation/q2/**` except matching monster and weapon checks |
| W74 | Q3 foundation lane | `src/content/q3/foundation/**`, `tests/gameplay/foundation/q3/**` |
| W74 | `w14_q3_models`, resumed monster assignment | `src/content/q2/foundation/monsters/**` and matching Q2 foundation monster checks |
| W74 | `w74_q2_weapons` | `src/content/q2/foundation/weapons/**` and matching Q2 foundation weapon checks |
| W73 | `w73_bootstrap` | `src/app/bootstrap/**`, `src/main.ts`, `tests/first-playable/**`, `verification/first-playable/**` |
| Lead records | `execution_state` | `docs/work-packages.json`, `docs/dependency-graph.mmd`, `docs/validate-plan.ts`, `docs/test-plan-validator.ts`, `docs/project-plan.md`, `docs/execution-status.md` |

W03 owns every build and package registration. Other workers submit registration requests. Its early build proof compiles real implemented TypeScript tools through the production Bun build pipeline. The production runtime entry arrives in W73. W03 requires no placeholder engine entry and does not wait on W73. Production engine compilation and gameplay remain W73, W66 and W69 obligations.

## Handoffs and acceptance

Each bounded handoff names its source revision, exact changed paths, commands and results, retained evidence, unresolved findings, reviewer and commit disposition. The lead appoints the sole Git writer before staging exact reviewed paths. A progress commit records its bounded result; it does not accept a package automatically.

Only the lead changes a package to accepted after evaluating its acceptance requirements and independent review against the combined tree. An accepted task includes an `acceptanceRecord` with `acceptedBy: "lead"`, full `sourceRevision` and `commit` revisions, and nonempty `evidence` and `review` references. The validator checks the record's shape and requires accepted prerequisites for review and accepted states. Running work may use concrete published inputs while prerequisites remain unfinished; a failed or blocked prerequisite rejects dependent running work. The lead judges actual input readiness and assigns exclusive paths. It does not authenticate the author, inspect those commits, or prove the referenced runtime results. Reopening a prerequisite requires the lead to invalidate dependent active or accepted states and stale evidence before the graph passes again.

| Handoff | Source revision | Evidence and review | Commit disposition |
| --- | --- | --- | --- |
| Initial dispatch of W01/W02/W03/W04/W06 | `6439acd6c5f0d38ca93867ae0f6100ecf4cdbee4` | Work running; package acceptance evidence and independent reviews pending | No implementation commit recorded here |
| Execution records and validator v1.1.0 | Same starting revision plus the scoped working-tree changes | `bun docs/validate-plan.ts`: PASS, 74 tasks/309 edges; `bun docs/test-plan-validator.ts`: PASS, 20 negative cases and synthetic accepted-prerequisite progression; `bun run typecheck`: PASS. `review_dependency_graph` independently passed all 20 negative checks; the lead accepted this bounded execution-state code review. No W01/W02/W03/W04/W06 acceptance follows from it. Full policy check reported three findings in concurrently edited `tools/verify/` files and no findings in these docs scripts; those findings were sent to W03. | Lead review accepted for execution-state code only; included in foundation checkpoint `e291921df44b5778a9dd5189c1f383e5752a8b02`. No Git mutations by this worker |

`w02_references` also resumed inventory of retail binaries in the Steam source supplied by the user. Its existing write boundary remains unchanged.

The synthetic acceptance fixture uses invented revisions and evidence text only inside a temporary fixture directory. It proves validator behavior and accepts no real package. This ledger is a passive file maintained through exclusive ownership; no daemon or shared task service coordinates workers.

## Code-first steering

The user directed code imports first, then live human play and optimization, followed by extended hardening and regression infrastructure. W05 is now running from the existing reviewed `docs/reference-contracts.md` and `verification/source-manifest.json`; W01 exhaustive inventory continues in parallel. The lead removed W01 completion as a W05 prerequisite. No package was marked accepted by this change.

W42–W49 no longer wait for W73. W42, W45 and W49 directly require W74 permanent behavior foundations, with expansions inheriting those foundations through their base packages. W73 remains a required real integration milestone, and W66 retains its bootstrap ownership handoff. W02 initial source cases and actual Q2 references are sufficient code inputs. W03 retains practical build/type guards; W04 retains a basic runner. Broad retail/performance capture, extended hardening, regression infrastructure and final real runtime qualification remain mandatory at W67–W71 and release.

This authorized graph change removes W01 → W05 and eight W73 → W42–W49 edges, then adds W74 → W42/W45/W49: 74 packages and 303 edges. The earlier 309-edge validator and review results above describe the previous snapshot only.

W50 also consumes the existing source census and comparison rather than requiring the unfinished exhaustive feature ledger as an import input. Final feature-ledger reconciliation remains required. `bun docs/validate-plan.ts` passes the revised 74-package/303-edge graph with W01–W06 running and none accepted. The existing 20 negative validator cases and synthetic progression case pass after updating their dependent-package fixture from W05 to W07. No new hardening tests were added.

## Published-input dispatch

The lead now starts source ports against concrete published interfaces while prerequisite packages finish. Acceptance dependency edges still govern review and accepted states. No foundation package is marked accepted merely to dispatch downstream code. W05 math, identity, numeric and time contracts are on disk, with content, scene and execution interfaces being implemented under published names. W02/W03/W06 code and reference inputs are frozen for the next imports. The lead is dispatching W08 common services and W09–W15 raw format ports under exclusive ownership.

Validator v1.2.0 rejects running work only when a direct prerequisite is failed or blocked; it leaves concrete-input readiness to the lead. Review and acceptance still require accepted prerequisites. Existing fixture coverage is updated for this scheduling rule, without adding a readiness framework or new hardening suite.

## Foundation checkpoint and active ports

The lead committed and verified foundation checkpoint `e291921df44b5778a9dd5189c1f383e5752a8b02`. W03 is accepted only for EARLY_TOOLING under its current criteria, using that revision for both source and commit identity. The lead reports passing live typecheck, policy, 32 combined tests and the actual compiled validator; the W03 owner also verified compiled tool relocation. `review_w03_policy` closed its tooling review, and the lead independently executed the artifact. This accepts no production engine runtime or release behavior.

W01, W02, W04, W05 and W06 remain running. W08–W15, W18 and W19 are now running under the exclusive assignments above. W05 content interface work is complete as a bounded handoff; the package remains running while its parent and scene/execution peers finish. W06 platform work resumed for the minimal shader FFI extension in `gl.ts` and `gl-programs.ts`. No other package acceptance is recorded.

## Collision, scene and execution imports

W16, W17, W20, W21, W22, W27, W29, W30 and W41 are running under the assignments above. W03 remains accepted only for its recorded early tooling scope. Several format and component owners have completed bounded implementations; their packages remain running until integration and the lead's explicit acceptance. Acceptance order and final feature/runtime requirements are unchanged.

This checkpoint follows the user's code-first direction with limited checks: update the passive ledger and run the basic live validator. It adds no readiness framework, verification framework or fixture suite.

`w08_math` owns the shared `src/core/common-parse.ts`, `src/core/common-error.ts` and `tests/core/parse.test.ts`. The materials owner migrates its callers and removes its own private copies after those shared modules land. The resumed `w13_q12_models` worker owns only the TrueType files within the W21 text split; W21 remains running.

## Core, content and formats checkpoint

Checkpoint `393833a821230bef2b2420f98b22e56a8f1b840e` records 121 files of core, content and format implementation. The lead reports independent snapshot type and policy checks, 90 passing checks and one explicit MD4 skip, plus 73 root-run tests and snapshot TypeScript checking. The skip is retained as a limit of that checkpoint. These results record completed code slices; they do not accept the full packages or establish live engine gameplay.

W24, W25, W26, W52, W74 and W73 entered running state at this checkpoint. Actual source behavior imports target Q1 rerelease `e1m1`, with 428 authored entities across 41 classes, and Q2 `base1`, with 634 entities across 60 classes. The earlier Q1 count of 460 was incorrect; `src/content/q1/foundation/README.md` records the corrected count. Q2 foundation monsters belonged to the resumed `w14_q3_models` worker, and Q2 weapons to `w74_q2_weapons`. The completed font worker `w13_q12_models` was not assigned weapons: the attempted resume failed at the tool thread limit.

W06 gained GL framebuffer work with an actual smoke run. W03 had a bounded allowlist correction, while its accepted early tooling scope remained distinct from final runtime qualification. W73 had started joining the real components at that checkpoint. The current source and runtime section above supersedes that startup status. Neither the checkpoint nor the later bounded live results accept W73 or W74.

This ledger checkpoint uses only the basic live validator. No additional framework, fixture suite or package acceptance was added.

T09 source now joins menu hosting, source administration/rcon and retained filter/master state, profile-path resolution and ordered map rotation. Q3 master DNS no longer waits in the frame loop. Actual Q2 UDP command output and cvar updates passed the existing Application workflow. Local identity/UI, Q1 operators/hosting, Q2 chase HUD and Q3 pause/capture policy are now integrated. T09 remains accepted. The earlier T10 completion assessment was superseded when general mod composition was reopened; T10 remains active. See [T09 progress](functional-targets/status.md#t09-discovery-and-administration-progress).
